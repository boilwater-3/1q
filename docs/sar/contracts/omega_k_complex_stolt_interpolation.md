# SAR Omega-K 复数 Stolt 插值与中间域参考真值契约

## 1. 目标

在已审批 `SarOmegaKGeometry` Stolt 查询位置基础上，冻结内部二维复数频谱的 Stolt
线性插值、越界拒绝和中间域参考真值。首批只批准独立插值能力，不实现参考相位、
二维 IFFT 或完整 Omega-K 聚焦。

## 2. 输入边界

- 输入二维复数频谱为行主序：行表示方位 Doppler bin，列表示距离频率 bin。
- 输入形状必须与 `OmegaKGeometryConfig` 的方位/距离计数一致。
- 源距离频率轴、目标距离频率轴和 Stolt 查询复用 `SarOmegaKGeometry` 结果。
- 几何诊断必须有效，且所有查询位于支持区内。
- 所有输入复数样本和查询频率必须有限。

## 3. 未 shift 频率轴处理

距离频率轴保持现有未 shift FFT 顺序，例如奇数长度可为
`[0, +df, +2df, -2df, -df]`。该内存顺序不是单调频率顺序。

每个方位行插值前必须按频率值构建严格递增的源样本视图。插值只在该排序视图中查找
左右邻点；输出列仍按原未 shift 目标距离频率顺序写回。禁止直接使用内存相邻列作为
插值邻点，也禁止改变最终输出列顺序。

## 4. 复数线性插值

对查询频率 `q`，若排序后的相邻源频率为 `f_0 <= q <= f_1`，复数样本为 `z_0,z_1`：

$$
w = \frac{q-f_0}{f_1-f_0}
$$

$$
z(q) = (1-w)z_0 + wz_1
$$

实部和虚部使用相同权重进行笛卡尔线性插值。首批不执行相位展开、极坐标插值、sinc、
高阶插值或周期边界外推。

查询与源频率在数值容差内相等时必须直接返回对应源样本。任何查询越出排序后的闭支持
区间必须原子拒绝，不裁剪、不填零、不外推。

## 5. 输出与诊断

成功时返回与输入同形状的复数频谱，并至少记录：

- 输入/输出行列数；
- 总查询数、精确命中数和线性插值数；
- 越支持区查询数；
- 最大绝对 Stolt shift；
- 最大插值区间宽度；
- 输入不变与确定性状态。

拒绝时输出矩阵为空，并返回结构化原因：结构无效、几何无效、样本非有限、查询无效、
越支持区或插值失败。

## 6. 首批参考真值

1. 零 Doppler 行：Stolt shift 为零，输出逐样本严格等于输入。
2. 仿射复数谱 `z(f)=a+bf`：任意支持区内查询由线性插值精确恢复。
3. 非对称二维复数谱：验证逐方位行独立、未 shift 排序和输出列顺序。
4. 复指数谱：记录幅相误差趋势，不要求线性插值精确恢复。
5. 越支持区查询：明确拒绝且输出为空。

## 7. 验收

- 仿射复数真值误差在双精度数值容差内。
- 零 Doppler 行严格退化。
- 正负 Doppler 对称查询使用相同插值规则，但不得错误混合不同行。
- 输入不变，相同请求重复执行结果确定。
- 默认与 Eigen 3.3.9 环境测试及 C++11 门通过。

## 8. 后置内容

- 参考函数相位、bulk azimuth compression、residual phase 和二维 IFFT。
- 完整 Omega-K 聚焦、选择器、Session、public API、schema、trace、replay 和 Auto。
- sinc/高阶/极坐标插值、越界填零、网格收缩、尺寸扩展和 GPU。

## 9. 下一阶段

下一阶段只实现内部复数 Stolt 线性插值器、结构化诊断和固定中间域真值测试。
