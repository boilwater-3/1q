# SAR 参考场景矩阵扩展工程契约

## 1. 目标

在不改变 public API、默认算法和尺寸门的前提下，扩展可重复、可比较、可审批的 SAR 参考场景矩阵，补齐当前算法对比主要依赖中心单点和同距离向多目标的代表性缺口。

本阶段只建设内部测试支持、质量矩阵与审批报告，不启用 Auto。

## 2. 固定比较契约

每个矩阵用例必须固定并记录：

- 相同的 raw pulse history、matched filter、逐脉冲轨迹和输出网格。
- 目标位置、RCS、载频、带宽、采样率、PRF、孔径和随机 seed。
- 实际执行算法与插值路径。
- 峰值位置、距离/方位 3dB 宽度、PSLR、ISLR、图像熵。
- 相对参考图像的全局常数相位、单位能量形状 NRMS 和相干相关系数。
- 适用时记录轨迹误差、残余斜距误差和运行时间。

跨算法比较只允许消除全局常数相位并分别单位能量归一化，不得移除相位斜坡、空间变化误差或散焦。

## 3. 首批参考场景

| 编号 | 轨迹与处理 | 目标布局 | 主要比较 |
|---|---|---|---|
| M1 | L1，RDA linear/GBP/BP | 中心单点 | 保持现有基线 |
| M2 | L1，RDA linear/GBP/BP | 距离向偏置单点 | 距离定位与边界趋势 |
| M3 | L1，RDA linear/GBP/BP | 方位向偏置单点 | 方位定位与空间变化误差 |
| M4 | L1，RDA linear/GBP/BP | 二维分离多目标 | 峰值定位、幅度顺序与相互影响 |
| M5 | L2 零扰动/固定种子非零扰动，一阶补偿 RDA | 中心与偏置目标 | 严格退化和补偿改善 |
| M6 | L3 `3 m` 转弯，一阶补偿 RDA/GBP/BP | 中心与偏置目标 | 当前通过区复核 |
| M7 | L3 `12 m` 转弯，一阶补偿 RDA/GBP/BP | 中心与偏置目标 | 明确失效区与 BP 参考 |

首批矩阵继续使用当前已审批小场景参数。硬件参数扫描、时变 PRF 和真实动力学轨迹不进入本阶段。

## 4. 验收门

- 所有场景生成必须确定性重复，固定 seed 场景重复结果一致。
- M1 保持现有已审批质量值，不允许因矩阵重构退化。
- M2/M3 的参考峰值必须落在预期邻域，不得通过扩大窗口掩盖定位偏差。
- M4 必须保持二维目标的预期峰值数量、位置邻域和 RCS 幅度顺序。
- M5 零扰动严格退化；非零扰动补偿相对未补偿路径产生可测改善。
- M6 保持当前一阶补偿通过区结论。
- M7 保持一阶补偿明确失效且 BP/GBP 参考一致。
- 任一新场景若暴露失败，必须记录为适用边界或修复算法；禁止放宽现有门只为通过矩阵。
- 默认与 Conan Eigen 3.3.9 SAR 回归、`sar_ci`、`sar_performance` 和 C++11 兼容门继续通过。

## 5. 输出

- 内部可复用参考场景描述与执行 helper。
- 矩阵级确定性测试。
- 记录每个场景质量与比较结果的审批报告。
- 对失败区域给出结构化边界结论，不生成 Auto 规则。

## 6. 冻结边界

- 不新增 public 配置、输出字段、runtime patch 或 replay schema。
- 不启用 Auto，不冻结自动选择阈值或降级规则。
- 不扩大 RDA `1024x1024` 或 GBP/BP `128x128` 尺寸门。
- 不实现时变 PRF、外部逐脉冲轨迹、真实动力学转弯、二阶补偿、自聚焦、并行/GPU 或全图复矩阵 replay。

## 7. 后续审批

矩阵完成后，根据暴露的首要证据缺口单独选择下一方向：

1. 扩大参考参数与场景覆盖。
2. 审批时变 PRF 或更真实 L3 轨迹。
3. 审批多参考点或二阶补偿。
4. 重新审计 Auto，但不得由本阶段自动批准。
