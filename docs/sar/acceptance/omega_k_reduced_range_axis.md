# SAR Omega-K 收缩距离频率网格与相对延迟轴验收报告

日期：2026-06-11

## 验收范围

阶段 110 实现收缩距离频率轴均匀性与相对延迟轴诊断，并验证现有 FFT facade 的
inverse-range 归一化和往返约定。本阶段不定义绝对斜距或完整 Omega-K 图像。

## 实现结果

- 验证严格递增、有限、至少两点且数值等间隔的收缩频率轴。
- 输出频率间隔、有效带宽、无模糊延迟窗口、相对延迟采样间隔和双程相对距离间隔。
- 输出未 shift inverse FFT 顺序下的周期性非负相对延迟轴。
- 非均匀、逆序、重复、非有限和空输出参数明确拒绝。

## 验证证据

- 默认环境完整 CTest：25/25 passed。
- 默认完整 CTest 覆盖 `SarOmegaKReducedRangeAxisTest.*` 全部 4 个测试。
- FFT inverse/forward 非对称复数频谱往返通过。
- Eigen 3.3.9 运行记录待审批通道恢复后补充。

## 验收结论

阶段 110 当前平台默认环境通过。相对延迟轴只用于内部数值解释；绝对斜距原点、负延迟
中心化、参考相位、方位 IFFT 和完整 Omega-K 聚焦继续后置。
