---
Status: reference
Date: 2026-08-27
Source: 用户提供的验收测试规范截图（OCR 整理）
Retrieval: 2026-08-27 对照 `src/**/AcceptanceRecords*.cpp`、`acceptance_log_mapping_2026-08-24.md`
Note: OCR 文件名与仓库落盘对照见 §0；开关默认 OFF，需 CMake `-DONEQ_ENABLE_*_ACCEPTANCE_LOG=ON` 或 acceptance preset
---

# 功能验收测试判定标准

> **通用判定规则**：各测试项以指定日志文件中**能否记录**对应数据为准——能记录视为**通过**，否则视为**不通过**。

## 0. OCR 文件名 ↔ 仓库落盘

| OCR / 规范文件名 | 仓库实际路径 | 编译开关 |
|---|---|---|
| `Sbirs_acceptance.log` / `SatelliteTarget.log` | `log/sbirs_acceptance.log` | `ONEQ_ENABLE_SBIRS_ACCEPTANCE_LOG` |
| `Fusion_acceptance.log`（第 11–14 项） | 同上（红外宽窄视场，**不在** fusion 文件） | 同上 |
| `Fusion_acceptance.log`（第 15–18 项） | `log/fusion_acceptance.log` | `ONEQ_ENABLE_FUSION_ACCEPTANCE_LOG` |
| `Rir_acceptance.log` / `RadarSingalProcessingDateFlow.log` | `log/rir_acceptance.log` | `ONEQ_ENABLE_RIR_ACCEPTANCE_LOG` |
| `inference_acceptance.log` / `Inference_acceptance.log` | `log/inference_acceptance.log` | `ONEQ_ENABLE_INFERENCE_ACCEPTANCE_LOG` |
| `Precision_acceptance.log` | `log/precision_acceptance.log` | `ONEQ_ENABLE_PRECISION_EVALUATION_LOG` |
| `Rir_scan_pattern.csv` | `log/rir_scan_pattern.csv` | 随 RIR 开关 |
| `integration_events.log` | 示例场景 `log_dir` 下 | 跑 examples 即写，无需库开关 |

**行格式**（各 `*_acceptance.log` 统一；方括号内为规范测试项名称，**不写「验收项」三字，方括号后不写「验收内容：」**）：

```text
仿真时间=1.000s 仿真周期=1 [探测角度计算功能测试] 相对卫星ID=4 目标ID=1001 量测方位/俯仰=(12.312,18.491)°
```

**示例写作规则**（2026-08-28，文档口径；库内现状见各条证据矩阵「超出规范」）：

1. **只写规范原文指标**：一条验收行对应本条判定字段。本条 OCR 点名的字段必须写，**不因其它测试项也有同名字段而省略**；不把其它条目独占的量串写进来。
2. **禁止派生量**：不写由其它字段公式算出的量（如 SNR←功率比、航向←速度、抑制比←功率比、RMSE/CEP50/协方差迹、会话汇总统计、马氏距离² 等）。
3. **无则省略**：无有效值时不写该字段，不写 `无`/`0.000` 占位。
4. **分项拆分**：规范拆成多子项的（如脉压/MTI/MTD），示例仍用**规范测试项名**作方括号（如 `[目标信号增益功能测试]`），子指标在方括号后以 **`字段名=值`** 分行写出，不合并成「大包行」；库内检索关键字见各条「库内 `[…]`」说明，可与规范方括号不同。
5. **key=value**：方括号后每个量须显式 `字段名=值`（含单位），禁止裸写数值（如 `48.573dB`）。
6. **缺失**：库内无对等字段、本条尚未落盘的子指标，在该条「视为不通过」后写 **缺失。** 若能由本条已落盘字段或真值作差算出，再写 **可计算。**，**示例仍补该行**（key=判定指标名）。

**缺失项能否计算**（本文档判定句已标缺失的仅下列三条；示例已按可计算口径补行）：

| 项 | 缺失指标 | 能否计算 | 示例口径 |
|---|---|---|---|
| 32 | 总增益 | **能** | 脉压+MTI+MTD（dB）。主链分子是脉压+`target_processing_gain_db`，与此不同。 |
| 38 | 目标量测误差统计 | **能** | 量测−真值（斜距误差、方位/俯仰误差）。会话 RMSE 另见第 26 项。 |
| 42 | 估计误差指标 | **能** | 滤波 ECEF/速度−真值。 |
7. **存疑**：OCR 截图判定句无法确认（疑图片缺字、串行、与邻项完全相同等），在该条「视为不通过」后写 **存疑。** 示例仍按**测试项名称**区分，不以存疑句当作已冻结规范。
8. **身份字段**：面向天基红外 / RIR 雷达的示例行必须能看出「哪颗星或哪部雷达、哪个目标」。身份字段放在方括号后、指标 key 之前（第 24 项事件行除外：`事件=` 打头，随后 `卫星ID=` / `目标ID=`）。

| 对象 | 必写身份 | 说明 |
|---|---|---|
| SBIRS 相对某星的目标量 | `相对卫星ID=` + `目标ID=` | 双星示例 4 / 104；每星一行 |
| SBIRS 双星定位位置/距离（第 7 项） | `目标ID=` + 两颗 `相对卫星ID=` 与各自 `相对卫星斜距=` | 位置 LLA 只在双星定位时写，两星同一行，不拆行 |
| SBIRS 卫星自身量 | `卫星ID=` | 无目标时不写 `目标ID=` |
| 融合航迹 | `航迹键=` 或 `目标键=`；能映射时再写 `目标ID=` | 天基通道可另写 `源=实体N` |
| RIR 装备级（方向图、指向、扫描调度、波束表、调度策略） | `雷达ID=` | 示例取 `1` |
| RIR 干扰功率（第 30 项） | `干扰源ID=` | 一个 ID；不写平台/设备/发射三元组 |
| RIR 检测 / 测量 / 增益 | `目标ID=` | 已有则保持 |
| RIR 跟踪 / 识别 / 滤波 | `航迹=` + `目标ID=` | 两条都写，不能只写航迹 |
| 不强制 | — | 模块计时（50–54）、AHP（27）、连续运行次数 |

---

## 一、卫星轨道与探测基础（`Sbirs_acceptance.log`）

> **本节登记（2026-08-27）**：第 **1–4** 项**不需要**进行验收日志输出；第 **5** 项仍须按下列判定标准写入 `Sbirs_acceptance.log`。

### 1. 实时计算卫星当前轨道位置功能测试

- **当前修改后情况：完成。按 2026-08-27 登记口径本项不输出验收日志，代码维持现状（无对应验收行）。**

- **验收日志输出**：不需要。
- 原规范判定（不执行）：`Sbirs_acceptance.log` 文件中能够记录卫星轨道位置信息视为通过，否则视为不通过。
- **当前输出**：不输出验收日志（2026-08-27 登记）。轨道解算在仿真主链内运行，无对应 `[验收项：…]` 行。

### 2. 坐标系转换功能测试

- **当前修改后情况：完成。按 2026-08-27 登记口径本项不输出验收日志，代码维持现状（无对应验收行）。**

- **验收日志输出**：不需要。
- 原规范判定（不执行）：
  - `Sbirs_acceptance.log` 文件中能够记录卫星 **ECI** 坐标系坐标视为通过，否则视为不通过。
  - `Sbirs_acceptance.log` 文件中能够记录卫星 **ECEF** 坐标系坐标视为通过，否则视为不通过。
  - `Sbirs_acceptance.log` 文件中能够记录卫星 **WGS-84** 坐标系坐标视为通过，否则视为不通过。
- **当前输出**：不输出验收日志（2026-08-27 登记）。无 ECI/ECEF/WGS-84 独立验收项。

### 3. 卫星轨道动力学计算功能测试

- **当前修改后情况：完成。按 2026-08-27 登记口径本项不输出验收日志，代码维持现状（无对应验收行）。**

- **验收日志输出**：不需要。
- 原规范判定（不执行）：
  - `Sbirs_acceptance.log` 文件中能够记录卫星的**经纬度**坐标视为通过，否则视为不通过。
  - `Sbirs_acceptance.log` 文件中能够记录卫星的**轨道高度**视为通过，否则视为不通过。
  - `Sbirs_acceptance.log` 文件中能够记录卫星的**地心坐标系**坐标视为通过，否则视为不通过。
  - `Sbirs_acceptance.log` 文件中能够记录卫星的**速度**视为通过，否则视为不通过。
- **当前输出**：不输出验收日志（2026-08-27 登记）。

### 4. 龙格库塔积分功能测试

- **当前修改后情况：完成。按 2026-08-27 登记口径本项不输出验收日志，代码维持现状（无对应验收行）。**

- **验收日志输出**：不需要。
- 原规范判定（不执行）：
  - `Sbirs_acceptance.log` 文件中能够记录卫星的**经纬度**坐标视为通过，否则视为不通过。
  - `Sbirs_acceptance.log` 文件中能够记录卫星的**轨道高度**视为通过，否则视为不通过。
  - `Sbirs_acceptance.log` 文件中能够记录卫星的**地心坐标系**坐标视为通过，否则视为不通过。
  - `Sbirs_acceptance.log` 文件中能够记录卫星的**速度**视为通过，否则视为不通过。
- **当前输出**：不输出验收日志（2026-08-27 登记）。

### 5. 探测角度计算功能测试

- **当前修改后情况：完成。`SbirsPipeline.cpp` WFOV 过门循环新增逐目标行 `[探测角度计算功能测试]`：`目标ID=` 与 `量测方位/俯仰(ECI)=(…,…)rad`（带误差量测角经 deg→rad 写出，ECI 参考系；2026-08-28 双星场景已验证落盘）。**

- `Sbirs_acceptance.log` 文件中能够记录实体相对卫星的**方位角和俯仰角**（**ECI 坐标、弧度制**）信息视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索，对照现有代码与 `log/sbirs_acceptance.log`）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 参考系 | **部分符合** | `真值方位/俯仰`、`量测方位/俯仰` 来自 ECI 视线 `los = target_eci − sat_eci`，经 `ComputeAzimuthDeg/ComputeElevationDeg`（ECI 极坐标，非探测器局部系）。`探测器系方位/俯仰` 为 **Body→Sensor 链旋转后的传感器系角**，**不是**本条要求的 ECI 角。 |
  | 单位 | **不符合** | 验收行写 **度（°）**，未写 **rad**。公开主输出 `SbirsDetectionRecord::azimuth_rad/elevation_rad` 已是 ECI 弧度（az∈[0, 2π)、el∈[-π/2, π/2]），但**不进** `sbirs_acceptance.log`。 |
  | 相对卫星标识 | **部分符合** | 规范示例已补 `相对卫星ID=`；库内第 5 项行仅有 `目标ID=`（本次不改代码）。 |
  | 多目标 A/B/C | **符合** | 每个过 WFOV 距离/视场/SNR 门的候选目标各写一行，`目标ID=` 区分；未过门目标无该行。 |
  | 验收项名 | **无独立项** | 尚无 `[探测角度计算功能测试]`；角度合并在 `[验收项：目标可探测性与动态参数]`。 |

- **实现**（如何写出 §0 示例行；本次仅文档，不改代码）：
  - **时机**：`SbirsPipeline.cpp` WFOV 候选过距离/视场/SNR 门后，与现有 `目标可探测性与动态参数` 同循环，**逐目标**写一行。
  - **数据**：ECI 视线 `los = target.position_eci_m − satellite_position_eci_m` → 量测角取 `candidate.measured_azimuth_deg / measured_elevation_deg`（已过 `ApplyAngularErrorModel`）；规范要求弧度时经 `ToEciAzimuthRad` / `ToEciElevationRad` 转换后写入。
  - **调用**：`SBIRS_ACCEPTANCE_ITEM(sim_time_sec, cycle, "探测角度计算功能测试", content)`，`content` 须含 **`相对卫星ID=`**、`目标ID=` 与 `量测方位/俯仰=(az,el)…`（ECI；单位与规范一致）。
  - **示例**：
    ```text
    仿真时间=1.000s 仿真周期=1 [探测角度计算功能测试] 相对卫星ID=4 目标ID=1001 量测方位/俯仰=(12.312,18.491)°
    仿真时间=1.000s 仿真周期=1 [探测角度计算功能测试] 相对卫星ID=4 目标ID=1002 量测方位/俯仰=(45.120,22.300)°
    仿真时间=1.000s 仿真周期=1 [探测角度计算功能测试] 相对卫星ID=104 目标ID=1001 量测方位/俯仰=(12.310,18.488)°
    ```

---

## 二、卫星目标探测（OCR：`SatelliteTarget.log` → `log/sbirs_acceptance.log`）

### 6. 最大探测距离计算功能测试

- **当前修改后情况：完成。改名 `[最大探测距离计算功能测试]`，行含 `相对卫星ID= 目标ID= 相对卫星最大探测距离=…m`；`窄场最大探测距离`（超出规范附加字段）已删。卫星 ID 由示例层经 `SbirsSession::SetSatelliteEntityId` 透传（双星 4/104/204 已验证）。**

- `SatelliteTarget.log` 文件中能够记录目标实体相对卫星的**最大探测距离**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 主字段 | **符合** | 宽场 `d_max` 已由 `ComputeMaxDetectionRangeM`（WFOV SNR 门反解）写入 `相对卫星最大探测距离`；逐过门候选目标、逐周期。 |
  | 相对卫星标识 | **不符合** | 现有行仅有 `目标ID=`，**未标明相对哪颗卫星**；双星场景靠分日志文件/分 pipeline 实例区分，读单文件时无法直接看出 `相对卫星ID`。 |
  | 附加字段 | **超出规范** | 同条另写 `窄场最大探测距离`（窄场参数重算），规范仅要求最大探测距离。 |
  | 多目标 | **符合** | 每个过 WFOV 门的候选各一行，`目标ID=` 区分。 |
  | 验收项名 | **部分符合** | 库内 `[验收项：最大探测距离]`，尚无 `[最大探测距离计算功能测试]`。 |
  | 日志文件 | **部分符合** | 落盘 `log/sbirs_acceptance.log`（非 OCR 名 `SatelliteTarget.log`）。 |

- **实现**（如何写出 §0 示例行；本次仅文档，不改代码）：
  - **时机**：`SbirsPipeline.cpp` WFOV 候选过门后，与 `目标可探测性与动态参数` 同循环，逐目标一行。
  - **数据**：`max_detection_range_m`（本周期宽场 `d_max`）；须同时写 **`相对卫星ID=`**（场景实体/融合源 ID，如双星 `sbirs_dual_sat_fix` 为 4 与 104），再写 `目标ID=` 与 `相对卫星最大探测距离`。
  - **调用**：`SBIRS_ACCEPTANCE_ITEM(sim_time_sec, cycle, "最大探测距离计算功能测试", content)`。
  - **示例**：
    ```text
    仿真时间=1.000s 仿真周期=1 [最大探测距离计算功能测试] 相对卫星ID=4 目标ID=1001 相对卫星最大探测距离=4500000.0m
    仿真时间=1.000s 仿真周期=1 [最大探测距离计算功能测试] 相对卫星ID=104 目标ID=1001 相对卫星最大探测距离=4200000.0m
    ```

### 7. 目标可探测性与动态参数生成功能测试（修改）

- **当前修改后情况：完成。改名 `[目标可探测性与动态参数生成功能测试]`。位置 LLA 仅在双星定位时写出，同一行带两颗 `相对卫星ID=` 与各自 `相对卫星斜距=`；另含量测方位/俯仰(ECI rad)、`红外可探测=`。ECEF 位置/探测器系角/相对角速度/SNR 等超出规范字段已删。**

- `SatelliteTarget.log` 文件中，在**双星定位**的特殊情况下能够记录目标的**位置、距离**视为通过，否则视为不通过。
- `SatelliteTarget.log` 文件中能够记录目标的**角度及红外可探测性**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 位置（LLA） | **不符合** | 库内写 `ECEF位置m=`（场景输入 ECEF），**非**规范要求的经纬高（LLA）。 |
  | 相对卫星距离 | **部分符合** | 库内有单星 `斜距`；规范示例须同一行写出两颗 `相对卫星ID=` 与各自 `相对卫星斜距=`。 |
  | 量测角（ECI rad） | **不符合** | 写 `量测方位/俯仰=(…,…)°`（度）；另混写 `探测器系方位/俯仰`（非 ECI）。规范要求 **ECI 弧度制**。 |
  | 红外可探测性 | **符合** | `红外可探测=是`（行只写过门候选）。 |
  | 双星定位 | **部分符合** | 规范示例：位置+两星 ID+两斜距写同一行。库内仍每星一行，且无第二颗星的 ID/斜距。 |
  | 验收项名 | **部分符合** | 库内 `[验收项：目标可探测性与动态参数]`，尚无 `[目标可探测性与动态参数生成功能测试]`。 |
  | 日志文件 | **部分符合** | 落盘 `log/sbirs_acceptance.log`（非 OCR 名 `SatelliteTarget.log`）。 |

- **实现**（如何写出 §0 示例行；本次仅文档，不改代码）：
  - **时机**：同第 6 项 WFOV 过门；**双星定位**时同一目标写**一行**（位置只在此情况下出现），不得每星拆行。
  - **数据**：目标 **LLA**（双星定位结果，只写一次）；两颗参与定位卫星的 **`相对卫星ID=`** 与各自 **`相对卫星斜距=`**；量测角按星写 ECI **rad**；`红外可探测=是`。
  - **调用**：`SBIRS_ACCEPTANCE_ITEM(sim_time_sec, cycle, "目标可探测性与动态参数生成功能测试", content)`。
  - **示例**：
    ```text
    仿真时间=1.000s 仿真周期=1 [目标可探测性与动态参数生成功能测试] 目标ID=1001 位置LLA=(31.120,120.750,3500088.0) 相对卫星ID=4 相对卫星斜距=3500000.0m 量测方位/俯仰(ECI)=(0.215,0.323)rad 相对卫星ID=104 相对卫星斜距=4200000.0m 量测方位/俯仰(ECI)=(0.218,0.325)rad 红外可探测=是
    ```

### 8. 卫星自身定位误差功能测试

- **当前修改后情况：完成。合并为单行 `[卫星自身定位误差功能测试]`：`卫星ID= 真值ECEF=… 实际ECEF=… 定位误差ECEF=(…)m |定位误差|=…m`；东/北/天统计、会话 n、均值/RMS、分量σ、口径说明（规范口径明确不输出）连同旧 `[卫星自身定位误差]`/`[卫星ECEF三维导航定位误差]` 两行一并删除；传入 ECEF 不可用分支如实写 `真值ECEF=不可用`。**

- `SatelliteTarget.log` 文件中能够记录卫星自身**定位误差统计数据**视为通过，否则视为不通过。
- **规范口径（文档澄清）**：记录卫星 **真值 ECEF**（外部传入）、**实际 ECEF**（带误差）及 **定位误差**（ECEF 误差向量与模长，m）；**不输出**东/北/天统计、会话 n、均值/RMS、分量 σ、口径说明等。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 真值/实际 ECEF | **部分符合** | `[验收项：卫星ECEF三维导航定位误差]` 含 `真值ECEF=`、`错误三维ECEF=`；真值来自外部传入。 |
  | 定位误差 | **部分符合** | 同条含 `误差向量=(…,…)m`、`|误差|=…m`（即误差多少）；但字段名/写法未对齐规范示例，且混入口径说明。 |
  | 统计量 | **超出规范** | `[验收项：卫星自身定位误差]` 写东/北/天、均值/RMS、σ 等会话统计；不应输出。 |
  | 卫星标识 | **不符合** | 单行未写 `卫星ID=`；双星靠分实例/分文件区分。 |
  | 写入频率 | **符合** | 每周期（`WriteSbirsOrbitSample`）；按卫星实例分账。 |
  | 验收项名 | **部分符合** | 库内拆为两项 `[卫星自身定位误差]` / `[卫星ECEF三维导航定位误差]`，尚无 `[卫星自身定位误差功能测试]`。 |
  | 日志文件 | **部分符合** | 落盘 `log/sbirs_acceptance.log`（非 OCR 名 `SatelliteTarget.log`）。 |

- **实现**（如何写出 §0 示例行；本次仅文档，不改代码）：
  - **时机**：`SbirsPipeline.cpp` 每周期调用 `WriteSbirsOrbitSample`（`SbirsAcceptanceRecords.cpp`）。
  - **数据**：**真值ECEF** = 外部传入；**实际ECEF** = 真值 + 误差抽样；**定位误差ECEF** = 实际 − 真值（三分量，m），**`|定位误差|`** = 模长（m）；附 `卫星ID=`。
  - **调用**：`SBIRS_ACCEPTANCE_ITEM(..., "卫星自身定位误差功能测试", content)`；不写东/北/天会话统计或口径字段。
  - **示例**：
    ```text
    仿真时间=1.000s 仿真周期=1 [卫星自身定位误差功能测试] 卫星ID=4 真值ECEF=(-2761051.5,4782281.5,3681832.1)m 实际ECEF=(-2760903.0,4782120.0,3681882.1)m 定位误差ECEF=(148.5,-161.5,50.0)m |定位误差|=225.0m
    仿真时间=1.000s 仿真周期=1 [卫星自身定位误差功能测试] 卫星ID=104 真值ECEF=(-2759563.0,4782120.0,3683158.1)m 实际ECEF=(-2759415.0,4781958.0,3683208.0)m 定位误差ECEF=(148.0,-162.0,49.9)m |定位误差|=225.2m
    ```

### 9. 红外载荷角误差功能测试

- **当前修改后情况：完成。新行 `[红外载荷角误差功能测试]`：`相对卫星ID= 目标ID= 测角残差方位/俯仰(ECI)=(…)rad`（量测−真值，rad）。既有 `[红外系统测角误差]` 行保留——它是第 55 项的数据源，待 26–55 批次按其口径调整，不在本批删除。**

- `SatelliteTarget.log` 文件中能够记录红外探测器对高速动态目标进行方位角和俯仰角测量时的**测角残差**视为通过，否则视为不通过。
- **规范口径（文档澄清）**：只记录相对指定卫星、指定目标的 **ECI 测角残差**（量测角 − 真值角，弧度）；**不输出**测量/真值角本身、会话 RMSE、σ 来源等。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 测角残差 | **部分符合** | `[验收项：红外载荷测角误差]` 含 `方位测角误差`、`俯仰测角误差`（°，ECI 量测−真值）；语义对，**单位为 deg 非 rad**。 |
  | 相对卫星标识 | **不符合** | 仅有 `目标ID=`，无 `相对卫星ID=`。 |
  | 多余字段 | **超出规范** | 同函数另写 `[验收项：红外系统测角误差]`（测量/真值/偏差/RMS/σ 来源），本条不应输出。 |
  | 写入频率 | **符合** | WFOV 过门与 NFOV 跟踪路径均调用 `WriteSbirsAngleError`；逐目标、逐周期。 |
  | 验收项名 | **部分符合** | 库内 `[验收项：红外载荷测角误差]`，尚无 `[红外载荷角误差功能测试]`。 |
  | 日志文件 | **部分符合** | 落盘 `log/sbirs_acceptance.log`（非 OCR 名 `SatelliteTarget.log`）。 |

- **实现**（如何写出 §0 示例行；本次仅文档，不改代码）：
  - **时机**：`SbirsPipeline.cpp` WFOV/NFOV 路径，量测角与 ECI 真值角已算后调用 `WriteSbirsAngleError`。
  - **数据**：`az_err` / `el_err`（ECI，`AzimuthDelta` + 俯仰差）→ 转 **rad** 写 `测角残差方位/俯仰(ECI)=`；附 `相对卫星ID=`、`目标ID=`。
  - **调用**：`SBIRS_ACCEPTANCE_ITEM(..., "红外载荷角误差功能测试", content)`；不写 `红外系统测角误差` 行。
  - **示例**：
    ```text
    仿真时间=1.000s 仿真周期=1 [红外载荷角误差功能测试] 相对卫星ID=4 目标ID=1001 测角残差方位/俯仰(ECI)=(0.000209,-0.000157)rad
    仿真时间=1.000s 仿真周期=1 [红外载荷角误差功能测试] 相对卫星ID=104 目标ID=1001 测角残差方位/俯仰(ECI)=(0.000198,-0.000142)rad
    ```

### 10. 安装矩阵误差功能测试

- **当前修改后情况：完成。新行 `[安装矩阵误差功能测试]`：`卫星ID= 理想指向R=… 实际指向R=… 安装误差矩阵R=…`；`失准角yaw/pitch/roll`（超出规范）已删；改为首个执行周期写一次（周期 0 语义，卫星 ID 此时已可标注），`ApplyConfig` 重抽失准后下一执行周期重写一行。**

- `SatelliteTarget.log` 文件中能够记录传感器**理想指向、传感器实际指向**视为通过，否则视为不通过。
- `SatelliteTarget.log` 文件中能够记录传感器**安装误差矩阵**视为通过，否则视为不通过。
- **规范口径（文档澄清）**：只记录 **`理想指向R`**（标称安装矩阵）、**`实际指向R`**（含失准合成指向）、**`安装误差矩阵R`**（失准矩阵）；**不输出** `失准角yaw/pitch/roll` 等欧拉角字段。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 理想/实际指向 | **符合** | `[验收项：安装矩阵误差]` 含 `传感器安装R`（标称）、`实际指向R`（mount⊗失准合成）。 |
  | 安装误差矩阵 | **符合** | 同条含 `失准R`（失准旋转，即安装误差矩阵）。 |
  | 多余字段 | **超出规范** | 同条另写 `失准角yaw/pitch/roll=(…,…)°`；规范仅要矩阵。 |
  | 卫星标识 | **不符合** | 会话首行无 `卫星ID=`；双星靠分实例各写一条。 |
  | 写入频率 | **符合** | 构造/`ApplyConfig` 时写一次（周期 0）；**标识行全会话一条**，安装矩阵不随周期变。 |
  | 验收项名 | **部分符合** | 库内 `[验收项：安装矩阵误差]`，尚无 `[安装矩阵误差功能测试]`。 |
  | 日志文件 | **部分符合** | 落盘 `log/sbirs_acceptance.log`（非 OCR 名 `SatelliteTarget.log`）。 |

- **实现**（如何写出 §0 示例行；本次仅文档，不改代码）：
  - **时机**：`SbirsPipeline` 构造或 `ApplyConfig` 重抽失准角后调用 `WriteSbirsInstallMatrices`（`SbirsAcceptanceRecords.cpp`）。
  - **数据**：`BuildRotationMatrix(mount_deg)` → 理想指向；`BuildRotationMatrix(misalignment_deg)` → 安装误差矩阵；`Compose(mount, Inverse(mis))` → 实际指向；附 `卫星ID=`。
  - **调用**：`SBIRS_ACCEPTANCE_ITEM(0, 0, "安装矩阵误差功能测试", content)`；字段名对齐规范：`理想指向R`、`实际指向R`、`安装误差矩阵R`。**`[安装矩阵误差功能测试]` 会话内只写一次**（周期 0；安装矩阵全会话不变，双星配置相同时不必重复标识行）。
  - **示例**：
    ```text
    仿真时间=0.000s 仿真周期=0 [安装矩阵误差功能测试] 卫星ID=4 理想指向R=[[1.000000,0.000000,0.000000];[0.000000,1.000000,0.000000];[0.000000,0.000000,1.000000]] 实际指向R=[[0.999996,0.001744,-0.002619];[-0.001744,0.999997,0.000436];[0.002619,-0.000431,0.999996]] 安装误差矩阵R=[[0.999996,-0.001745,0.002618];[0.001744,0.999997,-0.000436];[-0.002619,0.000431,0.999996]]
    ```

---

## 三、融合与跟踪

> OCR 将第 **11–14** 项写在 `Fusion_acceptance.log`；仓库实际落在 **`log/sbirs_acceptance.log`**（红外宽窄视场层）。第 **15–18** 项在 **`log/fusion_acceptance.log`**。

### 11. 红外预警卫星大幅面扫描与探测（`log/sbirs_acceptance.log`）

- **当前修改后情况：完成。逐目标行 `[红外预警卫星大幅面扫描与探测功能测试]`：`卫星ID= 扫描幅宽az/el(ECI)=(…)rad 目标ID= 接收功率=…W 信号能量=…J SNR=… 探测概率=… 检测标志=是`；原逐周期"扫描幅宽+扫描掠角"独立行（超出规范）已删。**

- `Fusion_acceptance.log` 文件中能够记录红外预警卫星的**扫描幅宽**视为通过，否则视为不通过。
- `Fusion_acceptance.log` 文件中能够记录红外预警卫星的**目标辐射**视为通过，否则视为不通过。
- `Fusion_acceptance.log` 文件中能够记录红外预警卫星的**检测标志**视为通过，否则视为不通过。
- `Fusion_acceptance.log` 文件中能够记录红外预警卫星的**探测 SNR** 视为通过，否则视为不通过。
- `Fusion_acceptance.log` 文件中能够记录红外预警卫星的**探测概率**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 扫描幅宽/辐射/SNR/Pd | **符合** | `[验收项：大幅面扫描与探测]` 含 `扫描幅宽az/el`、`接收功率`、`信号能量`、`SNR`、`探测概率`（Marcum Q 派生）。 |
  | 扫描幅宽单位 | **不符合** | 库内 `扫描幅宽az/el=(…,…)°`（度）；规范要求 **ECI 弧度制**。 |
  | 检测标志 | **部分符合** | 无独立 `检测标志=` 字段；过门候选隐含可探测，未单独写是/否。 |
  | 卫星标识 | **不符合** | 无 `卫星ID=`。 |
  | 验收项名/文件 | **部分符合** | 库内 `[大幅面扫描与探测]`；落盘 `sbirs_acceptance.log`（非 OCR `Fusion_acceptance.log`）。 |

- **实现**（如何写出 §0 示例行；本次仅文档，不改代码）：
  - **时机**：WFOV 过门逐目标写一行；周期首行可另写扫描幅宽摘要。
  - **数据**：宽场 FOV 经 `deg→rad` 写 **`扫描幅宽az/el(ECI)=(az,el)rad`**；`received_power_w`、`signal_energy_j`、`snr`、`pd_wide`；`检测标志=是`；附 `卫星ID=`、`目标ID=`。
  - **示例**：
    ```text
    仿真时间=1.000s 仿真周期=1 [红外预警卫星大幅面扫描与探测功能测试] 卫星ID=4 扫描幅宽az/el(ECI)=(0.349,0.349)rad 目标ID=1001 接收功率=1.2e-14W 信号能量=1.2e-16J SNR=8.210 探测概率=0.8520 检测标志=是
    ```

### 12. 宽视场扫描探测功能测试（`log/sbirs_acceptance.log`）

- **当前修改后情况：完成。改名 `[宽视场扫描探测功能测试]`，行首加 `卫星ID=`；覆盖四角/中心/驻留字段维持。**

- `Fusion_acceptance.log` 文件中能够记录各扫描周期的**地面覆盖范围的区域坐标**视为通过，否则视为不通过。
- `Fusion_acceptance.log` 文件中能够记录各覆盖区域的**驻留时间**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 覆盖区坐标/驻留 | **符合** | `[验收项：宽视场扫描探测]` 含 `覆盖四角经纬`、`中心`、`驻留`（s）；指向太空记 `miss`。 |
  | 卫星标识 | **不符合** | 无 `卫星ID=`。 |
  | 验收项名/文件 | **部分符合** | 库内 `[宽视场扫描探测]`；落盘 `sbirs_acceptance.log`。 |

- **实现**（如何写出 §0 示例行；本次仅文档，不改代码）：
  - **时机**：每周期 WFOV footprint 计算后写一行。
  - **数据**：四角经纬（`;` 分隔）、中心经纬、驻留 = 方位视场/扫描速率；附 `卫星ID=`。
  - **示例**：
    ```text
    仿真时间=1.000s 仿真周期=1 [宽视场扫描探测功能测试] 卫星ID=4 覆盖四角经纬=[31.20,120.10;31.20,121.40;30.10,121.40;30.10,120.10] 中心=(30.65,120.75) 驻留=2.000s
    ```

### 13. 窄视场跟踪探测功能测试（`log/sbirs_acceptance.log`）（修改）

- **当前修改后情况：完成。改名 `[窄视场跟踪探测功能测试]`，行首加 `卫星ID=`；脱靶量/跟踪状态/信号能量/SNR 字段维持。**

- `Fusion_acceptance.log` 文件中能够记录各采样时刻目标在焦平面上的**脱靶量**视为通过，否则视为不通过。
- `Fusion_acceptance.log` 文件中能够记录各采样时刻**跟踪状态**视为通过，否则视为不通过。
- `Fusion_acceptance.log` 文件中能够记录各采样时刻**目标信号能量**视为通过，否则视为不通过。
- `Fusion_acceptance.log` 文件中能够记录各采样时刻**信噪比**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 脱靶量/状态/能量/SNR | **符合** | `[验收项：窄视场跟踪探测]` 含 `脱靶量m/像素`、`跟踪状态`、`信号能量`、`SNR`。 |
  | 卫星标识 | **不符合** | 无 `卫星ID=`。 |
  | 验收项名/文件 | **部分符合** | 库内 `[窄视场跟踪探测]`；落盘 `sbirs_acceptance.log`。 |

- **实现**（如何写出 §0 示例行；本次仅文档，不改代码）：
  - **时机**：NFOV 跟踪路径，逐目标、逐周期。
  - **数据**：焦平面脱靶（m+像素）、`跟踪/滑行`、`signal_energy_j`、`snr`；附 `卫星ID=`、`目标ID=`。
  - **示例**：
    ```text
    仿真时间=8.000s 仿真周期=8 [窄视场跟踪探测功能测试] 卫星ID=4 目标ID=1001 脱靶量m=(0.0012,-0.0008) 跟踪状态=跟踪 信号能量=2.1e-16J SNR=9.100
    ```

### 14. 宽窄视场联合探测功能测试（`log/sbirs_acceptance.log`）

- **当前修改后情况：完成。按规范三条分行：①`宽视场扫描覆盖区域=…`（周期行）②`宽场疑似列表=[…]`（候选汇总行，`数量=` 超出规范已删）③窄场状态行 `窄场方位/俯仰(ECI)=(…)rad 跟踪状态=… SNR=…`（跟踪逐目标行 + 捕获事件行同项名）；`连续命中/序列确认` 超出规范字段及 E3 计数行已删。**

- `Fusion_acceptance.log` 文件中能够记录**宽视场扫描覆盖区域**视为通过，否则视为不通过。
- `Fusion_acceptance.log` 文件中能够记录**疑似目标列表**视为通过，否则视为不通过。
- `Fusion_acceptance.log` 文件中能够记录**窄视场跟踪目标的精确状态序列**视为通过，否则视为不通过。精确状态序列」要体现的是：同一个目标 ID，在窄场阶段每个周期状态怎么变。例如：

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 宽视场扫描覆盖区域 | **不符合** | `[验收项：宽窄视场联合探测]` **无**覆盖四角/中心/驻留；同内容仅在 `[宽视场扫描探测]`（第 12 项）写出，本条缺字段。 |
  | 疑似目标列表 | **符合** | 汇总行 `宽场疑似列表=[id,…]`（候选非空时写）。 |
  | 窄视场精确状态序列 | **部分符合** | 捕获/跟踪行含窄场角、SNR、捕获/跟踪状态；**无**独立逐拍序列字段；库内窄场角为 **度**，规范要 **ECI rad**。 |
  | 多余字段 | **超出规范** | 库内另写 `连续命中=n/门限`、`序列确认=达标/未达标`（宽场确认逻辑），规范三条未要求。 |
  | 卫星标识 | **不符合** | 无 `卫星ID=`。 |
  | 验收项名/文件 | **部分符合** | 库内 `[宽窄视场联合探测]`；落盘 `sbirs_acceptance.log`。 |

- **实现**（如何写出 §0 示例行；本次仅文档，不改代码）：
  - **时机**：三条规范字段**分行**写——覆盖区（周期首行）、疑似列表（候选汇总）、窄场状态（NFOV 捕获/跟踪逐目标）。
  - **数据**：
    - **宽视场扫描覆盖区域**：同第 12 项 footprint（四角经纬、中心、驻留）；附 `卫星ID=`。
    - **疑似目标列表**：`宽场疑似列表=[id,…]`；**不写** `连续命中` / `序列确认`。
    - **窄视场精确状态序列**：逐目标 `窄场方位/俯仰(ECI)=(az,el)rad`、`跟踪状态`、`SNR`；附 `卫星ID=`、`目标ID=`。
  - **示例**（三条规范字段各一行，不合并）：
    ```text
    仿真时间=5.000s 仿真周期=5 [宽窄视场联合探测功能测试] 卫星ID=4 宽视场扫描覆盖区域=覆盖四角经纬=[31.20,120.10;31.20,121.40;30.10,121.40;30.10,120.10] 中心=(30.65,120.75) 驻留=2.000s
    仿真时间=5.000s 仿真周期=5 [宽窄视场联合探测功能测试] 卫星ID=4 宽场疑似列表=[1001]
    仿真时间=8.000s 仿真周期=8 [宽窄视场联合探测功能测试] 卫星ID=4 目标ID=1001 窄场方位/俯仰(ECI)=(0.215,0.323)rad 跟踪状态=跟踪 SNR=9.100
    ```

### 15. 多传感器目标跟踪功能测试（`log/fusion_acceptance.log`）

- **当前修改后情况：完成。改名 `[多传感器目标跟踪功能测试]`；行内容 `航迹键= ECEF位置m=… 协方差对角=…` 与文档示例一致（完整 6×6 仍在同文件 `[UKF滤波]` 行）。**

- `Fusion_acceptance.log` 文件中能够记录目标在地心地固系下的**三维位置坐标、误差协方差矩阵**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | ECEF/协方差 | **部分符合** | `[验收项：多传感器目标跟踪]` 含 `ECEF位置m`、`协方差对角`（6 维）；**非完整 6×6 矩阵**。 |
  | 目标身份 | **部分符合** | 规范示例已补 `目标ID=`；库内仅 `航迹键=`（本次不改代码）。 |
  | 验收项名 | **部分符合** | 库内 `[多传感器目标跟踪]`，尚无 `[多传感器目标跟踪功能测试]`。 |

- **实现**（如何写出 §0 示例行；本次仅文档，不改代码）：
  - **时机**：`WriteFusionAcceptance` 逐航迹、逐周期。
  - **数据**：`航迹键`、`ECEF位置m`、协方差（对角或 6×6）；附 `目标ID=`（若可映射）。
  - **示例**：
    ```text
    仿真时间=10.000s 仿真周期=10 [多传感器目标跟踪功能测试] 航迹键=1 目标ID=1001 ECEF位置m=(-2300120.1,4800450.2,3500088.0) 协方差对角=(100.0,100.0,100.0,25.0,25.0,25.0)
    ```

### 16. 多传感器接力跟踪功能测试（`log/fusion_acceptance.log`）（修改）

- **当前修改后情况：完成。改名 `[多传感器接力跟踪功能测试]`。位置/速度/加速度归入 `多源数据融合后的目标跟踪结果=`（红外只测向）；**仅当本航迹确有多源融合（≥2 个通道）时写出，单星跟踪不写该字段。** 单星跟踪时融合模块仍按目标速度方向判断下一颗卫星视场归属，**接力计划与交接指令都写**（含 **`接力卫星ID=`**）。不写预测轨迹。**

- `Fusion_acceptance.log` 文件中能够记录跟踪目标的**位置、速度、加速度**，**初始卫星对目标的剩余覆盖时间**，**预测目标的运动轨迹**，**接力计划**，**交接指令**及**多源数据融合后的目标跟踪结果**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 位置/速度/加速度 | **部分符合** | 库内单/多源都平铺 `位置LLA`/`速度模`/`加速度模`。规范：这三项即融合跟踪结果，**仅多源融合时**写入 `多源数据融合后的目标跟踪结果=`，单星跟踪不写。 |
  | 剩余覆盖/接力/交接 | **部分符合** | 库内：`剩余覆盖时间`、`接力计划` 仅离场源；交接需同航迹第二源。规范：单星也写接力计划与交接指令——融合模块按目标速度方向判定下一颗卫星视场，`接力计划` 含 **`接力卫星ID=`**。 |
  | 预测轨迹 | **不输出** | 本条示例不写该字段。 |
  | 融合结果 | **部分符合** | **仅多源融合时**写 `多源数据融合后的目标跟踪结果=`；单星不写。库内另写 `融合航迹数`/`置信度`，不采用。 |

- **实现**（如何写出 §0 示例行；本次仅文档，不改代码）：
  - **时机**：`WriteFusionAcceptance` 与多传感器跟踪同周期。
  - **数据**：`目标键=`；初始卫星剩余覆盖。**仅当本航迹有多源融合（≥2 个通道）时**写 `多源数据融合后的目标跟踪结果=`（位置 LLA、速度模、加速度模）；单星跟踪省略该字段。无论单星还是多源，都写 **接力计划**（离场星 + **`接力卫星ID=`**，接力星=目标速度方向上的下一颗视场归属卫星）和 **交接指令**。不写预测轨迹。
  - **示例**（多源融合 + 接力）：
    ```text
    仿真时间=50.000s 仿真周期=50 [多传感器接力跟踪功能测试] 目标键=1 多源数据融合后的目标跟踪结果=位置LLA=(31.120,120.750,3500088.0) 速度模=2500.000m/s 加速度模=0.000m/s² 剩余覆盖时间=108.1s(卫星ID=4) 接力计划=卫星ID=4预计108.1s离开视场 接力卫星ID=104 交接指令=卫星ID=4(剩余108.1s离场)→卫星ID=104接管
    ```
  - **示例**（单星跟踪：不写融合跟踪结果；接力计划/交接指令仍写）：
    ```text
    仿真时间=50.000s 仿真周期=50 [多传感器接力跟踪功能测试] 目标键=1 剩余覆盖时间=108.1s(卫星ID=4) 接力计划=卫星ID=4预计108.1s离开视场 接力卫星ID=104 交接指令=卫星ID=4(剩余108.1s离场)→卫星ID=104接管
    ```

### 17. 协同探测信息融合功能测试（`log/fusion_acceptance.log`）

- **当前修改后情况：完成。改名 `[协同探测信息融合功能测试]`；字段不变。**

- `Fusion_acceptance.log` 文件中能够记录跟踪目标的**协同探测融合信息**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 协同融合信息 | **符合** | `[验收项：协同探测信息融合]` 含 `参与通道`、`定位LLA`、`测向信息`（逐源方位/俯仰）、`融合目标数`。 |
  | 验收项名 | **部分符合** | 库内名一致，方括号尚无 `功能测试` 后缀。 |

- **实现**（如何写出 §0 示例行；本次仅文档，不改代码）：
  - **时机**：`WriteFusionAcceptance` 逐目标一行。
  - **示例**：
    ```text
    仿真时间=10.000s 仿真周期=10 [协同探测信息融合功能测试] 目标键=1 参与通道=实体4+实体104 定位LLA=(31.120,120.750,3500088.0) 测向信息=[实体4(方位12.3°/俯仰18.5°);实体104(方位12.1°/俯仰18.4°)] 融合目标数=2
    ```

### 18. 标准卡尔曼滤波功能测试（`log/fusion_acceptance.log`）

- **当前修改后情况：完成。fusion 文件新增 `[标准卡尔曼滤波功能测试]` 行：逐（航迹,源）4 维 [az,ω_az,el,ω_el] 线性标准 KF（复用 SBIRS kAngleCvKf 估计器，F=CV、R=`default_bearing_noise_sigma_rad`），输出 `滤波方位/俯仰=(…)rad 方位变化率=…rad/s 俯仰变化率=…rad/s`，逐周期一行（2026-08-28 双星场景 320 行已验证）。与文档示例的一处如实差异：融合侧视线角是源本地北东天参考（非 ECI），行内不标 ECI；SBIRS 侧 `[目标角度状态估计]` 行（真 ECI 滤波角+变化率）保留不动。**

- `Fusion_acceptance.log` 文件中能够记录滤波后的目标**方位角、俯仰角及其变化率**视为通过，否则视为不通过。
- `Fusion_acceptance.log` 文件中能够记录连续时刻目标**视线方向的平滑估计结果**视为通过，否则视为不通过。
其中：
「视线方向」 = 卫星（红外载荷）看向目标的那条线的方向，用 方位角 + 俯仰角 表示（ECI 下常见写法）。
「平滑估计」 = 不是原始点迹里带噪的角，而是 卡尔曼滤波后 的角——相邻周期之间不会乱跳，比量测更稳。
「连续时刻」 = 多个仿真周期各写一行，能按时间串成一条曲线，不是只给某一拍快照。
「平滑估计」 = 不是原始点迹里带噪的角，而是 卡尔曼滤波后 的角——相邻周期之间不会乱跳，比量测更稳。
「连续时刻」 = 多个仿真周期各写一行，能按时间串成一条曲线，不是只给某一拍快照。
- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 融合 UKF | **部分符合** | `[验收项：UKF滤波]` 写 ECEF 位置/速度/协方差；**非**方位/俯仰/变化率字段。 |
  | 角域滤波 | **部分符合** | 红外层 `[验收项：目标角度状态估计]` 含滤波 az/el 及变化率（`sbirs_geo_triple_angle_kf`）；**不在** fusion 文件。 |
  | 源/目标身份 | **部分符合** | 规范示例已补 `目标键=`、`源=实体4`、`目标ID=`；库内有 `目标键=`+`源=`，无外部 `目标ID=`（本次不改代码）。 |

- **实现**（如何写出 §0 示例行；本次仅文档，不改代码）：
  - **时机**：融合 UKF 开启写 fusion 行；角变化率写 SBIRS `目标角度状态估计` 或统一合并到本项。
  - **数据**：滤波后 ECI 方位/俯仰（rad）及变化率（rad/s）；身份写 `目标键=`、`源=实体N`、`目标ID=`。连续时刻即逐周期各写一行。
  - **示例**：
    ```text
    仿真时间=10.000s 仿真周期=10 [标准卡尔曼滤波功能测试] 目标键=1 源=实体4 目标ID=1001 滤波方位/俯仰(ECI)=(0.214,0.322)rad 方位变化率=0.000120rad/s 俯仰变化率=0.000085rad/s
    仿真时间=11.000s 仿真周期=11 [标准卡尔曼滤波功能测试] 目标键=1 源=实体4 目标ID=1001 滤波方位/俯仰(ECI)=(0.215,0.323)rad 方位变化率=0.000118rad/s 俯仰变化率=0.000083rad/s
    ```

---

## 四、识别与推演

### 19. 规模目标识别功能测试（`log/rir_acceptance.log`）

- **当前修改后情况：完成。改名 `[规模目标识别功能测试]`；新增被跟踪对象明细：`确认目标ID=[…]` 与 `在跟列表=航迹N→目标M(确认|待确认)`（丢失列表有则写，external_target_id=0 写 未知；2026-08-28 rir_boost_burnout 场景已验证）。**

- `Rir_acceptance.log` 文件中能够记录集群目标的**具体规模数量**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 集群规模 | **符合** | `[验收项：集群目标识别]` 含 `集群目标数量`（=确认航迹数，下界口径）、在跟/待确认/丢失。 |
  | 被跟踪对象 | **不符合** | `[集群目标识别]` **仅计数**，无 `目标ID=` / `航迹→目标` 明细。 |
  | 旁路可读 | **部分符合** | 同周期 `[验收项：多目标跟踪]` 写 `航迹1→目标1001 航迹2→目标1002…`（`RirController.cpp` 汇总行）；须自行与集群行对照。 |
  | 验收项名 | **部分符合** | 库内 `[集群目标识别]`，尚无 `[规模目标识别功能测试]`。 |

- **实现**（如何写出 §0 示例行；本次仅文档，不改代码）：
  - **时机**：`WriteRirClusterCount` 逐周期；数据已在 `tracks` 向量（含 `external_target_id`、`association_key`、`status`）。
  - **数据**：保留计数四元组；**建议追加**按状态分列的 identity 列表，例如 `确认目标ID=[1001,1002]`，或 `在跟列表=航迹1→目标1001(确认);航迹2→目标1002(确认)`（待确认/丢失同理，无则省略该列表）。
  - **示例**：
    ```text
    仿真时间=3.000s 仿真周期=3 [规模目标识别功能测试] 集群目标数量=2 在跟=2 待确认=0 丢失=0 确认目标ID=[1001,1002] 在跟列表=航迹1→目标1001(确认);航迹2→目标1002(确认)
    ```

### 20. 目标轨迹预报功能测试（`log/inference_acceptance.log`）

- **当前修改后情况：完成。改名 `[目标轨迹预报功能测试]`，行首加 `目标键=`；10s 预测点 LLA 与落点经纬高维持。**

- `inference_acceptance.log` 文件中能够记录目标从当前时刻到 **10 秒之间**的**轨迹**视为通过，否则视为不通过。
- `inference_acceptance.log` 文件中能够记录**预测落点位置**（经度、纬度、高程）视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 10s 轨迹 | **符合** | `[验收项：目标轨迹预报]` 含 `预报时段=[0,10]s`、`预测点LLA`（+1s…+10s 逐秒 LLA）。 |
  | 落点 | **符合** | 同条含 `落点经纬高`、误差椭圆半长/半短/方位。 |
  | 验收项名 | **部分符合** | 库内 `[目标轨迹预报]`。 |

- **实现**（如何写出 §0 示例行；本次仅文档，不改代码）：
  - **示例**：
    ```text
    仿真时间=10.000s 仿真周期=10 [目标轨迹预报功能测试] 目标键=1 预报时段=[0,10]s 预测点LLA=[+1s:(31.121,120.751,3499000.0);…;+10s:(31.130,120.760,3490000.0)] 落点经纬高=(31.120,123.109,0.0)
    ```

### 21. 落点预测功能测试（`log/inference_acceptance.log`）

- **当前修改后情况：完成。改名 `[落点预测功能测试]`，行首加 `目标键=`。**

- `inference_acceptance.log` 文件中能够记录 **DD 目标**的**初步落点位置**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 初步落点 | **符合** | `[验收项：落点预测]` 含 `初步落点经纬高`（仅有落点解的周期写）。 |
  | 验收项名 | **部分符合** | 库内 `[落点预测]`。 |

- **实现**（示例）：
    ```text
    仿真时间=10.000s 仿真周期=10 [落点预测功能测试] 目标键=1 初步落点经纬高=(31.120,123.109,0.0)
    ```

### 22. 发射点预测功能测试（`log/inference_acceptance.log`）（修改）

- **当前修改后情况：完成。改名 `[发射点预测功能测试]`；椭圆字段对齐规范 `误差椭圆半长/半短/方位`；硬编码占位 `置信度=0.68` 已删（评审口径：不写置信度）。**

- `inference_acceptance.log` 文件中能够记录目标的**预测发射时刻和地理位置**，并记录**误差范围**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 发射点/误差 | **部分符合** | `[验收项：发射点预测]` 含地理位置、位置误差1σ、椭圆；**预测发射时刻**已按评审删除；**不写** `置信度`（库内硬编码占位）。 |
  | 验收项名 | **部分符合** | 库内 `[发射点预测]`。 |

- **实现**（示例）：
    ```text
    仿真时间=10.000s 仿真周期=10 [发射点预测功能测试] 目标键=1 预测发射点地理位置=(30.470,123.088,0.0) 位置误差1σ=10616m 误差椭圆半长/半短/方位=(12000m,9000m,15°)
    ```

### 23. 关机点预测功能测试（`log/inference_acceptance.log`）

- **当前修改后情况：完成。改名 `[关机点预测功能测试]`；未确认/速度σ护栏分支维持如实写 `正在模拟计算`。**

- `inference_acceptance.log` 文件中能够记录目标的**预测关机时刻和关机点位置**视为通过，否则视为不通过。
- `inference_acceptance.log` 文件中能够记录**误差范围**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 关机点/误差 | **部分符合** | `[验收项：关机点预测]` 已确认写时刻+LLA+误差1σ；未确认/速度σ护栏写 `正在模拟计算`。 |
  | 验收项名 | **部分符合** | 库内 `[关机点预测]`。 |

- **实现**（示例）：
    ```text
    仿真时间=301.000s 仿真周期=301 [关机点预测功能测试] 目标键=1 关机时刻=50.0s 关机点经纬高=(30.000,120.000,400.0) 关机点误差1σ=850m
    ```

### 24. 特殊事件监测与提示功能测试

- **当前修改后情况：完成。SBIRS 事件行与推演轨迹突变行统一改名 `[特殊事件监测与提示功能测试]`；`事件=` 置于方括号后首字段；SBIRS 行加 `卫星ID=`；轨迹突变位置由 ECEF 改为 LLA（2026-08-28 已验证两类行落盘）。**

- `Sbirs_acceptance.log` 文件中能够记录**首次探测、跟踪中断、轨迹突变**等关键事件信息（含**事件等级、目标 ID、时间、位置**）视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 首次探测/中断 | **符合** | `sbirs_acceptance.log` `[特殊事件监测与提示]`：等级、目标ID、事件、阶段、时间、位置ECEF。 |
  | 轨迹突变 | **部分符合** | `inference_acceptance.log` 同名项：等级=3、相对Δv、位置ECEF（规范要 **LLA**）；**事件**应写在方括号后第一字段。 |
  | 卫星标识 | **不符合** | SBIRS 事件行无 `卫星ID=`。 |

- **实现**（示例；方括号后**首字段为 `事件=`**）：
    ```text
    仿真时间=1.000s 仿真周期=1 [特殊事件监测与提示功能测试] 事件=首次探测 卫星ID=4 等级=1 目标ID=1001 阶段=宽视场搜索 时间=1.000s 位置ECEF=(-2300120.1,4800450.2,3500088.0)
    仿真时间=120.000s 仿真周期=120 [特殊事件监测与提示功能测试] 事件=轨迹突变 目标键=1 等级=3 相对Δv=0.25 位置LLA=(31.120,120.750,3495000.0)
    ```

### 25. 落点预报与信息分发功能测试（`log/inference_acceptance.log`）（修改）

- **当前修改后情况：完成。改名 `[落点预报与信息分发功能测试]`；`弹道模型`/`标准化封装`（规范示例省略）已删；行首加 `目标键=`。`分发状态=已发布` 须同时写 **`分发来源ID=`** 与 **`分发对象ID=`**（实体 ID，不用通道名或角色名代替）。**

- `Inference_acceptance.log` 文件中能够记录 **DD 模型**的**预测落点**、**落点误差范围及置信度**，以及**模拟预报告信息的标准化封装与分发**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 落点/封装/分发 | **部分符合** | 库内有预测落点、关机点误差1σ、置信度、`分发状态=已发布(<通道名>)`。规范示例须写 **`分发来源ID=`**、**`分发对象ID=`**；库内无这两字段。**不写** `弹道模型`、**不写** `标准化封装`。 |
  | 验收项名 | **部分符合** | 库内 `[落点预报与信息发布]`。 |

- **实现**（示例；本次仅文档，不改代码）：
  - **数据**：`目标键=`、预测落点。**分发状态**写是否已发布；**分发来源ID**为发布方实体 ID，**分发对象ID**为接收方实体 ID。不把通道类名或角色名当成来源/对象。
  - **示例**：
    ```text
    仿真时间=10.000s 仿真周期=10 [落点预报与信息分发功能测试] 目标键=1 预测落点=(31.120,123.109,0.0) 分发状态=已发布 分发来源ID=1 分发对象ID=2
    ```

---

## 五、精度评估（`log/precision_acceptance.log`）

### 26. 关键精度指标提取功能测试（修改）

- **当前修改后情况：完成。三轴位置误差改 ECEF（不再写东/北/天）；本条补 **`脱靶量m=`**（规范点名，不得因第 13 项已有而省略）。不写 RMSE/CEP50/CI。**

- `Precision_acceptance.log` 文件中能够记录并显示目标的**三轴位置误差、距离误差、方位角和俯仰角误差、脱靶量**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 三轴位置误差 | **部分符合** | 规范示例用 **ECEF 三分量** `三轴位置误差ECEF=`。库内会话汇总写 `东/北/天RMSE`（ENU 统计），未写 ECEF 向量。 |
  | 距离/方位俯仰误差 | **部分符合** | 库内为 `距离RMSE`、`方位/俯仰RMSE`；规范示例写 **误差**（非 RMSE）。 |
  | 脱靶量 | **不符合** | 规范本条须写 `脱靶量m=`。库内不在 precision 文件，只在 SBIRS `[窄视场跟踪探测]`。 |
  | 多余字段 | **超出规范** | 库内另写 `斜距RMSE`、`合成RMSE`、`CEP50`、`位置误差95%CI`；规范示例**不写**。 |
  | 目标身份 | **部分符合** | 规范示例已补 `目标ID=`；库内会话汇总行无目标身份（本次不改代码）。 |

- **实现**（示例，会话汇总一行；字段对齐规范四条，**不写 RMSE/CEP50/CI 等派生项**）：
  - **数据**：`目标ID=`；**三轴位置误差 ECEF**（估计−真值，m）；距离误差；方位/俯仰误差；**脱靶量**（焦平面 m）。
  - **示例**：
    ```text
    仿真时间=0.000s 仿真周期=0 [关键精度指标提取功能测试] 目标ID=1001 三轴位置误差ECEF=(90.0,-80.0,150.0)m 距离误差=180.0m 方位/俯仰误差=(0.010,0.012)° 脱靶量m=(0.0012,-0.0008)
    ```

### 27. 层次分析法功能测试

- `Precision_acceptance.log` 文件中能够记录并显示**各层次元素的权重**视为通过，否则视为不通过。
- `Precision_acceptance.log` 文件中能够记录并显示**各底层指标的得分值**视为通过，否则视为不通过。
- `Precision_acceptance.log` 文件中能够记录并显示**各层次元素的综合效能总分及等级评估**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 权重/得分/综合分 | **符合** | 规范要权重、底层得分、综合分、等级；库内另写 `贡献排序`、`CR`（派生，示例省略）。 |
  | 验收项名 | **部分符合** | 库内 `[层次分析法]`。 |

- **实现**（示例，会话一行；不写派生项 `贡献排序`/`CR`）：
    ```text
    仿真时间=0.000s 仿真周期=0 [层次分析法功能测试] 准则层权重=(0.200×5) 底层分=(0.85,0.78,0.90,0.72,0.70) 综合分=0.790 等级=B
    ```

---

## 六、雷达信号处理（OCR：`RadarSingalProcessingDateFlow.log` → `log/rir_acceptance.log`）

> 原文日志文件名拼写为 `RadarSingalProcessingDateFlow.log`；仓库统一写入 `log/rir_acceptance.log`（开关 `ONEQ_ENABLE_RIR_ACCEPTANCE_LOG`）。天线方向图明细另写 `log/rir_antenna_pattern.csv`。**第 28–55 项示例遵循 §0 写作规则（禁止派生量、无则省略）。**

### 28. 天线方向图仿真功能测试

- 能够记录**天线增益方向图**数据视为通过，否则视为不通过。
- 能够记录**扫描波束宽度**视为通过，否则视为不通过。
- 能够记录**峰值增益值**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 2D/峰值/波束宽度 | **符合** | 规范要增益方向图、波束宽度、峰值增益；CSV 存 2D 明细。 |
  | 多余字段 | **超出规范** | 库内写 `三维增益文件=` 路径行；示例保留峰值与波束宽度，路径可指向 CSV。 |
  | 雷达身份 | **部分符合** | 规范示例已补 `雷达ID=`；库内装备级行无此字段（本次不改代码）。 |
  | 验收项名 | **部分符合** | 库内 `[天线方向图仿真]`。 |

- **实现**（会话首周期一行；2D 明细在 CSV，日志只写峰值与波束宽度）：
    ```text
    仿真时间=0.000s 仿真周期=0 [天线方向图仿真功能测试] 雷达ID=1 峰值增益=52.00dBi 波束宽度az/el=(4.000,4.000)°
    ```

### 29. 回波功率计算功能测试（修改）

- 能够记录**目标回波功率信息**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 回波功率 | **部分符合** | 规范示例单位 **W**。库内 `[回波功率计算]` 写 `回波功率`(dBW)。 |
  | 多余字段 | **超出规范** | 库内同条还写 `斜距`、`SNR`、`热噪声/干扰/杂波功率`（分属第 30–31、33–34 项）；零值干扰/杂波仍输出 `0.000W`；另重复一行 `[经处理后雷达回波信号]`。 |
  | 验收项名 | **部分符合** | 库内 `[回波功率计算]`。 |

- **实现**（逐检测目标一行；**只写回波功率**，不写 SNR/噪声/干扰/杂波/斜距）：
    ```text
    仿真时间=1.000s 仿真周期=1 [回波功率计算功能测试] 目标ID=1001 回波功率=3.880e-18W
    ```

### 30. 干扰功率计算功能测试（修改）

- 能够**打印**各干扰源到达雷达接收端的**干扰功率**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 干扰功率 | **部分符合** | 规范示例：`干扰源ID=` + 到达雷达功率。库内身份仍是 `平台/设备/发射` 三元组（本次不改代码）。 |
  | 多余字段 | **超出规范** | 库内另写 `路径`、`时域/频域重叠`（派生/链路上下文）。 |
  | 验收项名 | **部分符合** | 库内 `[干扰功率计算]`。 |

- **实现**（逐干扰源一行；身份只写 **`干扰源ID=`** + 到达功率）：
    ```text
    仿真时间=1.000s 仿真周期=1 [干扰功率计算功能测试] 干扰源ID=2 到达雷达功率=1.091e-02W
    ```

### 31. 接收机噪声功率计算

- 能够记录**信噪比信息**视为通过，否则视为不通过。

- **规范口径（文档澄清）**：本条输出接收机 **热噪声功率**（W），**不写 SNR**。OCR 判定句虽写「信噪比」，示例按项标题与用户口径写 `热噪声功率=`。热噪声是接收机底噪（k·T·B·F），不是逐目标量。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 热噪声功率 | **部分符合** | 库内 `[接收机噪声功率]` 有 `热噪声功率=…W`，同时写 `SNR=`。规范示例**只写热噪声功率**。 |
  | SNR | **超出规范** | 库内同条写 `SNR=…dB`；本条示例不写。 |
  | 雷达身份 | **部分符合** | 规范示例写 `雷达ID=`；库内按检测目标写 `目标ID=`（本次不改代码）。 |
  | 验收项名 | **部分符合** | 库内 `[接收机噪声功率]`。 |

- **实现**（示例；只写热噪声功率，不写 SNR）：
    ```text
    仿真时间=1.000s 仿真周期=1 [接收机噪声功率计算功能测试] 雷达ID=1 热噪声功率=3.2e-15W
    ```

### 32. 目标信号增益功能测试

- 能够记录经处理后的**雷达回波信号**视为通过，否则视为不通过。
- 能够记录**总增益**视为通过，否则视为不通过。缺失。可计算。
- 能够记录**脉压增益**视为通过，否则视为不通过。
- 能够记录 **MTI 增益**视为通过，否则视为不通过。
- 能够记录 **MTD 增益**视为通过，否则视为不通过。

- **规范口径（文档澄清）**：OCR **一条**测试项，库内**无**同名 `[目标信号增益]` 聚合行。子要求分拆检索；**无**「相干积累增益」子项（库内有行，**不属于**本条规范）。「经处理后雷达回波信号」在本条**再写一行** `[回波功率计算功能测试]`（与第 29 项同口径，只写回波功率）。「总增益」库内**无字段**，判定句标 **缺失。可计算。** 示例按本条已点名三分项 **脉压+MTI+MTD**（dB）写出；主链分子是脉压+`target_processing_gain_db`（MTI/MTD 为验收旁路），与示例口径不同。

| OCR 子要求 | 库内检索 `[…]` | 规范示例 `[…]` | 能否输出 |
|---|---|---|---|
| 经处理后雷达回波信号 | `回波功率计算` | `[回波功率计算功能测试]`（同第 29 项） | **符合** |
| 总增益 | —（脉压+MTI+MTD） | `[目标信号增益功能测试]` `总增益=` | **不符合（缺失，可计算）** |
| 脉压增益 | `脉压增益` | `[目标信号增益功能测试]` | **符合** |
| MTI 增益 | `MTI增益` | `[目标信号增益功能测试]` | **符合**（无 cell 时省略该行） |
| MTD 增益 | `MTD增益` | `[目标信号增益功能测试]` | **符合**（同上） |

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 聚合验收项名 | **不符合** | 库内**无** `[目标信号增益]` 聚合行；规范示例方括号用测试项名，检索用上表库内子项名。 |
  | 回波+三分项增益 | **符合** | 回波按第 29 项再写一行；脉压/MTI/MTD 各一行，内容 `key=value`。 |
  | 相干积累/总增益 | **不符合（可计算）** | 规范本条不含相干积累。总增益无落盘字段，但脉压/MTI/MTD 已出，dB 可相加；与主链分子口径不同，见上。 |

- **实现**（回波一行 + 增益子项各行；总增益=脉压+MTI+MTD）：
    ```text
    仿真时间=1.000s 仿真周期=1 [回波功率计算功能测试] 目标ID=1001 回波功率=3.880e-18W
    仿真时间=1.000s 仿真周期=1 [目标信号增益功能测试] 目标ID=1001 总增益=66.635dB
    仿真时间=1.000s 仿真周期=1 [目标信号增益功能测试] 目标ID=1001 脉压增益=48.573dB
    仿真时间=1.000s 仿真周期=1 [目标信号增益功能测试] 目标ID=1001 MTI增益=6.021dB
    仿真时间=1.000s 仿真周期=1 [目标信号增益功能测试] 目标ID=1001 MTD增益=12.041dB
    ```

### 33. 噪声增益功能测试

- 能够记录**脉冲压缩后的噪声功率**视为通过，否则视为不通过。
- 能够记录**各多普勒滤波器通道的噪声功率**视为通过，否则视为不通过。
- 能够记录 **MTD 处理过后的等效噪声功率**视为通过，否则视为不通过。
- 能够记录**噪声总增益**视为通过，否则视为不通过。

- **规范口径（文档澄清）**：OCR 为 **四条**子指标（`radar_signal_processing_test_design_gap_2026-08-22.md` §1.2）。原先 `…**总增益**` 是文档整理时的**缩写**，不是 OCR 没解析完。库内**无** `[噪声增益]` 聚合行，须按子项名检索（同第 32 项）。

| OCR 子要求 | 库内检索 `[…]` | 规范示例 `[…]` | 能否输出 |
|---|---|---|---|
| 脉冲压缩后的噪声功率 | `脉冲压缩后的噪声功率` | `[噪声增益功能测试]` | **符合** |
| 各多普勒通道噪声功率 | `各多普勒滤波器通道噪声功率` | `[噪声增益功能测试]` | **符合**（无 cell 时省略） |
| MTD 等效噪声功率 | `MTD等效噪声功率` | `[噪声增益功能测试]` | **符合**（无 cell 时省略） |
| 噪声总增益 | `噪声总增益` | `[噪声增益功能测试]` | **符合** |

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 聚合验收项名 | **不符合** | 库内**无** `[噪声增益]` 聚合行；规范示例方括号用 `[噪声增益功能测试]`，检索用上表库内子项名。 |
  | 四子项 | **符合** | 见上表；四行各写一项 `key=value`。 |

- **实现**（四子项各一行）：
    ```text
    仿真时间=1.000s 仿真周期=1 [噪声增益功能测试] 目标ID=1001 脉冲压缩后的噪声功率=3.2e-15W
    仿真时间=1.000s 仿真周期=1 [噪声增益功能测试] 目标ID=1001 各多普勒滤波器通道噪声功率=[3.2e-15,3.2e-15,3.2e-15,3.2e-15,3.2e-15,3.2e-15,3.2e-15,3.2e-15]W
    仿真时间=1.000s 仿真周期=1 [噪声增益功能测试] 目标ID=1001 MTD等效噪声功率=8.0e-16W
    仿真时间=1.000s 仿真周期=1 [噪声增益功能测试] 目标ID=1001 噪声总增益=-3.010dB
    ```

### 34. 杂波信号处理增益功能测试（修改）

- 能够记录 **MTI 处理后杂波剩余功率**视为通过，否则视为不通过。
- 能够记录 **MTD 各多普勒通道杂波剩余功率分布**视为通过，否则视为不通过。
- 能够记录 **MTD 处理后的杂波抑制比**视为通过，否则视为不通过。
- 能够记录**总杂波抑制增益**视为通过，否则视为不通过。

- **规范口径**：OCR 仍列四句。**示例只写前两条**（MTI 剩余功率、MTD 通道分布）。`MTD处理后的杂波抑制比`、`总杂波抑制增益` 为派生量，**不写**。库内**无** `[杂波信号处理增益]` 聚合行，检索用子项名。无 cell 时省略对应行（不标缺失）。

| OCR 子要求 | 库内检索 `[…]` | 规范示例 | 能否输出 |
|---|---|---|---|
| MTI 处理后杂波剩余功率 | `MTI处理后杂波剩余功率` | 写 `MTI处理后杂波剩余功率=` | **符合** |
| MTD 各多普勒通道杂波剩余功率分布 | `MTD各多普勒通道杂波剩余功率分布` | 写 `MTD各多普勒通道杂波剩余功率分布=` | **符合**（无 cell 时省略） |
| MTD 处理后的杂波抑制比 | `MTD处理后的杂波抑制比` | **不写** | **不输出** |
| 总杂波抑制增益 | `总杂波抑制增益` | **不写** | **不输出** |

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 聚合验收项名 | **不符合** | 库内**无** `[杂波信号处理增益]` 聚合行；规范示例方括号用 `[杂波信号处理增益功能测试]`。 |
  | MTI 剩余 / MTD 通道分布 | **符合** | 规范示例只写这两项。库内剩余功率为杂波/`10^(抑制dB/10)`，不是真 MTI 滤波剩余；通道分布为验收旁路，未进 SINR。 |
  | 抑制比 / 总增益 | **不输出** | OCR 有这两句，示例不写。库内仍落盘抑制比与配置偏置 `clutter_suppression_gain_db`。 |

- **实现**（两子项各一行；无 cell 时省略通道行）：
    ```text
    仿真时间=1.000s 仿真周期=1 [杂波信号处理增益功能测试] 目标ID=1001 MTI处理后杂波剩余功率=1.2e-18W
    仿真时间=1.000s 仿真周期=1 [杂波信号处理增益功能测试] 目标ID=1001 MTD各多普勒通道杂波剩余功率分布=[1.2e-18,1.1e-18,1.0e-18,9.5e-19,9.0e-19,8.8e-19,8.5e-19,8.2e-19]W
    ```

### 35. 干扰信号处理增益功能测试（修改）

- 能够记录 **MTI 处理后干扰剩余功率**视为通过，否则视为不通过。
- 能够记录 **MTD 各多普勒通道干扰功率分布**视为通过，否则视为不通过。
- 能够记录 **MTD 处理后的干扰总抑制比**视为通过，否则视为不通过。
- 能够记录**总干扰抑制增益**视为通过，否则视为不通过。

- **规范口径**：OCR 仍列四句。**示例只写前两条**（MTI 剩余功率、MTD 通道分布）。`MTD处理后的干扰总抑制比`、`总干扰抑制增益` 为派生量，**不写**。库内**无** `[干扰信号处理增益]` 聚合行，检索用子项名。CFAR 归 **第 36 项**。无外部干扰单音时省略对应行（不标缺失）。

| OCR 子要求 | 库内检索 `[…]` | 规范示例 | 能否输出 |
|---|---|---|---|
| MTI 处理后干扰剩余功率 | `MTI处理后干扰剩余功率` | 写 `MTI处理后干扰剩余功率=` | **符合**（无外部干扰时省略） |
| MTD 各多普勒通道干扰功率分布 | `MTD各多普勒通道干扰功率分布` | 写 `MTD各多普勒通道干扰功率分布=` | **符合**（同上） |
| MTD 处理后的干扰总抑制比 | `MTD处理后的干扰总抑制比` | **不写** | **不输出** |
| 总干扰抑制增益 | `总干扰抑制增益` | **不写** | **不输出** |

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 聚合验收项名 | **不符合** | 库内**无** `[干扰信号处理增益]` 聚合行；规范示例方括号用 `[干扰信号处理增益功能测试]`。 |
  | MTI 剩余 / MTD 通道分布 | **符合** | 规范示例只写这两项。有外部干扰单音时写出（场景 `rir_jammed_scan`）；通道分布为验收旁路，未进 SINR。 |
  | 抑制比 / 总增益 | **不输出** | OCR 有这两句，示例不写。库内仍落盘抑制比与 `10 log10(jam_in/MTI剩余)`。 |
  | CFAR | **归第 36 项** | 库内同链另写统计检测门限/结果/概率，本条不写。 |

- **实现**（两子项各一行；无外部干扰时省略）：
    ```text
    仿真时间=1.000s 仿真周期=1 [干扰信号处理增益功能测试] 目标ID=1001 MTI处理后干扰剩余功率=1.091e-02W
    仿真时间=1.000s 仿真周期=1 [干扰信号处理增益功能测试] 目标ID=1001 MTD各多普勒通道干扰功率分布=[1.09e-02,1.08e-02,1.07e-02,1.06e-02,1.05e-02,1.04e-02,1.03e-02,1.02e-02]W
    ```

### 36. 恒虚警检测功能测试

- 能够记录**统计检测门限**视为通过，否则视为不通过。
- 能够记录**统计检测结果**视为通过，否则视为不通过。
- 能够记录**统计检测概率**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 门限/结果/Pd | **符合** | 规范要统计检测门限、结果、概率（Pd 为 CFAR 链路规定输出，非示例层另算）。 |
  | 验收项名 | **部分符合** | 库内检索用 CFAR 三行子项名；规范示例方括号用 `[恒虚警检测功能测试]`。 |

- **实现**（三子项各一行）：
    ```text
    仿真时间=1.000s 仿真周期=1 [恒虚警检测功能测试] 目标ID=1001 统计检测门限=13.815511
    仿真时间=1.000s 仿真周期=1 [恒虚警检测功能测试] 目标ID=1001 统计检测结果=否
    仿真时间=1.000s 仿真周期=1 [恒虚警检测功能测试] 目标ID=1001 统计检测概率=0.05189
    ```

### 37. 对指定空域进行搜索功能测试

- 能够记录当前周期的**方位/俯仰指向信息**视为通过，否则视为不通过。
- 能够记录**扫描调度信息**视为通过，否则视为不通过。
- 能够记录**检测与量测信息**视为通过，否则视为不通过。

- **规范口径**：OCR 三条（§1.6）。库内拆成 `[本周期方位俯仰指向]`、`[扫描调度信息]`、`[检测量测信息]`（另有汇总行 `[指定空域搜索]`）。本条三条**都写**；扫描调度不因第 49 项也有而不写。检测与量测写目标 ID、斜距、**量测角度**；**不写 Pd**。

| OCR 子要求 | 库内检索 `[…]` | 规范示例 key | 能否输出 |
|---|---|---|---|
| 方位/俯仰指向信息 | `本周期方位俯仰指向` | `方位/俯仰指向信息` | **符合** |
| 扫描调度信息 | `扫描调度信息` | `扫描调度信息` | **符合** |
| 检测与量测信息 | `检测量测信息` | `检测与量测信息` | **符合**（含目标 ID、斜距、量测方位/俯仰；不写 Pd） |

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 方位/俯仰指向信息 | **符合** | 库内 `方位/俯仰=`；规范示例 key=`方位/俯仰指向信息`。 |
  | 扫描调度信息 | **符合** | 库内 `[扫描调度信息]`：计划驻留/实际执行、搜索、跟踪、预算/已耗时。本条示例要写，不因第 49 项也有调度而不写。 |
  | 检测与量测信息 | **符合** | 规范要检测结果与量测（斜距 + 量测方位/俯仰）。库内当前写 `搜到目标=[ID=… 斜距=…m Pd=…]`：**缺量测角**、多写 `Pd`（归第 36 项，本条不写）。规范示例补 `量测方位/俯仰`、去掉 Pd。 |
  | 多余字段 | **超出规范** | 库内汇总行另含 `搜索角域`（配置派生，本条 OCR 未点名）。 |
  | 雷达身份 | **部分符合** | 指向/调度规范示例已补 `雷达ID=`；库内无此字段（本次不改代码）。检测行目标身份在 `搜到目标=[ID=…]`。 |
  | 验收项名 | **部分符合** | 库内用子项名；规范示例方括号用 `[对指定空域进行搜索功能测试]`。 |

- **实现**（三子项各一行；key=判定指标名）：
    ```text
    仿真时间=1.000s 仿真周期=1 [对指定空域进行搜索功能测试] 雷达ID=1 方位/俯仰指向信息=(-110.000,85.000)°
    仿真时间=1.000s 仿真周期=1 [对指定空域进行搜索功能测试] 雷达ID=1 扫描调度信息=计划驻留/实际执行=1/1 搜索=1 跟踪=2 预算/已耗时=1.000/0.850s
    仿真时间=1.000s 仿真周期=1 [对指定空域进行搜索功能测试] 检测与量测信息=搜到目标=[ID=1001 斜距=3400000.0m 量测方位/俯仰=(0.000,20.000)°]
    ```

### 38. 对目标的角度和距离进行测量功能测试

- 能够记录目标的**量测角度和距离**视为通过，否则视为不通过。
- 能够记录**目标量测误差统计**视为通过，否则视为不通过。缺失。可计算。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 角/距 | **符合** | 规范要量测角度与距离（斜距 + 方位/俯仰）。 |
  | 误差统计 | **不符合（缺失，可计算）** | 本项雷达日志无此字段。能算：量测−真值；会话统计已在第 26 项 `precision_acceptance.log`。 |
  | 多余字段 | **超出规范** | 库内另写高度、速度、航向（派生）、加速度、识别类型（归第 44–47 项）。 |
  | 验收项名 | **部分符合** | 库内 `[目标测量角度与距离]`。 |

- **实现**（角距一行；量测误差=量测−真值）：
    ```text
    仿真时间=2.000s 仿真周期=2 [对目标的角度和距离进行测量功能测试] 目标ID=1001 斜距=3400000.0m 方位/俯仰=(0.000,20.000)°
    仿真时间=2.000s 仿真周期=2 [对目标的角度和距离进行测量功能测试] 目标ID=1001 目标量测误差统计=斜距误差=180.0m 方位/俯仰误差=(0.010,0.012)°
    ```

### 39. 对典型目标/再入目标进行跟踪功能测试

- 能够记录目标的**定位信息**（距离、方位、高度）视为通过，否则视为不通过。
- 能够记录目标的**运动参数**（速度、航向、加速度）视为通过，否则视为不通过。

- **规范口径**：OCR 截图里本条判定句与第 **40** 项字面相同，**第 40 项标存疑**（疑图片把第 39 项判定复制到第 40 项）。本条项名是「典型/再入」，判定（定位+运动）与项名相符，暂按截图执行。定位含距离、方位、**高度**；运动参数含速度、**航向**、**加速度**（三项都写）。示例只写 **1 条**航迹。库内无「典型/再入」专用标志、无再入专用动力学，不另造 `跟踪对象=` 字段。

| OCR 子要求 | 库内检索 `[…]` | 规范示例 key | 能否输出 |
|---|---|---|---|
| 定位（距离、方位、高度） | `典型/再入目标跟踪` | `定位信息` | **符合** |
| 运动参数（速度、航向、加速度） | `典型/再入目标跟踪` | `运动参数` | **符合** |

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 定位信息 | **符合** | 斜距、方位/俯仰、高度；库内同行还写航迹/目标ID/状态。 |
  | 运动参数 | **符合** | 速度、航向、加速度（库内加速度可写 ECEF 三分量）。规范示例三项都写。 |
  | 与第 40 项 | **存疑** | 截图判定句与第 40 项完全相同，疑为图片问题；第 40 项判定句标存疑。 |
  | 验收项名 | **部分符合** | 库内 `[典型/再入目标跟踪]`。 |

- **实现**（一条航迹；定位、运动各一行）：
    ```text
    仿真时间=2.000s 仿真周期=2 [对典型目标/再入目标进行跟踪功能测试] 航迹=1 目标ID=1002 状态=确认 定位信息=斜距=8550002.0m 方位/俯仰=(0.045,20.000)° 高度=2924272.0m
    仿真时间=2.000s 仿真周期=2 [对典型目标/再入目标进行跟踪功能测试] 航迹=1 目标ID=1002 运动参数=速度=6287.000m/s 航向=90.00° 加速度=(0.000,0.000,0.000)m/s²
    ```

### 40. 多目标跟踪功能测试

- 能够记录目标的**定位信息**（距离、方位、高度）视为通过，否则视为不通过。存疑。
- 能够记录目标的**运动参数**（速度、航向、加速度）视为通过，否则视为不通过。存疑。

- **规范口径**：**存疑。** 截图（`微信图片_20260827201949_109_242.jpg`）中本条两条判定句与第 39 项**完全相同**，未出现「多目标 / 多航迹」字样，疑为图片串行或复制错误，正式口径待原规范确认。示例仍按**项名**「多目标跟踪」写同周期 ≥2 条航迹的定位+运动，不与第 39 项共用一行。

| OCR 子要求 | 库内检索 `[…]` | 规范示例 key | 能否输出 |
|---|---|---|---|
| 定位（距离、方位、高度） | `多目标跟踪` | `定位信息` | **符合**（逐航迹） |
| 运动参数（速度、航向、加速度） | `多目标跟踪` | `运动参数` | **符合**（逐航迹） |

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 多航迹 | **符合** | 库内逐航迹写与第 39 项同结构字段；另有 `本周期航迹数`、`航迹n→目标ID` 汇总。 |
  | 定位/运动 | **符合** | 每条航迹都要斜距、方位/俯仰、高度、速度、航向、加速度；不因第 39 项已写而不写。 |
  | 与第 39 项 | **存疑** | 截图判定句与第 39 项完全相同，疑图片问题；正式多目标判定待原规范。 |
  | 验收项名 | **符合** | 库内 `[多目标跟踪]`。 |

- **实现**（同周期 2 条航迹；每条定位、运动各一行）：
    ```text
    仿真时间=2.000s 仿真周期=2 [多目标跟踪功能测试] 本周期航迹数=2
    仿真时间=2.000s 仿真周期=2 [多目标跟踪功能测试] 航迹=1 目标ID=1001 定位信息=斜距=3400000.0m 方位/俯仰=(0.000,20.000)° 高度=1162868.0m
    仿真时间=2.000s 仿真周期=2 [多目标跟踪功能测试] 航迹=1 目标ID=1001 运动参数=速度=2500.000m/s 航向=90.00° 加速度=(0.000,0.000,0.000)m/s²
    仿真时间=2.000s 仿真周期=2 [多目标跟踪功能测试] 航迹=2 目标ID=1002 定位信息=斜距=8550002.0m 方位/俯仰=(0.045,20.000)° 高度=2924272.0m
    仿真时间=2.000s 仿真周期=2 [多目标跟踪功能测试] 航迹=2 目标ID=1002 运动参数=速度=6287.000m/s 航向=90.00° 加速度=(0.000,0.000,0.000)m/s²
    ```

### 41. 航迹关联功能测试

- 能够记录**检测结果、目标位置、检测概率**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 位置/Pd/关联 | **符合** | 规范要检测位置、检测概率、关联结果。 |
  | 多余字段 | **超出规范** | 库内另写计数汇总、马氏距离²（派生）、重复 `[更新后的航迹集合]` 行。 |
  | 验收项名 | **符合** | 库内 `[航迹关联]`。 |

- **实现**（逐量测一行）：
    ```text
    仿真时间=2.000s 仿真周期=2 [航迹关联功能测试] 目标ID=1001 位置LLA=(31.121,120.751,3499000.0) 检测概率=0.0519 关联结果=航迹1
    ```

### 42. 跟踪滤波功能测试

- 能够记录当前时刻的**位置（ECEF）、速度和加速度估计值**视为通过，否则视为不通过。
- 能够记录当前时刻的**目标状态协方差**视为通过，否则视为不通过。
- 能够记录当前时刻的**估计误差指标**视为通过，否则视为不通过。缺失。可计算。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | ECEF/速度/加速度 | **符合** | 规范要滤波估计状态。 |
  | 目标状态协方差 | **符合** | 库内可写完整 6×6；规范示例 key=`目标状态协方差`（勿写 `协方差对角`）。 |
  | 误差指标 | **不符合（缺失，可计算）** | 本项雷达日志无此字段。能算：滤波 ECEF/速度−真值；第 26 项已有 `航迹位置误差` 与会话 RMSE。 |
  | 多余字段 | **超出规范** | 库内另写下一时刻预测（外推）、协方差迹、`IMM权重`（分子项）。 |
  | 目标身份 | **部分符合** | 规范示例已补 `航迹=`+`目标ID=`；库内滤波行仅 `航迹=`（本次不改代码）。 |
  | 验收项名 | **部分符合** | 库内 `[跟踪滤波]` 等子项名。 |

- **实现**（当前估计 + 协方差 + 估计−真值）：
    ```text
    仿真时间=2.000s 仿真周期=2 [跟踪滤波功能测试] 航迹=1 目标ID=1001 位置（ECEF）=(-2300120.1,4800450.2,3500088.0)m 速度=(2500,0,0)m/s 加速度=(0,0,0)m/s²
    仿真时间=2.000s 仿真周期=2 [跟踪滤波功能测试] 航迹=1 目标ID=1001 目标状态协方差=(100.0,100.0,100.0,25.0,25.0,25.0)
    仿真时间=2.000s 仿真周期=2 [跟踪滤波功能测试] 航迹=1 目标ID=1001 估计误差指标=位置误差ECEF=(90.0,-80.0,150.0)m 速度误差=(1.200,0.500,0.300)m/s
    ```

### 43. 根据目标 RCS 实时计算目标探测结果功能测试

- 能够记录对已探测目标的持续**定位**和**运动参数**视为通过，否则视为不通过。存疑。

- **规范口径**：**存疑。** 项名是「根据目标 RCS 实时计算目标探测结果」，截图判定却只写持续定位（距离、方位、高度）和运动参数（速度、航向、加速度），**未点名 RCS**，且与第 39/40 项判定相近，疑为图片串行。正式口径待原规范确认。示例仍按**项名**写本周期 RCS，并带定位；运动参数截图有点名则写，不因第 39 项已有而不写。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 判定句 vs 项名 | **存疑** | 截图判定无 RCS、有定位/运动；项名要 RCS 探测。疑图片问题。 |
  | RCS | **符合** | 库内 `[RCS实时探测]` 写 `本周期RCS`；按项名落入示例。 |
  | 定位/运动 | **符合** | 库内同条有斜距、方位/俯仰、高度、速度、航向。 |
  | 验收项名 | **部分符合** | 库内 `[RCS实时探测]`。 |

- **实现**（按项名写 RCS；定位+运动按截图判定）：
    ```text
    仿真时间=2.000s 仿真周期=2 [根据目标RCS实时计算目标探测结果功能测试] 目标ID=1001 本周期RCS=0.025m² 定位信息=斜距=3400000.8m 方位/俯仰=(0.045,20.000)° 高度=1162868.0m
    仿真时间=2.000s 仿真周期=2 [根据目标RCS实时计算目标探测结果功能测试] 目标ID=1001 运动参数=速度=2500.000m/s 航向=90.00° 加速度=(0.000,0.000,0.000)m/s²
    ```

### 44. 运动特征处理功能测试（修改）

- 能够记录**目标类别**视为通过，否则视为不通过。

- **规范口径**：先写运动判定条件（速度、高度、加速度、近似直线），再写由此得到的 **目标类别**。斜距/方位不是运动分类条件，示例不写。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 判定条件→类别 | **部分符合** | 库内有速度、高度、近似直线、目标类别。规范示例再补 **加速度**（提取器有，验收行未写）。 |
  | 多余字段 | **超出规范** | 库内另写斜距、方位/俯仰、重复 `[目标类别]` 行。 |
  | 目标身份 | **部分符合** | 规范示例已补 `航迹=`+`目标ID=`；库内仅 `航迹=`（本次不改代码）。 |
  | 验收项名 | **符合** | 库内 `[运动特征处理]`。 |

- **实现**（判定条件在前，类别在后）：
    ```text
    仿真时间=2.000s 仿真周期=2 [运动特征处理功能测试] 航迹=1 目标ID=1001 速度=2500.000m/s 高度=1162868.0m 加速度=0.000m/s² 目标类别=弹道导弹
    ```

### 45. RCS 统计特征处理功能测试

- 能够记录**目标类别**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | RCS统计+类别 | **符合** | 规范要 RCS 统计特征与目标类别。 |
  | 目标身份 | **部分符合** | 规范示例已补 `航迹=`+`目标ID=`；库内仅 `航迹=`（本次不改代码）。 |
  | 验收项名 | **符合** | 库内 `[RCS统计特征处理]`。 |

- **实现**（示例）：
    ```text
    仿真时间=2.000s 仿真周期=2 [RCS统计特征处理功能测试] 航迹=1 目标ID=1001 RCS均值/标准差=-16.021/1.208dBsm 目标类别=弹道导弹
    ```

### 46. 极化特征解算功能测试

- 能够记录**极化散射矩阵行列式**视为通过，否则视为不通过。
- 能够记录**功率迹（Span）**视为通过，否则视为不通过。
- 能够记录**去极化系数**视为通过，否则视为不通过。
- 能够记录**本征极化方向角**视为通过，否则视为不通过。
- 能够记录**本征极化椭圆率**视为通过，否则视为不通过。
- 能够记录**目标类型**视为通过，否则视为不通过。

- **规范口径**：OCR 六条。规范示例 key=判定指标名，六行各写一项；缺样本时省略该行（不写 `无`）。不写库内汇总的 `极化差/相对/和`、`置信度`。

| OCR 子要求 | 库内检索 `[…]` | 规范示例 key | 能否输出 |
|---|---|---|---|
| 极化散射矩阵行列式 | `极化散射矩阵行列式` | `极化散射矩阵行列式` | **符合**（缺样本省略） |
| 功率迹（Span） | `功率迹` | `功率迹（Span）` | **符合**（缺样本省略） |
| 去极化系数 | `去极化系数` | `去极化系数` | **符合**（缺样本省略） |
| 本征极化方向角 | `本征极化方向角` | `本征极化方向角` | **符合**（缺样本省略） |
| 本征极化椭圆率 | `本征极化椭圆率` | `本征极化椭圆率` | **符合**（缺样本省略） |
| 目标类型 | `目标类型` | `目标类型` | **符合** |

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 五行极化量 | **符合** | 库内旁路派生 `|det(S)|`、Span、去极化、Graves ψ/τ；缺交叉极化/相位时省略。 |
  | 目标类型 | **符合** | 库内写 `目标类别=`；规范示例 key=`目标类型`。 |
  | 多余字段 | **超出规范** | 库内汇总行 `极化差/相对/和`、`置信度`、`正确识别率`。 |
  | 目标身份 | **部分符合** | 规范示例已补 `航迹=`+`目标ID=`；库内仅 `航迹=`（本次不改代码）。 |
  | 验收项名 | **部分符合** | 库内用子项名；规范示例方括号用 `[极化特征解算功能测试]`。 |

- **实现**（六子项各一行；有值才写）：
    ```text
    仿真时间=2.000s 仿真周期=2 [极化特征解算功能测试] 航迹=1 目标ID=1001 极化散射矩阵行列式=1.20e-03
    仿真时间=2.000s 仿真周期=2 [极化特征解算功能测试] 航迹=1 目标ID=1001 功率迹（Span）=5.00e-02
    仿真时间=2.000s 仿真周期=2 [极化特征解算功能测试] 航迹=1 目标ID=1001 去极化系数=0.1200
    仿真时间=2.000s 仿真周期=2 [极化特征解算功能测试] 航迹=1 目标ID=1001 本征极化方向角=15.000°
    仿真时间=2.000s 仿真周期=2 [极化特征解算功能测试] 航迹=1 目标ID=1001 本征极化椭圆率=5.000°
    仿真时间=2.000s 仿真周期=2 [极化特征解算功能测试] 航迹=1 目标ID=1001 目标类型=弹道导弹
    ```

### 47. 宽带一维像特征解算功能测试（修改）

- 能够记录**散射中心、或轮廓特征、识别类别**视为通过，否则视为不通过。

- **规范口径**：须写 **散射中心**（各中心距离向位置 + RCS），轮廓用长度/峰数，再写识别类别。不写 `能量集中`/`置信度`。无散射中心时省略该字段（不写 `无`）。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 散射中心 | **不符合** | 规范示例须写 `散射中心=[(相对距离,RCS);…]`。库内只写长度/峰数等统计，**无逐中心列表**。 |
  | 轮廓/类别 | **符合** | 库内有长度、峰数、识别类型。 |
  | 多余字段 | **超出规范** | 库内另写 `能量集中`、`分辨率`、`置信度`。 |
  | 目标身份 | **部分符合** | 规范示例已补 `航迹=`+`目标ID=`；库内仅 `航迹=`（本次不改代码）。 |
  | 验收项名 | **部分符合** | 库内拆行。 |

- **实现**（散射中心 + 轮廓 + 识别类别）：
    ```text
    仿真时间=2.000s 仿真周期=2 [宽带一维像特征解算功能测试] 航迹=1 目标ID=1001 散射中心=[(0.0m,-16.0dBsm);(6.0m,-16.0dBsm);(12.0m,-16.0dBsm)] 长度=12.000m 峰数=3 识别类型=弹道导弹
    ```

### 49. 调度策略功能测试

- 能够记录**各类事件的实际执行数量列表**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 执行列表 | **符合** | 规范仅要 `[搜索×Ns,跟踪×Nt,…]` 计数列表。 |
  | 多余字段 | **超出规范** | 库内 `[调度策略]` 另写计划/实际驻留、预算/耗时（派生/资源统计）。 |
  | 雷达身份 | **部分符合** | 规范示例已补 `雷达ID=`；库内无此字段（本次不改代码）。 |
  | 验收项名 | **部分符合** | 库内 `[各类事件的实际执行列表]`。 |

- **实现**（示例）：
    ```text
    仿真时间=1.000s 仿真周期=1 [调度策略功能测试] 雷达ID=1 [搜索×1,跟踪×2,识别×2]
    ```

---

## 七、波束扫描（`log/rir_scan_pattern.csv`）

### 48. 波束扫描功能测试

- `Rir_scan_pattern.csv` 文件中能够记录**波位排列表、扫描轨迹序列**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | CSV 波位表 | **符合** | 规范要波位排列表/扫描轨迹；CSV 存全表（`index,az_deg,el_deg`）。 |
  | 日志指引 | **部分符合** | 库内日志写文件路径、序号、下一波位、总数、驻留中心等；示例只保留轨迹序号与波位。 |
  | 雷达身份 | **部分符合** | 规范示例日志行已补 `雷达ID=`；库内无此字段（本次不改代码）。 |
  | 验收项名 | **部分符合** | 库内 `[波束扫描]` 等为子项名。 |

- **实现**（CSV 存表；日志一行只写本周期波位）：
    ```text
    仿真时间=1.000s 仿真周期=1 [波束扫描功能测试] 雷达ID=1 本周期序号=0 波位=(-110.000,85.000)°
    ```

---

## 八、性能测试

### 50. 初始化时间性能测试（`integration_events.log`）

- `integration_events.log` 文件中能够记录**初始化时间**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 墙钟计时 | **符合** | 示例层 `integration_events.log`：`[验收项：初始化时间]`，`…ms 模块=…`（RIR/SBIRS/Fusion/Precision 等）。 |
  | 库内占位 | **部分符合** | `sbirs/rir_acceptance.log` 写指引行「见 integration_events.log…」，非墙钟值。 |
  | 验收项名 | **符合** | 库内与示例层同名 `[初始化时间]`。 |

- **实现**（示例层，周期 0 一行/模块）：
    ```text
    仿真时间=0.000s 仿真周期=0 [初始化时间性能测试] 12.345ms 模块=RIR
    ```

### 51. 单步执行时间性能测试（`integration_events.log`）

- `integration_events.log` 文件中能够记录**单步实例仿真模型单步执行时间**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 墙钟计时 | **符合** | 示例层 `[验收项：单步执行时间]`（首个 Step 写一次，墙钟 ms）。 |
  | 库内占位 | **不符合** | SBIRS/RIR 库验收文件写 `暂无`（评审条 23 暂不处理）。 |
  | 验收项名 | **符合** | 库内与示例层同名 `[单步执行时间]`。 |

- **实现**（示例层，首周期一行/模块）：
    ```text
    仿真时间=1.000s 仿真周期=1 [单步执行时间性能测试] 3.210ms 模块=RIR
    ```

### 52. 单个模型加载时间性能测试（`integration_events.log`）

- `integration_events.log` 文件中能够记录**单个模型加载时间**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 墙钟计时 | **符合** | 示例层 `[验收项：单个模型加载时间]`（主要 RIR 识别库）。 |
  | 库内占位 | **部分符合** | RIR 写指引行；其它层无独立加载计时。 |
  | 验收项名 | **符合** | 库内与示例层同名。 |

- **实现**（示例）：
    ```text
    仿真时间=0.000s 仿真周期=0 [单个模型加载时间性能测试] 8.100ms 模块=RIR
    ```

### 53. 多个模型并行加载性能测试（`integration_events.log`）

- `integration_events.log` 文件中能够记录**加载状态**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 装配状态 | **符合** | 规范要加载状态；示例层写模块清单即可。 |
  | 多余字段 | **超出规范** | 库内写 `暂无`；示例层另写 `装配墙钟`（与第 50–52 项计时重复口径）。 |
  | 验收项名 | **符合** | 库内与示例层同名。 |

- **实现**（示例，会话首周期一行；只写加载状态）：
    ```text
    仿真时间=0.000s 仿真周期=0 [多个模型并行加载性能测试] 动态库加载完毕 模块数=8 模块=[ESR,AR,EOS,SBIRS,SAR,Fusion,Inference,Threat]
    ```

### 54. 可支持连续运行次数性能测试

- `integration_events.log` 文件中能够记录**模型运行次数及运行状态**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 连续运行 | **符合** | `[连续运行次数]`：`本会话已运行周期`、`状态=正常`。 |
  | 场景/总次数 | **符合** | 会话 `[典型场景和总仿真次数]`：`场景数`、`总仿真周期`（两条规范点，分行）。 |
  | 多余字段 | **超出规范** | 库内写 `场景=本会话` 等叙述性前缀。 |
  | 日志文件 | **部分符合** | OCR 写 `integration_events.log`；实际在库验收文件。 |
  | 验收项名 | **符合** | 库内 `[连续运行次数]`。 |

- **实现**（示例，两条规范点各一行）：
    ```text
    仿真时间=10.000s 仿真周期=10 [可支持连续运行次数性能测试] 本会话已运行周期=10 状态=正常
    仿真时间=0.000s 仿真周期=0 [可支持连续运行次数性能测试] 场景数=1 总仿真周期=10
    ```

### 55. 红外系统测角误差性能测试（`log/sbirs_acceptance.log`）

- `Sbirs_acceptance.log` 文件中能够记录红外系统**测量角度值**，并能够计算测量值与真实值得到**偏差**视为通过，否则视为不通过。

- **证据矩阵**（2026-08-27 检索）：

  | 维度 | 能否输出 | 说明 |
  |---|---|---|
  | 测量/偏差 | **符合** | 规范要测量角与测量−真值偏差。 |
  | 多余字段 | **超出规范** | 库内另写 `真值`、`本星会话RMSE`（派生统计）、`σ来源`；与第 9 项重复。 |
  | 卫星标识 | **不符合** | 行内无 `相对卫星ID=`。 |
  | 单位 | **部分符合** | 当前为度；性能指标 ≤3 μrad 需 rad 口径。 |
  | 验收项名 | **符合** | 库内 `[红外系统测角误差]`。 |

- **实现**（示例；只写测量与偏差，不写真值/RMSE/σ）：
    ```text
    仿真时间=1.000s 仿真周期=1 [红外系统测角误差性能测试] 相对卫星ID=4 目标ID=1001 测量方位/俯仰=(12.312,18.491)° 偏差az/el=(0.012,-0.009)°
    ```

---

## 附录：日志文件索引

| 日志文件（OCR） | 仓库路径 | 涉及测试项 |
|---|---|---|
| `Sbirs_acceptance.log` / `SatelliteTarget.log` | `log/sbirs_acceptance.log` | 5–10, 11–14, 24, 54–55（1–4 不输出） |
| `Fusion_acceptance.log` | `log/fusion_acceptance.log` | 15–18 |
| `Rir_acceptance.log` / `RadarSingalProcessingDateFlow.log` | `log/rir_acceptance.log` | 19, 28–47, 49, 54 |
| `inference_acceptance.log` | `log/inference_acceptance.log` | 20–25, 24（轨迹突变） |
| `Precision_acceptance.log` | `log/precision_acceptance.log` | 26–27；38/42 误差统计 |
| `Rir_scan_pattern.csv` | `log/rir_scan_pattern.csv` | 48 |
| `integration_events.log` | 场景 `log_dir` | 50–53 |

**合计：55 项功能/性能验收测试。**

**检索依据**：`docs/review/acceptance_log_mapping_2026-08-24.md`、`docs/review/acceptance_item_catalog_2026-08-22.md`、`src/sbirs_sensor/pipeline/SbirsPipeline.cpp`、`src/**/AcceptanceRecords.cpp`。
