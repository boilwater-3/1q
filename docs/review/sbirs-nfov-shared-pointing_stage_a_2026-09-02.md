---
Status: draft
Date: 2026-09-02
Review-Baseline: `evidence/sbirs-nfov-shared-pointing` @ `e7fde405`
Authority: 非规范性记录；结论以 docs/common/contract.md、docs/common/session_contract.md
  及各模块 docs/<module>/design.md 为准；与库实现冲突时以库为准。
---

# sbirs-nfov-shared-pointing：证据矩阵

<!-- 本文档写作规则：
1、证据一律写成一行：- **证据**：[evidence: 路径]，可加 ::符号名；禁止行号。
2、说明简要，一项一行；多个要点用 1、2、3 序号分点分行，禁止大段描述。
3、引用规则时直接写出规则内容，并用证据形式锁定来源文件；禁止写"见xx规则"。
4、面向非专业开发者，用平实中文；术语首次出现时给一句白话解释。
5、探针/测试必须是已实际执行的；无法直接验证的判断以"推理："开头标注。
-->

## §0 背景与待裁定的问题

- 触发来源：2026-09-02 三星座场景验收复核，用户指出"只有卫星 4 同时跟踪两个目标，且用两个独立窄场通道各跟一个"，质疑真实卫星"单镜筒一次只能朝一个方向"的硬约束在模型中消失，且 `max_concurrent_nfov_locks` 被迫同时承担"指向并发数"与"精测航迹条数"两义。
- **证据**：[evidence: examples/log/sbirs_triple_sat_fix_messages/opir_acceptance.log]

术语白话：
1、窄视场（NFOV）：星上高精度小视场凝视通道，一次只盯一小块天空（本场景视场全角 8°）。
2、通道：配置 `max_concurrent_nfov_locks` 给出的"并发锁定通道"；当前实现里一个通道=一条独立视线+一套独立转动执行器，等效一个独立镜筒。
3、镜筒：真实传感器里能独立转向的望远镜；天基凝视卫星通常只有 1 个可转向窄场镜筒，同帧多目标靠图像内并行，不靠多镜筒。
4、占空比：一个镜筒轮着看多个分开目标时，每个目标分到的观测时间比例。
5、宽场保持（custody）：目标没进窄场时由宽场扫描继续粗跟，航迹不丢。

待裁定项（冻结什么→什么证据能证明/否定→通过后最小范围）：
1、F1 独立多镜筒行为是否已产生可观测错误（boundaries 暂缓项的再进入条件是否满足）。
2、F2 同帧多目标（一个视场内免费多跟几个）是否为已证实需求。
3、F3 单镜筒分时轮转（分离目标轮着看+占空比）是否必须本期建模。
4、F4 `max_concurrent_nfov_locks` 一字段双语义是否本期拆分。

## §1 证据矩阵

| 待裁定项 | 假设（要证明什么） | 证据来源 | 探针/测试（已执行） | 通过条件 | 否定条件 | 建议判定 |
|---|---|---|---|---|---|---|
| F1 独立多镜筒已产生可观测错误 | 同一颗星两条窄场视线可同时指向角距远超窄视场全角的方向并各自保持精跟——真实单镜筒做不到，属可复现错误 | 每通道独立执行器与独立视线：[evidence: src/sbirs_sensor/pipeline/SbirsPointingCoordinator.h] ::ChannelRuntime::actuator；目标→通道 1:1：[evidence: src/sbirs_sensor/pipeline/SbirsNfovScheduler.h]；场景几何：[evidence: examples/scenes/sbirs_triple_sat_fix_messages/sbirs_triple_sat_fix_messages.json]；验收输出：[evidence: examples/log/sbirs_triple_sat_fix_messages/opir_acceptance.log]；暂缓条件原文（"必须证明独立 LOS 假设导致可复现错误"）：[evidence: docs/sbirs_sensor/boundaries.md] | 1、几何探针（WGS84 ECEF 换算，t=0/40/79s）：卫星A到两目标视线夹角 17.17–17.18° 全程稳定；两目标各在地音乐盘东西边缘（离星下点 8.94°/8.79°，方向相反）；窄场全角 8.0°——任何时刻不可能同框。2、日志探针：周期 3 起卫星 4 持续输出双目标窄场跟踪行（如周期 40 同时输出目标1与目标2 两行），共 78 个周期×卫星组合双锁 | 视线夹角>窄场全角 且 同星同刻双目标锁定同时成立 | 夹角≤窄场全角（同框，双通道只是内部冗余）或无双锁事实 | **pass**（两探针均坐实；暂缓项再进入条件成立） |
| F2 同帧多目标免费多跟是否为已证实需求 | 单通道一个视场内同时精跟多个目标是验收需要的物理正确机制 | 规范第 13 项只要求逐目标逐采样字段（脱靶量/状态/能量/SNR），无"同星同时双目标"要求：[evidence: docs/review/functional_acceptance_test_criteria_ocr.md]；调度意图"每通道独立凝视锁定一个目标"：[evidence: docs/sbirs_sensor/algorithms.md] | 场景库全量扫描（examples/scenes/*/*.json 含窄场配置者，共 12 个场景）：唯一多目标 SBIRS 场景即三星座场景，无任何"两目标同处一颗星窄视场"组合 | 存在要求同帧多目标的验收行 + 同框几何可达的场景 | 无场景证据（当前状态） | **defer**（单镜筒方案落地后登记为已知限制；若用户改场景为同框几何则升级） |
| F3 单镜筒分时轮转是否本期必须 | 分离多目标的精跟容量应由转速+稳定时间+刷新需求推出的占空比决定，而非配置的通道数 | 限速+稳定判定积木已有：[evidence: src/sbirs_sensor/pipeline/SbirsPointingActuator.h] ::Step、[evidence: include/1q/sbirs_sensor/config/SbirsMissionConfig.h] ::narrow_pointing_max_slew_rate_deg_per_sec；无空闲通道时目标停留宽场候选态、粗跟不丢：[evidence: docs/sbirs_sensor/algorithms.md] | 1、状态机核对：无空闲通道时目标停留宽场候选态（WideCandidate），航迹不丢。2、推理：轮转需要确定性仲裁（谁先看/看多久）、失败归属（轮转饥饿算谁的丢锁）、回放验收三件套——正是暂缓条件点名的前置设计，当前无此设计证据 | 用户裁定验收需要"分离双目标精跟行为物理正确（含轮转等待/刷新下降）" | 用户裁定本期只需消除不可能行为（双锁改单锁+宽场保持） | **defer**（默认走最小闭环 A；升级需用户拍板并补三件套设计证据） |
| F4 并发字段双语义是否本期拆分 | `max_concurrent_nfov_locks` 同时承担"指向并发数（几个镜筒）"与"精测航迹条数"两义，是 F1 错误行为的配置载体 | 公开配置字段与默认值 1：[evidence: include/1q/sbirs_sensor/config/SbirsPolicyConfig.h] ::max_concurrent_nfov_locks；同一配置值同时构造调度器与指向协调器：[evidence: src/sbirs_sensor/pipeline/SbirsPipeline.cpp]；用户需求陈述（2026-09-02 对话，非仓库路径） | 1、代码走查：调度器与指向协调器用同一配置值构造，通道数=锁定目标数上限=独立视线数。2、场景值清点：全库 14 处配置 13 处=1，唯三星座场景 3 颗星全部=2 | F1 成立且用户确认物理语义应为"硬件镜筒数" | F1 被否定 | **pass**（随 F1；字段具体形态在契约冻结时定） |

## §2 判定汇总与待裁定问题

判定汇总：
1、F1 **pass**：几何探针（视线夹角 17.2°>窄场全角 8°，全程稳定）+ 日志探针（卫星 4 连续 78 个周期双锁）双坐实；boundaries 暂缓项「多通道机械耦合与共享姿态资源」的再进入条件（独立 LOS 假设导致可复现错误）成立。
2、F2 **defer**：全场景库无同框组合，规范第 13 项也不要求同帧双跟；单视场免费多跟暂无需求证据。
3、F3 **defer**：分时轮转的占空比建模需要仲裁/失败归属/回放三件套前置设计，本期证据不足；最小闭环（单镜筒+宽场保持）已能消除不可能行为。
4、F4 **pass**：一字段双语义是错误行为载体；全库仅三星座场景用到 2，改动面集中。

推荐组合（最小闭环 A）：
1、窄场指向并发冻结为单镜筒（1）：一颗星同一时刻至多 1 条窄场精跟航迹。
2、落选目标保持宽场粗跟（custody 不丢）；三星座场景下每个目标仍另有他星窄场归属（日志已存在：卫星104→目标1 共 78 行、卫星204→目标2 共 79 行）。
3、验收第 13 项逐目标行不受影响（规范不要求同星双跟）。

待用户拍板：
1、口径选 A（最小闭环：镜筒数上限=1，双锁行为消失）还是 B（完整分时轮转+占空比）？建议 A；B 需补三件套设计再进入。
2、字段形态：保留 `max_concurrent_nfov_locks` 改语义+上限 1，还是删除字段、库内冻结常量 1？涉及公开头文件、12 个场景 json 与回放标识符是否 bump（配置语义变更按"中段字段变更必 bump 标识符"纪律评估）。
3、三星座场景 json 中 3 处 `max_concurrent_nfov_locks: 2` 的处置（随字段形态定），以及验收读数变化（卫星 4 由双锁变单锁目标1）是否需在验收文档加注。

## §3 冻结契约（用户讨论结束后填写）

<!-- 一行一项：
1、允许范围：模块/目录、类/函数、测试与文档。
2、明确禁止范围：公开头文件、跨模块类型、schema/回放、测试阈值、兼容层。
3、行为边界：输入、输出、错误回退、生命周期。
4、验收门：构建、聚焦测试、契约测试、特征化测试。
5、非目标。
-->

## 修订记录

<!-- 编号条目（修订 1、修订 2……）：日期、裁定内容、来源（用户指令/新证据）；不静默改写既有条目。 -->

## §4 运行记录（Stage C 后填写）

<!-- 1、实现范围。
2、验证命令与结果。
3、权威回写去向：哪个结论写进了哪个文件。
4、残留风险。
5、后续冻结项。
-->
