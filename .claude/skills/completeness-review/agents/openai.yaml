interface:
  display_name: "Completeness Review"
  short_description: "Correctness / quality / test coverage review of uncommitted changes with merge gate"
  default_prompt: "Use $completeness-review to review the current change set (correctness via code-review subagent, code quality via code-simplifier subagent, test coverage) and gate the merge."
