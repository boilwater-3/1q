---
Status: active
Last-reviewed: YYYY-MM-DD
Authority: <module_name> 算法登记与实现边界
Answers: <module_name> 用了哪些算法、各自实现到什么地步、边界在哪、哪些反直觉、哪些刻意不实现
---

# <Module Name> 算法登记

本文是 `<module_name>` 模块算法与部件清单及边界的权威。算法本身的逐步逻辑读代码（`src/<module_name>/`）；本文只回答“用没用/到哪步/为什么不做”。模块级边界（dt_sec、环境事实、输出/失败语义、public API 边界）见 [boundaries.md](boundaries.md)。

## 算法登记表

| 算法/部件 | 意图 / 核心转换 | 实现状态 | 证据与单测 |
|---|---|---|---|
| [算法一名称] | [一句话业务意图或 输入 $\to$ 输出] | session-wired | [evidence: tests/unit/<module_name>/xxx_test.cpp] |
| [算法二名称] | [一句话业务意图或 输入 $\to$ 输出] | session-wired | [evidence: tests/unit/<module_name>/yyy_test.cpp] |

实现状态说明：
- **session-wired**：已接入 Session / Pipeline 主链路，覆盖配置、执行周期、重放与主集成。
- **characterized**：具备局部测试，且有确定性的质量评估与失效矩阵，但未接入 Session 主链。
- **experimental**：可编译，有单元测试覆盖，但尚未完成全场景标定与质量验证。
- **internal/受控**：库内私有能力或内部支撑工具，不暴露为公开能力契约。

---

## 核心算法详述

## 1. [算法一中文名称] ([Algorithm One English Name])

### 1.1 算法意图与调用时机
- **业务意图**：[清晰阐明该算法解决的工程或物理问题]
- **调用时机与宿主**：由 `<HostClass>::<Method>` 在 [例如：RunCycle 准备期 / 探测流水线] 逐周期调用。
- **公共/私有归属**：[例如：核心委托 `src/common/...` 公共单源，模块侧负责参数拼装 / 模块私有实现]

### 1.2 数学与物理模型
- **输入与坐标系**：
  - 输入：$x_k \in \mathbb{R}^n$（[单位]），参考坐标系为 [ECEF / ECI / ENU / 天线极坐标]。
- **核心公式 / 计算步骤**：
  1. [步骤一描述]：
     $$公式表达$$
  2. [步骤二描述]：
     $$公式表达$$
- **关键参数基准表**：
  | 参数名 | 物理/算法含义 | 默认值 | 物理单位 / 范围 | 调优影响 |
  |---|---|---|---|---|
  | `param_a` | 门限值 | 10.0 | dB, [0, 40] | 调大降低虚警，调小提高灵敏度 |

### 1.3 实现边界与工程简化
- **复杂度开销**：时间复杂度 $\mathcal{O}(...)$，空间复杂度 $\mathcal{O}(...)$；[是否存在动态堆分配 / 是否纯函数可重入]。
- **物理简化与假设**：[说明所有作出的近似与模型舍弃]
- **状态维护**：[纯函数 / 持跨周期状态；若有状态，说明重置、失跟回收与超时逻辑]

### 1.4 边界保护与降级策略
- **数值退化与保护**：[非有限值（NaN/Inf）、除零、近共线等异常输入时的降级行为，如保底返回值或安全丢弃]
- **配置防呆保护**：[非法配置参数的 clamp 钳制或原子拒绝规则]

### 1.5 反直觉点与工程陷阱
> [!IMPORTANT]
> - **[反直觉点一标题]**：[描述与常见直觉不符但符合系统设计的行为，例如数值未归一化、0值代表极端有效而非缺失等]
> - **[反直觉点二标题]**：[例如坐标系微小偏移、时间戳对齐偏差等工程经验]

### 1.6 证据链
- **源码入口**：`src/<module_name>/[File.cpp]`
- **测试证据**：[evidence: tests/unit/<module_name>/xxx_test.cpp]

---

## 辅助/映射类算法（可选）

### [辅助算法名称]
- **意图**：[简述意图]
- **规则 / 映射**：[输入 $\to$ 输出规则]
- **实现边界与反直觉点**：[关键边界]
- **证据**：[evidence: tests/unit/<module_name>/zzz_test.cpp]

---

## 非目标（刻意不实现的算法）

以下能力经系统设计明确排除在本模块职责之外：
1. **[非目标一]**：[原因与替代方案，见 boundaries.md]
2. **[非目标二]**：[原因与替代方案]
