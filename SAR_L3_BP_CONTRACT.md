# SAR L3 Backprojection 工程契约

## 1. 审批目标

在已识别的一阶运动补偿 L3 失效区中，建立适用于非直线逐脉冲轨迹的内部 BP 聚焦路径，并保持与现有 GBP 参考真值一致。

本阶段只批准内部小场景 BP，不接入 public Session。

## 2. GBP/BP 共享数学契约

GBP 与 BP 必须共享：

- 相同的 raw pulse history 与 matched filter 输入。
- 相同的逐脉冲实际位置。
- 相同的 local Cartesian 输出网格。
- 相同的距离压缩。
- 相同的双程斜距到距离门映射。
- 相同的 linear 距离插值。
- 相同的传播相位补偿。
- 相同的 `128x128` 严格尺寸门。

禁止为 BP 复制独立的距离压缩、插值或传播相位公式。

## 3. 唯一实现差异

- GBP：按输出像素遍历全部脉冲并相干累加。
- BP：按脉冲遍历全部输出像素并相干累加。

每个像素内部的脉冲累加顺序必须保持一致，因此相同输入的 GBP/BP 复图必须逐样本一致。

## 4. 验收门

- L1 单点场景 GBP/BP 复图逐样本一致。
- L3 折线轨迹场景 GBP/BP 复图逐样本一致。
- 在一阶补偿明确失效的 `12 m` 横向偏移场景中，BP 相对 GBP 为零误差，并优于补偿后 RDA。
- BP 拒绝超过 `128x128` 的场景。
- 新增独立 `128x128` BP Debug 性能证据。
- 默认与 Eigen 3.3.9/C++11 和现有 SAR CI 继续通过。

## 5. 冻结边界

- BP 只作为内部显式路径，不进入 public Session 或 Auto。
- 不增加并行、GPU 或快速 BP 变体。
- 不扩大 `128x128` 尺寸门。
- 不实现时变 PRF 成像、二阶补偿、自聚焦、CSA 或 Omega-K。
- 不宣称折线转角具有物理平台动力学保真。

## 6. 后续审批

BP 与 GBP 独立回归和性能证据通过后，再讨论：

1. BP public 配置与 replay 契约。
2. L3 public Session 接入。
3. Auto 是否具备纳入 BP 的算法选择证据。
