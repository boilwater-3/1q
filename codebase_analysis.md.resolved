# 1Q 机载雷达仿真库 — 代码库分析报告

## 一、代码库总览

| 维度 | 概况 |
|------|------|
| **定位** | 机载雷达仿真模型库，以静态/动态库形式供外部项目链接 |
| **技术栈** | C++11, CMake, Conan, GTest/GMock, Eigen, Sophus, spdlog, eventpp |
| **公共头文件** | 32 个（`include/1q/airborne_radar/`） |
| **内部源文件** | 约 51 个内部头文件（`src/`） |
| **食用方式** | `find_package(1q CONFIG REQUIRED)` + `target_link_libraries(... 1q::1q)` |
| **使用级别** | 两层：**Session 门面**（一步到位）、**Controller + 接口注入**（高级扩展） |

---

## 二、架构分析

### 2.1 分层架构（5 层）

```mermaid
graph TB
    subgraph "外部调用方"
        External["外部仿真系统"]
    end

    subgraph "公共 API 层（include/）"
        Session["RadarSession<br/>高层门面"]
        Controller["RadarController<br/>调度控制器"]
        Presets["ConfigPresets<br/>预设工厂"]
        Queries["TrackOutputQueries<br/>查询工具"]
        Validation["RadarInputValidation<br/>输入校验"]
        Utils["TargetFeatureUtils<br/>目标构造工具"]
        SceneBuilder["EnvironmentSceneBuilder<br/>场景构造器"]
    end

    subgraph "核心接口层"
        IContext["IRadarContext"]
        ISignal["ISignalPipeline"]
        IEnv["IEnvironmentService"]
        IDecision["ITacticalDecisionEngine"]
        IOutput["IRadarOutputReader"]
    end

    subgraph "信号处理层（src/signal/）"
        Detection["探测（RadarEquations, SignalDetector）"]
        Association["关联（DataAssociation, Gater, LAPJV）"]
        Tracking["跟踪（EKF, IMM, TrackLifecycleManager）"]
    end

    subgraph "决策层（src/decision/）"
        Classifier["ThreatAssessmentEvaluator"]
        LPI["EmissionControlEvaluator"]
        ECCM["SurvivabilityEvaluator"]
        Reducer["ControlReducer"]
    end

    subgraph "环境层（src/environment/）"
        EnvService["EnvironmentService"]
        SceneManager["SceneManager"]
        Propagation["PropagationModel"]
    end

    External --> Session
    External --> Controller
    External --> Presets
    External --> Queries
    Session --> Controller
    Controller --> ISignal
    Controller --> IContext
    Controller --> IDecision
    Controller --> IEnv
    ISignal --> Detection
    Detection --> Association
    Association --> Tracking
    IDecision --> Classifier
    IDecision --> LPI
    IDecision --> ECCM
    Classifier --> Reducer
    LPI --> Reducer
    ECCM --> Reducer
    IEnv --> EnvService
    EnvService --> SceneManager
    EnvService --> Propagation
```

### 2.2 架构优点

| 编号 | 优点 | 具体体现 |
|------|------|----------|
| ✅ A1 | **门面模式（Facade）设计出色** | `RadarSession` 封装了 Context → Signal → Decision → Environment 全链路，外部只需 `Step(input)` |
| ✅ A2 | **依赖倒置原则（DIP）** | 5 个核心接口（`IRadarContext`, `ISignalPipeline`, `IEnvironmentService`, `ITacticalDecisionEngine`, `IRadarOutputReader`）实现了依赖倒置 |
| ✅ A3 | **PIMPL 隐藏实现** | `RadarSession` 使用 `struct Impl` + `unique_ptr` 模式，确保二进制兼容性 |
| ✅ A4 | **值类型 POD 数据传递** | `TargetFeature`, `TrackOutputFrame`, `RadarCycleInput` 等均为 POD/聚合类型，跨模块传递安全 |
| ✅ A5 | **命名空间与目录映射清晰** | `airborne_radar::core::session` ↔ `include/1q/airborne_radar/core/session/` 保持对应 |
| ✅ A6 | **Builder 模式构造复杂对象** | `EnvironmentSceneBuilder` 提供流畅的链式 API |
| ✅ A7 | **显式输入校验** | `RadarInputValidation` 提供结构化校验，有 severity + code + message |
| ✅ A8 | **配置预设工厂** | `ConfigPresets.h` 提供 `MakeDetectionMission*` / `MakeHighRobustness*` 等预设，降低初始化门槛 |
| ✅ A9 | **安装消费验证** | `tests/install_consumer/` 验证安装后的两种使用模式均可编译链接 |
| ✅ A10 | **公共头冒烟测试** | `public_headers_smoke_test.cpp` 验证全部 32 个公共头可统一包含 |

### 2.3 架构不足与隐患

| 编号 | 问题 | 严重程度 | 说明 |
|------|------|----------|------|
| ⚠️ B1 | **命名空间嵌套过深** | 中等 | 最深 4 层：`airborne_radar::core::session::RadarSession`，外部 using 声明冗长 |
| ⚠️ B2 | **JammingTechnique 枚举重复定义** | 高 | `environment::JammingTechnique`（EnvironmentTypes.h）与 `common::JammingTechnique`（DecisionSourceInfo.h）重复定义，语义相同但类型不同 |
| ⚠️ B3 | **RadarController 暴露了过多私有状态** | 中等 | 公共头中暴露了 `owned_decision_engine_`, `tactical_state_store_` 等 `unique_ptr` 私有成员（未使用 PIMPL），内部类型变更会导致外部项目重新编译 |
| ⚠️ B4 | **ClampFloat 重复实现** | 低 | `RadarOrientationUtils.h` 和 `AntennaPatternUtils.h` 各有一份 `ClampFloat`，且都在公共命名空间 |
| ⚠️ B5 | **Umbrella header 内容单薄** | 中等 | `api.hpp` 仅定义导出宏，不包含任何公共头，名称 `TEMPLATE_API` 仍是模板残留 |

---

## 三、使用接口易用性分析

### 3.1 易用性亮点

#### 3.1.1 最简使用路径（3 行代码跑通）

```cpp
// 示例：最简路径
RadarSession session(MakeDetectionMissionRadarSessionConfig());
RadarCycleInput input;
input.dt_sec = 1.0f;
input.target_features.push_back(MakeAirTarget(1001U, 180.0f, -5.0f, 18.0f, 65.0f, 0.0f, 0.0f, 1.0f));
TrackOutputFrame frame = session.Step(input);
```

> [!TIP]
> 门面模式极大降低了入门门槛——用户无需了解 Signal/Decision/Environment 自底向上的装配过程。

#### 3.1.2 查询辅助函数完善

```cpp
CollectConfirmedTracks(frame);        // 按状态筛选
CollectJammingTracks(frame);          // 按干扰标记筛选
BuildTrackMapByExternalTargetId(frame); // 按 ID 映射
CountTracksByStatus(frame, status);   // 统计
ContainsExternalTargetId(frame, id);  // 存在性判断
```

#### 3.1.3 高级扩展路径友好

外部可通过实现 5 个公共接口（`IRadarContext`, `ISignalPipeline`, `IEnvironmentService`, `ITacticalDecisionEngine`, `IRadarOutputReader`）自由替换任何层：

```cpp
// 自定义决策引擎注入
MyDecisionEngine my_engine;
RadarController controller(context, signal_pipeline, my_engine, env_service);
```

### 3.2 易用性问题

| 编号 | 问题 | 严重程度 | 具体描述 |
|------|------|----------|----------|
| 🔴 C1 | **TargetFeature 构造函数参数过多且位置相关** | 高 | 构造函数有 10 个 float 位置参数，极易误传顺序。`MakeAirTarget` 也有 12 个参数 |
| 🔴 C2 | **EnvironmentSceneBuilder::AddNoiseJammer 存在两种签名不一致** | 高 | 示例代码中直接用 `(float, float, float, float, bool)` 参数调用 `AddNoiseJammer`，但头文件中只声明了 `(const JammerEmitterState&)` 签名，两者不一致 |
| 🟡 C3 | **无版本号查询 API** | 中等 | `api.hpp` 没有 `GetVersion()` 或 `版本常量`；外部集成者无法在运行时确认库版本 |
| 🟡 C4 | **错误处理模型不统一** | 中等 | `ValidateRadarCycleInput` 返回问题列表但 `Step()` 不做校验也不抛出；无返回码或 `std::expected` 等机制提示失败 |
| 🟡 C5 | **JammerEmitterParams 与 JammerEmitterState 字段几乎完全相同** | 中等 | 两个类型仅差一个 `technique` 字段，增加了用户理解成本 |
| 🟡 C6 | **DecisionTrackSnapshot 便捷构造函数参数意义不直观** | 中等 | 10 个浮点参数无标签区分：`(velocity_x, velocity_y, velocity_z, rcs, ax, ay, az, jamming, ext_id, key)` |
| 🟡 C7 | **缺少前向兼容的序列化/反序列化** | 中等 | 所有数据结构为纯 POD 但无 JSON/Proto 序列化支持，外部记录/回放困难 |
| 🟢 C8 | **using 声明冗长** | 低 | 外部需写大量 `using airborne_radar::core::session::*`，命名空间最深 4 层 |

---

## 四、优化建议

### 4.1 高优先级（影响正确性与易用性）

#### 建议 1：统一 `JammingTechnique` 枚举

> [!WARNING]
> `environment::JammingTechnique` 和 `common::JammingTechnique` 完全相同但是不同类型，用户在环境层与决策层之间传递干扰类型时需要做显式转换，这是潜在的错误源。

**方案**：将 `JammingTechnique` 统一到 `common/` 下，删除 `DecisionSourceInfo.h` 中的重复定义，改为 `#include`。

#### 建议 2：为 `RadarController` 引入 PIMPL

> [!IMPORTANT]
> `RadarController` 在公共头中暴露了大量内部类型的 `unique_ptr` 私有成员（`TacticalStateStore`, `ControlReducer`, `IDataOutputManager` 等），这些均为内部（`src/`）头文件中的类型。虽然使用了前向声明避免头文件循环，但任何内部类型的新增/删除都会改变 `RadarController` 的大小，导致外部项目重新编译。

**方案**：参照 `RadarSession` 的做法，将 `RadarController` 改为 PIMPL 模式。

#### 建议 3：改善高参数数量的构造函数

对于 `MakeAirTarget`（12 参数）和 `TargetFeature` 构造函数（10 参数），建议：

```cpp
// 方案 A：Builder 模式（推荐）
TargetFeature target = TargetFeatureBuilder(1001U)
    .Position(180.0f, -5.0f, 18.0f)
    .Velocity(65.0f, 0.0f, 0.0f)
    .Rcs(1.0f)
    .Build();

// 方案 B：参数对象模式
struct AirTargetParams {
    uint64_t id;
    Vec3f position;
    Vec3f velocity;
    float rcs{1.0f};
    // ...
};
TargetFeature MakeAirTarget(const AirTargetParams& params);
```

### 4.2 中优先级（改善开发体验）

#### 建议 4：完善 `api.hpp` Umbrella Header

```cpp
// 建议内容
#pragma once

// Version
#define ONEQ_VERSION_MAJOR 1
#define ONEQ_VERSION_MINOR 0
#define ONEQ_VERSION_PATCH 0

// Export macros (保持现有逻辑，但修正 TEMPLATE_ → ONEQ_ 命名)
// ...

// Convenience umbrella includes（快速入门路径）
#include "1q/airborne_radar/core/session/RadarSession.h"
#include "1q/airborne_radar/core/context/RadarCycleInput.h"
#include "1q/airborne_radar/core/context/RadarInputValidation.h"
#include "1q/airborne_radar/core/output/TrackOutputQueries.h"
#include "1q/airborne_radar/common/ConfigPresets.h"
#include "1q/airborne_radar/common/TargetFeatureUtils.h"
#include "1q/airborne_radar/environment/EnvironmentSceneBuilder.h"
```

这将使外部用户的快速入门路径简化为 `#include "1q/api.hpp"` 一行。

#### 建议 5：提供便捷命名空间别名

在 `api.hpp` 或独立的 `convenience.hpp` 中提供：

```cpp
namespace oneq = airborne_radar;
namespace oneq_session = airborne_radar::core::session;
namespace oneq_env = airborne_radar::environment;
```

#### 建议 6：统一错误处理模型

当前 `Step()` 不做校验也不返回错误码，`ValidateRadarCycleInput()` 是可选调用。建议：

- **方案 A**：`Step()` 内部先调用 `Validate`，遇到 `kError` 返回空帧并设置内部错误状态（`GetLastError()`）
- **方案 B**：`StepWithResult()` 的 `RadarCycleResult` 中增加 `validation_issues` 字段
- **方案 C（最小侵入）**：保持现有行为，但在文档中明确说明 `Step()` 对非法输入的行为是"尽力而为"（defensive processing）

#### 建议 7：合并 `JammerEmitterParams` 与 `JammerEmitterState`

`JammerEmitterParams` 和 `JammerEmitterState` 仅差一个 `technique` 字段，建议：

```cpp
// 合并方案
struct JammerEmitterState {
    JammingTechnique technique{JammingTechnique::kUnknown};
    // ... 所有参数字段
};
// 删除 JammerEmitterParams，Builder 方法直接接受 JammerEmitterState 或单字段参数
```

### 4.3 低优先级（工程质量提升）

#### 建议 8：消除重复工具函数

- `ClampFloat` 在 `RadarOrientationUtils.h` 和 `AntennaPatternUtils.h`（内部命名空间 `antenna_pattern_internal`）中各有一份
- 建议在 `common/` 下提供统一的数学工具头 `MathUtils.h`

#### 建议 9：添加导出符号修饰

- `api.hpp` 中的 `TEMPLATE_API` 命名应替换为项目专属前缀如 `ONEQ_API`
- 所有公共类/函数应标注 `ONEQ_API` 以支持动态库构建

#### 建议 10：增加序列化支持

为核心数据类型（`RadarCycleInput`, `TrackOutputFrame`, `RadarCycleResult`）提供可选的 JSON 序列化/反序列化，便于外部记录和回放。推荐使用 `nlohmann/json` 或自定义的轻量方案。

---

## 五、API 分层清晰度评估

### 当前双层 API 设计

```
┌──────────────────────────────────────────────────┐
│  Layer 1: RadarSession (一步到位门面)             │  ← 推荐入口
│  - Step(input) / StepWithResult(input, scene)    │
│  - ConfigPresets 预设                             │
│  - TrackOutputQueries 查询工具                    │
│  - RadarInputValidation 输入校验                  │
│  - EnvironmentSceneBuilder 场景构造               │
└──────────────────────────────────────────────────┘
                      │
                      │ 需要自定义组件时向下穿透
                      ▼
┌──────────────────────────────────────────────────┐
│  Layer 2: RadarController + 接口注入              │  ← 高级扩展
│  - 自定义 ISignalPipeline                         │
│  - 自定义 ITacticalDecisionEngine                 │
│  - 自定义 IEnvironmentService                     │
│  - 自定义 IRadarContext                           │
└──────────────────────────────────────────────────┘
```

> [!NOTE]
> 这种双层设计模式非常好——它为 80% 的标准使用场景提供了简洁路径，同时为 20% 的高级定制场景保留了完整控制力。

### 穿透边界清晰度评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 门面层封装完整度 | ⭐⭐⭐⭐⭐ | `RadarSession` 覆盖了标准使用的所有场景 |
| 接口层抽象稳定性 | ⭐⭐⭐⭐ | 5 个核心接口定义清晰，但 `IRadarContext::UpdateRadarControlProfile` 有默认空实现，语义不明确 |
| 数据类型一致性 | ⭐⭐⭐ | `JammingTechnique` 重复、`JammerEmitterParams` 冗余 |
| 入门文档/示例 | ⭐⭐⭐ | 示例文件存在但 README 仅 4 字节 |
| 配置灵活度 | ⭐⭐⭐⭐⭐ | 多级嵌套配置 + 预设工厂覆盖多种任务模式 |

---

## 六、总结

### 综合评价

该代码库的**架构设计水平较高**——分层清晰、依赖倒置、PIMPL 隐藏实现、Builder 模式、值类型传递等现代 C++ 库设计实践运用得当。双层 API（Session 门面 + Controller 注入）的设计对不同层级的外部用户都很友好。

**主要改进方向**集中在三个领域：
1. **消除类型冗余**（`JammingTechnique` 重复、`JammerEmitterParams`/`JammerEmitterState` 近似重复）
2. **降低参数位置耦合**（多参数构造函数 → Builder / 参数对象模式）
3. **补齐公共头门面**（`api.hpp` 应包含版本信息和 umbrella include）

### 优先级排列

| 优先级 | 建议项 | 预估工作量 |
|--------|--------|------------|
| 🔴 P0 | 统一 `JammingTechnique` 枚举 | 小（1–2 小时） |
| 🔴 P0 | `RadarController` 引入 PIMPL | 中（半天） |
| 🟡 P1 | 改善高参数构造函数（Builder 模式） | 中（1 天） |
| 🟡 P1 | 完善 `api.hpp` umbrella + 版本号 | 小（1–2 小时） |
| 🟡 P1 | 统一错误处理模型 | 中（半天） |
| 🟡 P1 | 合并 `JammerEmitterParams`/`JammerEmitterState` | 小（2–3 小时） |
| 🟢 P2 | 消除重复工具函数 | 小（1 小时） |
| 🟢 P2 | 导出符号命名修正 | 小（1 小时） |
| 🟢 P2 | 序列化支持 | 大（2–3 天） |
| 🟢 P2 | 便捷命名空间别名 | 小（30 分钟） |
