---
description: Windows 本机构建：1q.sh / PATH / preset / BOM / 门禁
alwaysApply: true
---

# Windows 本机构建约定（v141 主线）

本机 Windows 开发走 `scripts/1q.sh`（Git Bash 或 Cursor/WSL bash 均可）。
包装脚本会 exec Windows 的 `cmake.exe` / `ctest.exe`。禁止用 **Linux 自带 cmake** 去配 `VisualStudio.*` preset（会编到另一套树，表现为「编得动、测不对」）。裸 `command -v ctest` 在 WSL 里仍可能找不到 `ctest.exe`——一律用 `1q.sh test`，不要手敲 Linux `ctest`。

## 会话门禁（每个终端 / 每次代理开跑前）

```bash
source scripts/activate_1q_git_bash.sh
scripts/1q.sh doctor
# doctor 须：cmake=有路径、ctest=有路径（指向 scripts/bin 包装或 *.exe）
# uname=Linux（WSL）可接受；SHELL_NOTE 不是失败。SHELL_WARNING 或 ctest=NOT FOUND 才失败
```

门禁失败：**先修 PATH / `ONEQ_CMAKE_ROOT`，禁止改业务代码碰运气。**

持久化（Git Bash）：`echo 'source "/d/1q/1q/scripts/activate_1q_git_bash.sh" 2>/dev/null' >> ~/.bashrc`
本机 cmake 路径：`scripts/1q_env.local.sh`（`ONEQ_CMAKE_ROOT='D:/environment/CMake'`，已在 .gitignore）

## 标准命令（一律走 preset / 1q.sh；同 preset 先 build 再 test）

bootstrap/configure **只在首次或依赖/CMake 变更后跑**——重跑 configure 会打破增量状态、触发大重建。
**`1q.sh build` 禁止隐式 reconfigure**：CMake 输入比 `generate.stamp` 新时 build 直接失败，须先 `configure` 对应 preset；切换 preset 须自己选对 configure/build 族（例 `VisualStudio.15.0-amd64` + `-release`）。
日常只编需要的 target（全解决方案 119 个工程，很少需要全编）。Windows preset 默认
`ENABLE_PCH=ON`；build preset 并行 `jobs: 4`。日常开发用 v141 Release，勿用 VS2015 迭代：

```bash
scripts/1q.sh bootstrap VisualStudio.15.0-amd64     # 一次性
scripts/1q.sh configure VisualStudio.15.0-amd64     # 一次性
scripts/1q.sh build VisualStudio.15.0-amd64-release --target <target>
scripts/1q.sh test VisualStudio.15.0-amd64-release -R "unit::<module>" -j 4
```

裸目录构建自 2026-08-21 起可用（64 位注册表 KitsRoot10 已修复为真实 SDK 路径，UCRTContentRoot 注入已退役）；
仍推荐 preset / 1q.sh 入口。`scripts/bin/ctest` / `1q.sh test` 会解析 `ctest.exe`。

## UTF-8 BOM（中文注释）

新建/重写带中文的 `.h/.cpp` 必须带 BOM。`.githooks/pre-commit`（activate 自愈激活 `core.hooksPath`）
会在提交时自动为暂存的 C/C++ 文件补 BOM 并重新暂存；分块暂存（暂存≠工作区）会被拦截提示。
手动兜底：`scripts/utf8_bom.py convert`（只处理 git 已追踪文件 → 新建文件先 `git add`）。
**禁止**删中文注释来消 C4819。

## 陈旧增量产物 / 文件锁

public 头布局变更后若模块测试批量 SEH `0xc0000005` / 无关 `bad_alloc`：

```bash
scripts/1q.sh clean-stale VisualStudio.15.0-amd64 <module_slug>
scripts/1q.sh build VisualStudio.15.0-amd64-release --target <tests>
```

写 `.obj` 出现 `Permission denied`：先解除 IDE/杀毒锁定再编，不要当逻辑 bug。

权威细节（环境准备、排障顺序、交付档、历史事件）见 `docs/practice/windows_local_build.md`。
