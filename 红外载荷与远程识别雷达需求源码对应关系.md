# 红外载荷与远程识别雷达需求源码对应关系

## 1. 文档说明

- 本版为全仓库视角修订版：对照范围由两个传感器模块扩展到仓库五层架构（传感器/适配/估计/推演/决策），行号与结论以 HEAD `cab7c187`（2026-08-18）复核为准，路径为仓库相对路径。
- 本版同时并入需求方第二版对照文档的需求变化：章节名称与层级以需求方提供的三张最新目录图片为基线，输入、输出及验收语义按其转述的《功能需求》卫星需求正文（需求方工作区 `docs/xq/功能需求.doc`，**未随本仓库提供**）采纳；需求改名与新需求条目见第 6 节。
- 对照范围：
  - 传感器层：`src/sbirs_sensor`、`src/remote_identification_radar`
  - 估计层：`src/fusion` + `src/common/estimation`
  - 推演层：`src/target_inference`
  - 决策层：`src/threat_assessment`
  - 其余仓库模块（flight_dynamic、navigation、airborne_radar 等）按需求能力存在性检索
- 分层契约：`docs/common/contract.md:146-193` 规定传感器层只做量测事实生产，不得承载估计/推演逻辑（依赖方向单向、传感器产品边界、滤波原语单源等规则）。多传感器融合、航迹滤波、轨迹推演等需求能力按架构归属在传感器模块之外的层实现，本文在对应需求行标注实现层。
- 为说明输入、输出和配置边界，少量引用了同仓库 `include/1q/...`；坐标转换引用了红外模块直接调用的 `src/common/coordinate`。
- 行号以当前工作区源码为准，格式为 `文件:起始行-结束行`；3.2.1.3 各行行号随验收日志落地
  （2026-08-18，分支 feature/sbirs-acceptance-log）刷新，其余行号仍以 `cab7c187` 为基线。
- 状态定义：
  - **完整**：核心计算、周期接线和结果/状态形成闭环。
  - **部分**：存在直接相关实现，但算法层级、接口或需求所含子能力不完整。
  - **未发现**：在全仓库生产源码（`src/`、`include/`）中未找到实现；不会用名称相近的能力替代。tests/ 与 examples/ 仅作佐证注记，不计为实现。
- 用户提供文本存在 OCR 错字、标识截断和编号前缀异常，本文按需求章节名称判断语义，保留原章节编号。

## 2. 总体结论

| 模块 | 完整覆盖重点 | 部分覆盖或主要缺口 |
| --- | --- | --- |
| 下一代过顶持续红外仿真验证系统 | 传感器层：目标探测门控、探测角度与最大探测距离、载荷测角误差、安装失准模型、宽窄视场基础扫描/指向/捕获、传感器内部持续跟踪、基础生命周期与排除原因事件；估计层：多源融合与逐航迹 UKF（默认关闭）；推演层：弹道轨迹序列与落点/发射点预测（无推力弹道，为本库设计选择）；宽窄视场验收量（2026-08-18 完整）：地面覆盖区/驻留时间/焦平面脱靶量/目标信号能量/宽窄切换连续检测计数器经 `[SbirsAccept]` 验收日志通道输出（CMake 开关 `ONEQ_ENABLE_SBIRS_ACCEPTANCE_LOG`，默认关闭；见 3.2.1.3）；评估层（2026-08-19）：定位精度五项误差提取与 AHP 指标体系（`precision_evaluation`，`[PrecisionEval]` 日志门控；见 3.2.1.6.3） | 卫星状态由外部输入，全仓库无卫星轨道动力学；传感器层估计真值初始化且不外发状态（SBIRS-OQ-4/TARGET-OQ-3）；特殊事件等级/空间位置/关键点事件与产品级信息分发未实现（见 3.2.1.6.1/3.2.1.6.2）；多传感器接力无协议（剩余覆盖时间/接力计划/交接指令按 2026-08-22 甲方批注以「视场宽度÷视线角速率」简易外推经融合验收行输出，交接需同航迹第二源）；集群多目标数量识别按 2026-08-22 甲方批注以确认航迹数计数口径经验收行输出；关机点判断按同批注以机械能最大点近似经推演验收行输出（无推力模型无关机事件） |
| 远程识别雷达 | 雷达方程、方向图、噪声/杂波/干扰、统计级 CFAR、多目标关联、KF/IMM、RCS 实时探测、四类目标特征识别、波束扫描 | 指定空域搜索约束不足；雷达滤波层无再入动力学（重力/阻力/弹道系数已在推演层 target_inference 建模，非雷达滤波层）；未强制 2-4 GHz；"全天候"仅有外部环境损耗；无处置/对抗执行链；资源调度缺少容量和预算约束 |

## 3. 红外预警卫星红外载荷需求映射

### 3.2.1 下一代过顶持续红外仿真验证系统

**状态：部分。** 该需求由 `src/sbirs_sensor/session/SbirsSession.cpp:29-42` 的会话入口、`src/sbirs_sensor/runtime/SbirsController.cpp:19-67` 的周期控制和 `src/sbirs_sensor/pipeline/SbirsPipeline.cpp:523-1319` 的载荷仿真主链共同承载。传感器级探测、宽窄视场基础链和生命周期事件已有实现；卫星轨道动力学全仓库未实现；多传感器融合、UKF 与落点/发射点预测按分层契约在估计/推演层实现（见 3.4/3.5 节）；正文规定的完整宽窄视场产品（覆盖区/驻留时间/焦平面量/信号能量/连续命中计数器）已经验收日志通道闭环（见 3.2.1.3，公开输出结构不变），多传感器接力与产品级关机点判断未形成闭环（关机点已按 2026-08-22 甲方批注以机械能最大点近似经推演验收行输出）。

### 3.1 轨道运动能力

#### 3.2.1.1 模拟 OPIR 轨道运动能力

**状态：部分。** 模块消费调用方逐周期提供的卫星 ECEF 位置、速度、姿态和 UTC 时刻，并统一变换到 ECI；它不是卫星轨道传播器。

核心调用链：

- `src/sbirs_sensor/session/SbirsInputValidation.cpp:47-120`，`ValidateSbirsCycleInput` 校验时刻、位置、速度和姿态。
- `src/sbirs_sensor/pipeline/SbirsPipeline.cpp:567-605`，`SbirsPipeline::RunCycle` 将卫星和场景状态转入 ECI。
- `src/sbirs_sensor/pipeline/SbirsPipeline.cpp:794-805`，卫星速度进入相对视线角速度计算。

| 需求 | 状态 | 源码对应关系与实现说明 |
| --- | --- | --- |
| 3.2.1.1.1 实时计算卫星当前轨道位置 | **部分** | `SbirsInputValidation.cpp:72-88` 接收并校验当前 ECEF 位置/速度；`SbirsPipeline.cpp:581-605` 计算本周期 ECI 位置/速度。当前位置本身由外部输入，全仓库未发现由轨道根数或上一周期状态实时传播当前位置的模型。 |
| 3.2.1.1.2 构建卫星轨道动力学模型 | **未发现** | 全仓库未发现开普勒、J2、SGP4、摄动力、轨道根数或数值轨道传播状态（flight_dynamic 为飞机 JSBSim 机体动力学，navigation 为区域覆盖航路规划）。现有 `SbirsPipeline.cpp:1014-1041` 仅为 cue 延迟做一次 `p+v*t` 线性外推，不构成轨道动力学模型。 |
| 3.2.1.1.2.1 坐标系转换 | **完整** | `SbirsPipeline.cpp:567-605` 完成入口接线；依赖 `src/common/coordinate/InertialTransform.cpp:41-51` 的 `TryComputeGmstRad`、`:53-63` 的 `TryEcefToEci`、`:78-100` 的 `TryEcefVelocityToEci`。速度转换包含地球自转输运项 `R*v + omega x r`。 |
| 3.2.1.1.2.2 卫星轨道动力学计算 | **未发现** | 全仓库未发现面向卫星的引力、J2、大气阻力、第三体摄动或轨道状态微分方程。`SbirsPipeline.cpp:794-805` 只是用已输入速度计算 LOS 角速度。目标弹道的重力/阻力模型见推演层 target_inference（3.2.1.5.3），与卫星轨道动力学无关。 |
| 3.2.1.1.2.3 龙格库塔积分 | **部分** | 仓库已有生产 RK4：`src/target_inference/TargetInferenceEngine.cpp:56-89` 的 `Rk4Step` 实现经典四阶龙格-库塔（含负步长回推），用于目标弹道状态积分。卫星轨道传播语境下全仓库未发现积分器——没有以 RK 推进的轨道状态微分方程，也没有积分步长控制。 |

### 3.2 探测与跟踪能力

#### 3.2.1.2 模拟 OPIR 预警卫星对目标的探测与跟踪能力

**状态：部分。** `src/sbirs_sensor/pipeline/SbirsPipeline.cpp:719-1047` 依次执行地球遮挡、距离、视场、红外 SNR、带误差量测和 WFOV 候选生成；`:1109-1238` 执行 NFOV 捕获，`:812-930` 执行持续跟踪、滑行和丢锁。单载荷探测跟踪主链完整，但卫星自身定位误差（导航系统给出的卫星位置与真实位置之间的偏差）只折算为等效角误差注入量测噪声，未在三维空间中建模卫星位置误差。

| 需求 | 状态 | 源码对应关系与实现说明 |
| --- | --- | --- |
| 3.2.1.2.1 探测设备可观测范围生成 | **完整** | `SbirsPipeline.cpp:731-777` 形成遮挡和距离范围；`:779-810` 形成扫描角、视场与 SNR 可观测条件；`:787-792` 逐目标计算当前环境下最大探测距离。 |
| 3.2.1.2.1.1 探测角度计算 | **完整** | `SbirsPipeline.cpp:753-785` 由卫星到目标 LOS 计算 ECI 方位/俯仰并旋入传感器系；`src/sbirs_sensor/foundation/SbirsGeometry.cpp:45-55` 的 `ComputeAzimuthDeg`、`ComputeElevationDeg` 完成角度计算；`SbirsPipeline.cpp:807-810` 进行矩形视场判定。 |
| 3.2.1.2.1.2 最大探测距离计算 | **完整** | `src/sbirs_sensor/foundation/SbirsRadiometry.cpp:34-49` 的 `ComputeMaxDetectionRangeM` 根据辐射强度、孔径、透过率、量子效率、积分时间、噪声和 SNR 门限反解距离；`SbirsPipeline.cpp:787-792` 周期接线。 |
| 3.2.1.2.2 目标可探测性与动态参数生成 | **部分** | `SbirsPipeline.cpp:731-810` 生成遮挡、距离、视场、SNR、最大探测距离和相对角速度判据；`:994-1047` 生成候选量测及 cue 参数。动态参数主要限于距离、角度、SNR、LOS 角速度和短时外推，没有独立完整的目标运动参数产品。 |
| 3.2.1.2.3 高速动态目标观测误差模拟 | **完整** | `src/sbirs_sensor/foundation/SbirsErrorModel.cpp:59-101` 合成轨道、姿态、FOV 随机角误差、折射、动态滞后和距离比例误差；`SbirsPipeline.cpp:794-805` 计算高速目标相对角速度，`:1000-1007` 应用到 WFOV 量测。 |
| 3.2.1.2.3.1 卫星自身定位误差 | **完整** | `SbirsErrorModel.cpp:59-80` 将 `orbit_sigma_deg` 作为等效随机角误差预算，`src/sbirs_sensor/tracking/SbirsTrackingTypes.h:146-183` 的 `BuildMeasurementCovariance` 将其与折射、动态滞后合成角度量测协方差。卫星自身定位误差（导航定位偏差）折算为等效角误差注入量测。 |
| 3.2.1.2.3.2 红外载荷测角误差 | **完整** | `SbirsErrorModel.cpp:38-57` 计算折射和动态滞后，`:59-101` 计算角度/距离误差；`SbirsPipeline.cpp:1000-1007` 用于 WFOV 量测，`:905-910`、`:1219-1224` 用于传感器型真值辅助跟踪输出。 |
| 3.2.1.2.3.3 安装矩阵误差 | **完整，按安装失准角模型口径** | `src/sbirs_sensor/pipeline/SbirsPipeline.cpp:371-388` 的 `DrawMisalignmentTotal` 按配置生成运行期一次常值失准总量（常值偏置 + 每轴一次随机微扰，配置不变时运行内确定）；`src/sbirs_sensor/pipeline/SbirsBoresightChain.cpp:35-66` 将卫星姿态、Body-to-Sensor 安装角和安装失准旋转组成变换矩阵；`SbirsPipeline.cpp:529-550` 每周期用于实际光轴计算。该口径为失准角等效模型，非安装矩阵元素级误差标定。 |

### 3.3 宽视场和窄视场能力

#### 3.2.1.3 模拟 OPIR 宽视场扫描探测和窄视场跟踪探测能力

**状态：完整（验收量经日志通道，公开输出不变）。** 基础扫描、视场判定、宽场到窄场 cue、指向捕获、持续跟踪与本章所列全部验收量均已实现：地面覆盖区域（FOV 角-地球交会投影，圆球模型与遮挡判定同口径）、驻留时间（`FOV_az / scan_rate` 派生公式）、焦平面脱靶量（`focal_length_m`/`detector_pixel_pitch_m` 配置映射）、目标信号能量（`ComputeSnr` 出参透出）、宽窄切换连续检测计数器（`wide_to_narrow_required_consecutive_hits`，默认 1 行为不变）均已落地（2026-08-18），统一经**验收日志通道**输出：CMake 开关 `ONEQ_ENABLE_SBIRS_ACCEPTANCE_LOG` 开启后由 `SBIRS_ACCEPTANCE_LOG` 宏以 `[SbirsAccept]` 事件流写入 `1q_library.log`（`src/sbirs_sensor/pipeline/SbirsAcceptanceLog.h`）；关闭时零开销。口径说明：上述量不进公开输出结构（`SbirsOutputFrame` 不变，见 `docs/sbirs_sensor/boundaries.md` 非目标 10）——若需求方正式验收以 API 级输出为准则，此为唯一口径差异点。威胁排序按分层契约在决策层进行，不属于传感器模块。

| 需求 | 状态 | 源码对应关系与实现说明 |
| --- | --- | --- |
| 3.2.1.3.1 红外预警卫星大幅面探测 | **完整（验收量经日志通道）** | `SbirsPipeline.cpp:675-712` 实现二维方位/俯仰栅格扫描（含 `event=scan_footprint` 覆盖区日志），`:817-1150` 完成逐目标探测。正文要求的各扫描周期地面覆盖区域坐标和驻留时间已落地：覆盖区 = 实际扫描中心 ±半视场四角经指向链到 ECI 后与地球圆球交会（`SbirsGeometry` 的 `TryIntersectRayWithSphere`/`ComputeGeocentricLatLonDeg`/`TryComputeGroundIntersectionLatLonDeg`，交点旋回 ECEF 取经纬度，指向太空记 miss）；驻留时间 = `wide_field_fov_az_deg / scan_rate_deg_per_sec`；两者经 `SBIRS_ACCEPTANCE_LOG`（开关 `ONEQ_ENABLE_SBIRS_ACCEPTANCE_LOG`）输出，公开输出 `SbirsOutputTypes.h` 仍只有方位、俯仰、SNR、阶段和检测标志。 |
| 3.2.1.3.2 宽视场扫描探测和窄视场跟踪探测 | **完整（验收量经日志通道）** | `SbirsPipeline.cpp:1127-1225` 生成 WFOV 候选和 cue（含 `event=wfov_candidate` 日志），`:1255-1420` 完成 NFOV 调度、指向、捕获及状态晋级（含 `event=nfov_acquisition`/`nfov_schedule` 日志），`:866-1105` 保持已捕获目标（含 `event=nfov_track` 日志）。正文所列宽场覆盖区/疑似目标列表、窄场精确状态序列、焦平面量和目标信号能量均经 `[SbirsAccept]` 事件流输出（接收功率/信号能量由 `ComputeSnr` 出参透出）；不进公开输出结构。 |
| 3.2.1.3.2.1 宽窄视场联合探测 | **完整** | `src/sbirs_sensor/pipeline/SbirsCuePredictor.cpp:24-55` 根据前后两次有效 WFOV 角度估计角速度并生成提前 cue；`SbirsPipeline.cpp:1141-1170` 处理 cue 时延，`:1330-1400` 以预测后几何和 SNR 判定 NFOV 捕获（`src/sbirs_sensor/pipeline/SbirsNfovAcquisition.cpp:19-27` 的 `IsNfovAcquisitionEligible` 为几何门 + SNR 门单次判定）。WFOV 连续命中计数器已实现为切换前置条件：候选创建点自增、门失败/目标消失/待机清零、捕获成功清零，新候选须 `hits >= wide_to_narrow_required_consecutive_hits`（`SbirsPolicyConfig.h` 调度域，默认 1 与既有单次命中调度行为逐位一致）才进入调度；计数随 `event=wfov_candidate` 输出，被挡下时输出 `event=wfov_hit_gate`，计数表进 `SbirsPipelineSnapshot`。 |
| 3.2.1.3.2.2 宽视场扫描探测 | **完整（验收量经日志通道）** | `SbirsPipeline.cpp:200-250` 定义扫描角、行数、行中心和矩形视场，`:675-712` 推进二维扫描，`:1105-1225` 形成宽场检测。地面覆盖区域坐标与驻留时间同 3.2.1.3.1 的实现（`event=scan_footprint`，圆球模型 `kEarthRadiusM` 与遮挡判定同口径），疑似目标带误差量测、真值对照、d_max、信号能量随 `event=wfov_candidate` 输出。 |
| 3.2.1.3.2.3 窄视场跟踪探测 | **完整（验收量经日志通道）** | `src/sbirs_sensor/pipeline/SbirsNfovAcquisition.cpp:19-27` 判定首次捕获；`SbirsPipeline.cpp:866-1105` 以实际指向、NFOV 几何门和 SNR 门持续跟踪，并支持 coasting 和丢锁；`SbirsOutputTypes.h:92-97` 提供角度指向误差和跟踪诊断。正文要求的焦平面脱靶量、跟踪状态、目标信号能量与 SNR：跟踪状态、SNR、角度指向误差与 NIS 随 `event=nfov_track` 输出；焦平面脱靶量已按 `x=f·tan(Δaz)`、`y=f·tan(Δel)` 映射（`ComputeFocalPlaneOffset`，米+像素），配置项 `SbirsHardwareConfig.focal_length_m`/`detector_pixel_pitch_m`（默认 2.0 m / 30 μm，校验须为正）；信号能量（`E=P·t_int`）由 `ComputeSnr` 出参透出进同一事件。 |
| 3.2.1.3.2.4 协同工作机制 | **完整（威胁评分归决策层）** | `src/sbirs_sensor/pipeline/SbirsNfovScheduler.cpp:56-85` 按 SNR 降序、距离升序、目标 ID 升序选择候选，`SbirsPipeline.cpp:1470-1560` 分配有限 NFOV 通道并标记资源跳过（`event=nfov_schedule`：选中列表、通道分配、locked/max、resources_full、skipped）；`:960-1100` 对 NFOV 几何/SNR 门连续失败计数，达到阈值后释放锁定并回到宽场候选，`:1300-1330`、`:1425-1435` 也处理指向超时和捕获失败回退（各释放点输出 `event=nfov_release`：通道、原因、失败计数）。**威胁等级/航迹质量/任务优先级联合评分按分层契约在决策层进行，不属于传感器模块**（决策层 `src/threat_assessment/ThreatEvaluator.cpp`），不在 `[SbirsAccept]` 事件流中。 |

### 3.4 OPIR 目标跟踪能力

#### 3.2.1.4 模拟 OPIR 探测器目标跟踪能力

**状态：部分。** 传感器层（分层契约禁止其承载跨传感器逻辑）内仅有单载荷目标状态机、EKF/IMM(EKF) 和多 NFOV 通道；跨源量测归并、统一融合航迹与逐航迹滤波已在仓库估计层（`src/fusion` + `src/common/estimation`）实现，多传感器接力协议全仓库未发现。

| 需求 | 状态 | 源码对应关系与实现说明 |
| --- | --- | --- |
| 3.2.1.4.1 多传感器目标跟踪 | **完整（估计层）** | `include/1q/fusion/SensorAdapters.h:38-39` 定义 SBIRS/RIR 源通道（`kSbirsSourceId=4`、`kRirSourceId=5`），`:84`/`:101` 将 SBIRS 检测与 RIR 特征量测适配为泛型 `DetectionRecord`；`src/fusion/FusionEngine.cpp:277-298` 身份键直挂、`:356-451` 空间（KD-tree）/方位/特征跨源关联、`:653-707` 统一 `FusedTarget` 航迹输出（tentative/confirmed/coasting 生命周期与逐航迹运动学估计）。跨传感器量测集合、传感器标识、量测-航迹关联、统一航迹接口四要素齐备。传感器层内 `src/sbirs_sensor/pipeline/SbirsTrackingCoordinator.cpp:69-163` 仅对单载荷已知 `target_id` 的目标执行 EKF/IMM（分层契约规则 3 豁免的内部闭环）。 |
| 3.2.1.4.2 多传感器接力跟踪 | **未发现** | 全仓库未发现 handover 源/目的传感器、接力消息和接力状态机。`SbirsPipeline.cpp:994-1238` 的 WFOV 到 NFOV 交接是同一载荷内部 cue；估计层 fusion 的同键跨源归并是融合不是交接协议，不能等同于多传感器接力。 |
| 3.2.1.4.3 协同探测信息融合 | **完整（估计层，样本级证据融合口径）** | `FusionEngine.cpp:470-487` 对跨源航迹按源权重累积置信度（判决×质量×权重），分通道样本滑窗管理；示例层 `examples/component_attachment/components/fusion_component.cpp:39-53` 每周期聚合 AR+ESR+EOS+SBIRS 四源量测进入同一融合引擎。未做协方差交叉或航迹级融合。 |

### 3.5 轨迹预报能力

#### 3.2.1.5 模拟 OPIR 对目标的轨迹预报能力

**状态：部分。** 传感器层内只有服务于指向的短时 cue、线性相对位置外推和滤波下一周期预测；弹道轨迹产品（航路点序列、落点、发射点及误差预算）在推演层 `src/target_inference` 实现（无推力弹道模型，按设计选择不建模推力/质量）；关机点判断无推力模型不建模燃烧段，按 2026-08-22 甲方批注以机械能最大点近似（ε=½|v|²−μ/|r| 峰值下降沿，μ 取 TargetInferenceConfig 单源）经推演验收行输出（`src/target_inference/InferenceAcceptanceRecords.cpp`，开关 ONEQ_ENABLE_INFERENCE_ACCEPTANCE_LOG）。

| 需求 | 状态 | 源码对应关系与实现说明 |
| --- | --- | --- |
| 3.2.1.5.1 目标状态估计 | **部分；传感器层 EKF/IMM(EKF)，估计层另有无迹滤波** | `src/sbirs_sensor/pipeline/SbirsTrackingCoordinator.cpp:25-57` 初始化六维状态，`:69-99` 预测，`:101-163` 角度量测校正和 NIS 判定；`SbirsPipeline.cpp:823-827`、`:889-913` 接入持续跟踪。但初始化均值直接使用输入目标真值位置/速度（已登记为 SBIRS-OQ-4，`docs/common/open_questions.md:33`、`:211`），公开检测产品只输出角度和 SNR（TARGET-OQ-3，`docs/common/open_questions.md:37`、`:303`），未发布完整估计状态与协方差。估计层 `FusedTarget` 发布位置/速度/协方差运动学估计（`FusionEngine.cpp:683-703`，由量测初始化，默认关闭）。 |
| 3.2.1.5.1.1 UKF 滤波 | **完整（估计层，默认开启）** | `src/common/estimation/UnscentedPredictor.h:41`、`UnscentedUpdater.h:44`、`UnscentedTransform.h` 提供无迹原语（另有 `UdkfPredictor`/`UdkfUpdater`）；`src/fusion/FusionEngine.cpp:554-565` 逐航迹无迹预测、`:586-599` 位置量测无迹更新、`:610-631` ENU 方位量测无迹更新；`include/1q/fusion/FusionConfig.h` 的 `enable_track_filtering` 默认 true，置 false 时融合行为与无滤波版本一致。 |
| 3.2.1.5.1.2 集群多目标数量识别 | **部分；计数口径（验收行，2026-08-22 甲方批注）** | 无点迹聚类/集群划分/不可分辨目标合并模型。按批注「知道能打出几枚即可」的计数口径，RIR 验收行输出集群目标数量=确认航迹数（`src/remote_identification_radar/runtime/RirAcceptanceRecords.cpp` `WriteRirClusterCount`，`RirController.cpp` 逐周期接入 `rir_acceptance.log`，开关 `ONEQ_ENABLE_RIR_ACCEPTANCE_LOG`）。相邻基础设施注记：`src/common/geometry/BearingCluster.h:110-133` 提供同方向相干计数（fusion 用作方位关联门），ESR `KdTreeClusterer.h` 为辐射源截获聚类，均非目标群数量识别产品。`SbirsPipeline.cpp:704-1047` 仅按输入中已有的 `target_id` 分别处理已知目标；`src/sbirs_sensor/session/SbirsExternalOutputAdapter.cpp:6` 仅返回当前输出帧的检测记录数。 |
| 3.2.1.5.2 目标轨迹预测 | **完整（推演层，无推力弹道）** | `src/target_inference/TargetInferenceEngine.cpp:34-53` 中心引力 + 指数大气密度（弹道系数）模型、`:56-89` RK4 积分、`:182-226` 前向航路点序列（默认时域 300 s、间隔 10 s，`include/1q/target_inference/TargetInferenceConfig.h:21-23`）、经纬高输出及落点。无推力弹道为本库设计选择，不建模推力/质量。 |
| 3.2.1.5.3 落点及其他关键点预测 | **完整（推演层，无推力弹道）** | 推演层 `src/target_inference/TargetInferenceEngine.cpp:182-271` 实现落点与前推/回推关键点解算，`:273-366` 敏度 `J·P·Jᵀ` 误差预算，输出类型 `include/1q/target_inference/InferenceResult.h:32` 的 `TrajectoryPrediction`。无推力弹道为本库设计选择；关机点判断无推力模型不建模燃烧段，按 2026-08-22 甲方批注以机械能最大点近似（ε=½|v|²−μ/|r| 峰值下降沿，μ 取 TargetInferenceConfig 单源）经推演验收行输出（`src/target_inference/InferenceAcceptanceRecords.cpp`，开关 ONEQ_ENABLE_INFERENCE_ACCEPTANCE_LOG）。 |
| 3.2.1.5.3.1 落点预测 | **完整（推演层）** | `TargetInferenceEngine.cpp:182-226` 前向 RK4（引力 + 指数大气阻力）至地表穿越、线性插值得 `impact_point`/`impact_time_offset_sec`，`:322-366` 落点 1σ（`impact_position_sigma_m`）。 |
| 3.2.1.5.3.2 发射点预测 | **完整（推演层）** | `TargetInferenceEngine.cpp:228-271` 负步长 RK4 回推（地表交点或速度停机门）得 `launch_point`/`launch_time_offset_sec`；`:273-320` 敏度 `J·P·Jᵀ` 得 6×6 ECEF 发射点协方差（`launch_covariance_ecef`，可导出位置误差椭圆）与 `launch_position_sigma_m`。 |
| 3.2.1.5.3.3 关机点判断 | **超出范围** | 无推力弹道模型为本库设计选择，不建模目标推力、燃烧阶段和关机/燃尽事件，因此关机点判断不适用。 |

### 3.6 特殊事件、信息分发和精度分析

#### 3.2.1.6 模拟生成特殊事件提示信息和分析精度结果的能力

**状态：部分。** 已实现探测生命周期和排除原因事件；落点预报已在推演层实现（见 3.2.1.5.3.1）；定位精度分析（五项误差提取）与层次分析法已于 2026-08-19 在评估层 `precision_evaluation` 落地（见 3.2.1.6.3，`[PrecisionEval]` 验收日志 + 评估报告，公开输出结构不变）；正文所列事件等级/空间位置/关键点事件、产品级信息分发仍未实现（3.2.1.6.1/3.2.1.6.2）。

| 需求 | 状态 | 源码对应关系与实现说明 |
| --- | --- | --- |
| 3.2.1.6.1 特殊事件监测与提示 | **部分** | `src/sbirs_sensor/session/SbirsDetectionLifecycleRecorder.cpp:56-83` 推断原因，`:154-238` 生成首次发现、更新、滑行、丢失和未检测事件；`src/sbirs_sensor/session/SbirsExclusionCauseRecorder.cpp:73-143` 生成排除进入、变化和退出事件。对照正文：事件结构 `include/1q/sbirs_sensor/session/SbirsDetectionLifecycleRecorder.h:49-73` 只有周期、目标及观测诊断（SNR/距离/NIS/NFOV 诊断），未包含事件等级、事件地理/ECI 位置、轨迹突变、落点或发射点事件；`include/1q/sbirs_sensor/session/SbirsOutputTypes.h:107-160` 的通用问题条目 severity 与校验定位字段不构成事件等级/空间位置语义。记录器仅缓存最近一次执行周期事件（`SbirsDetectionLifecycleRecorder.h:103-121`）；replay 编解码只覆盖 `SbirsOutputFrame`（`src/sbirs_sensor/session/SbirsReplayFlatbufferCodec.h:30,53`），生命周期事件不进入输出帧，全仓库未实现事件持久化日志。注记：推演层每周期输出 `has_impact`/`has_launch` 关键点结果（`InferenceResult.h`），属结果字段而非事件产品。 |
| 3.2.1.6.2 落点预报与信息分发 | **部分** | 落点预报已在推演层实现（`TargetInferenceEngine.cpp:182-226`，含落点 1σ；发射点含 6×6 协方差，见 3.2.1.5.3.2）。对照正文：无置信度字段；信息分发未发现产品级实现——全仓库无事件总线、发布/订阅或订阅者分发器；仅示例层 `examples/component_attachment/core/signals.h:5,25` 使用 Boost.Signals2 做组件事件分发、`include/1q/replay/ReplayTrace.h:230,387` 提供回放事件回调。`src/sbirs_sensor/session/SbirsSession.cpp:29-50` 仍为同步返回周期结果并更新本地 recorder。 |
| 3.2.1.6.3 定位精度分析 | **完整（评估层，2026-08-19）** | 评估层新模块 `include/1q/precision_evaluation/` + `src/precision_evaluation/`：`PrecisionEvaluationSession` 编排双 SBIRS 会话（同场景双星历，主/辅星独立仿真）+ fusion 逐航迹滤波 + target_inference 推演，逐周期提取五项误差——红外定位角度误差（输出角 vs 真值角）、双星双视线交会位置误差（`DualLosFix.cpp` 异面直线最近点解算，附几何残差）、速度误差（`FusedTarget` 运动学估计 vs 真值）、落点/发射点预测误差（估计状态与真值状态经同一推演引擎的关键点之差，衡量状态误差传播）；误差序列汇总 mean/RMSE/P95/max（`PrecisionEvaluationMetrics.cpp`）。真值只进评估层、不回写产品（分层契约规则 9，`docs/common/contract.md`）；双星角度经 `SbirsBearingAdapter`（归属层 target_id 恢复身份键 + ECI 角→卫星局部 ENU）进入融合三维方位滤波。输出走 `[PrecisionEval]` 验收日志（开关 `ONEQ_ENABLE_PRECISION_EVALUATION_LOG`，默认 OFF）与 `PrecisionEvaluationReport` 结构。CEP/置信区间未做（正文未点名）。 |
| 3.2.1.6.3.1 关键精度指标提取 | **完整（评估层）** | 五项指标逐周期提取并汇总为 mean/RMSE/P95/max：角度误差另有 sbirs 验收日志显式字段（`SbirsPipeline.cpp` E2 `wfov_candidate`/E5 `nfov_track` 的 `az_error_deg/el_error_deg`，开关 `ONEQ_ENABLE_SBIRS_ACCEPTANCE_LOG`）；双星交会、速度、落点/发射点误差由 `PrecisionEvaluationSession::Step` 逐周期采样（事件 `angular_error`/`dual_sat_fix`/`velocity_error`/`keypoint_error`）。推演层的落点/发射点 1σ 与协方差（`TargetInferenceEngine.cpp:273-366`）仍属自报误差预算，与本指标体系（对真值误差）互补。 |
| 3.2.1.6.3.2 层次分析法 | **完整（评估层）** | `src/precision_evaluation/AhpEvaluator.cpp`：5×5 Saaty 1-9 互反判断矩阵（默认全 1 = 等权；对角 1/倒数对称/标度域校验，非法显式拒绝不静默退化）→ 幂迭代主特征向量权重 → λmax/CI/CR 一致性（RI 表 n≤5 固化，CR≤0.1 判一致，不一致时权重仍输出但显式标志）→ 归一化评分 `ref/(ref+rmse)` 加权合成综合定位精度得分与逐指标贡献分解（`ComposePrecisionScore`）；判断矩阵与五指标参考误差经 `PrecisionEvaluationConfig` 配置。事件 `metric_summary`/`ahp_score` 随 Summarize 输出。 |

## 4. 远程识别雷达需求映射

### 3.2.2 远程识别雷达

**状态：部分。** 该需求由 `src/remote_identification_radar/session/RirSession.cpp:195-303` 的会话和任务入口、`src/remote_identification_radar/runtime/RirController.cpp:271-386` 的检测量测链及 `:406-581` 的探测-跟踪-识别周期主链共同承载。基础探测、跟踪和识别能力较完整，但指定空域、雷达滤波层再入动力学、全天候约束、应对执行和受约束资源调度仍不完整；目标弹道的重力/阻力/弹道系数建模在仓库推演层（target_inference，见 3.2.1.5.3），不属雷达滤波层。

### 4.1 远程雷达探测能力

#### 3.2.2.1 模拟远程雷达的探测能力

**状态：部分。** `src/remote_identification_radar/runtime/RirController.cpp:271-386` 的 `TryBuildMeasurement` 完成角距、方向图、RCS、环境/干扰、检测判决和量测误差总接线，`:406-488` 完成每周期探测、关联和跟踪；但指定空域裁剪和公开角距量测不完整。

#### 3.2.2.1.1 检测是否存在目标

**状态：完整，按统计/效能级检测口径。**

| 需求 | 状态 | 源码对应关系与实现说明 |
| --- | --- | --- |
| 3.2.2.1.1 检测是否存在目标 | **完整** | `src/remote_identification_radar/dwell/RirSignalDetector.cpp:29-84` 的 `Detect` 和 `:86-113` 的 `DetectResolvedCell` 计算 SNR/SINR、Pd 和概率判决；`RirController.cpp:358-364` 应用检测准入门。 |
| 3.2.2.1.1.1 天线方向图仿真 | **完整** | `src/remote_identification_radar/dwell/RirAntennaPatternRuntime.h:90-128` 判定主瓣并计算主瓣衰减、扫描损耗、旁瓣/后瓣；`src/remote_identification_radar/dwell/RirBeamControl.h:58-63` 解析有效波束宽度，`:94-118` 解析指向波束状态；`RirController.cpp:284-296` 接线。 |
| 3.2.2.1.1.2 回波功率计算 | **完整** | `src/remote_identification_radar/internal/RirRadarEquations.cpp:33-48` 实现单站雷达方程；`src/remote_identification_radar/dwell/RirDetectionCellResolver.cpp:221-235` 在 detection cell 中计算回波。包含发射功率、双程增益、波长、RCS、`R^-4` 和损耗。 |
| 3.2.2.1.1.3 干扰功率计算 | **完整** | `RirDetectionCellResolver.cpp:80-178` 的 `TryResolveCellInterference` 按发射身份、时间门和频带重叠累计有效干扰功率，`:241-252` 接入 detection cell。`RirController.cpp:303-347` 提供入射链路；旧回退路径 `:349-355` 将干扰置零。 |
| 3.2.2.1.1.4 接收机噪声功率计算 | **完整** | `RirRadarEquations.cpp:50-55` 的 `ComputeThermalNoisePower_W` 实现 `k*T*B*F`；`RirSignalDetector.cpp:15-27` 预计算/更新噪声，`:48-59` 合入总噪声；`RirDetectionCellResolver.cpp:238-240` 物理链直接计算。 |
| 3.2.2.1.1.5 目标信号增益 | **完整** | `RirDetectionCellResolver.cpp:236-237` 计算脉冲压缩增益，`:256-257` 将 `target_processing_gain_db` 乘入 SINR 分子（`:253-272` SINR 组装）。 |
| 3.2.2.1.1.6 噪声增益 | **完整** | `RirDetectionCellResolver.cpp:253-272` 将 `noise_processing_gain_db` 解释为噪声代价，`:258-266` 乘到热噪声项。 |
| 3.2.2.1.1.7 杂波信号处理增益 | **完整** | `src/remote_identification_radar/internal/RirPropagationModel.cpp:44-53` 生成环境杂波功率；`RirController.cpp:198-211` 接线；`RirDetectionCellResolver.cpp:252-272` 用 `clutter_suppression_gain_db` 抑制后加入 SINR 分母。 |
| 3.2.2.1.1.8 干扰信号处理增益 | **完整** | `RirDetectionCellResolver.cpp:241-252` 求干扰功率，`:260-267` 用 `jamming_suppression_gain_db` 抑制后加入 SINR 分母。 |
| 3.2.2.1.1.9 恒虚警检测 | **部分** | `src/remote_identification_radar/dwell/RirSignalDetector.cpp:61-80`、`:98-111` 将配置的 `cfar_pfa` 送入 Pd 计算，并按 Pd 随机判决及 SNR 裕量门控；`src/remote_identification_radar/internal/RirRadarEquations.cpp:69-85` 提供门限、Pd 和随机判决包装。当前没有检测统计量与自适应背景门限的比较，也没有训练单元、保护单元、滑窗背景估计或距离-多普勒图 CA-CFAR/OS-CFAR，属于固定 Pfa 参数驱动的统计效能模型。 |

| 需求 | 状态 | 源码对应关系与实现说明 |
| --- | --- | --- |
| 3.2.2.1.2 对指定空域进行搜索 | **部分** | `include/1q/remote_identification_radar/config/RirMissionConfig.h:32-47` 定义扫描角限和步长；`src/remote_identification_radar/session/RirSession.cpp:142-163` 的 `ResolveScanWavePosition` 生成二维波位，`:264-274` 选择扫描波位或指定目标指向。`src/remote_identification_radar/runtime/RirController.cpp:284-296` 仅在 `enable_directional_pattern=true` 时让波位影响离轴增益，而该开关默认关闭（`include/1q/remote_identification_radar/config/RirHardwareConfig.h:87-99`）；`:428-478` 始终遍历全部非零 ID 场景目标，没有按指定角域裁剪目标，也没有重访率、驻留容量或时间预算约束。 |
| 3.2.2.1.3 对目标的角度和距离进行测量 | **部分** | `src/remote_identification_radar/runtime/RirController.cpp:213-220` 的 `ComputeLookAngles` 从场景目标位置/距离计算方位、俯仰和斜距；`src/remote_identification_radar/dwell/RirMeasurementErrorModel.h:37-50` 计算角距误差，`RirController.cpp:222-269` 将其转换为笛卡尔协方差并采样位置。公开的 `RirFeatureMeasurementRecord` 含 `look_az_deg/look_el_deg/range_m`（`include/1q/remote_identification_radar/session/RirFeatureMeasurementTypes.h:87-107`），并由 `RirController.cpp:523-537` 填充；但这些字段是无角度噪声的场景真值上下文（保真度语义见 `docs/review/rir_dual_product_stage_a_2026-08-18.md` 与 RIR-OQ-1），仅在形成有效识别特征时发布，不是检测链产生的独立原始角距量测产品。 |

### 4.2 精确跟踪能力

#### 3.2.2.2 模拟远程雷达的精确跟踪能力

**状态：部分。** 已实现全局多目标关联、三维恒速 KF、可选 IMM 和航迹生命周期；雷达滤波层没有再入目标专用动力学（重力/阻力/弹道系数在推演层 target_inference 建模，见 3.2.1.5.3）。

| 需求 | 状态 | 源码对应关系与实现说明 |
| --- | --- | --- |
| 3.2.2.2.1 对典型目标/再入目标进行跟踪 | **部分** | `src/remote_identification_radar/tracking/RirTrackFilter.cpp:34-66` 初始化、预测和更新六维恒速状态；`src/remote_identification_radar/tracking/RirImmFilter.cpp:30-113` 执行 IMM；`src/remote_identification_radar/tracking/RirTrackLifecycle.cpp:360-435` 处理命中、失配预测和 IMM 启用。可跟踪高速/机动输入，但滤波层未建模重力、阻力、弹道系数和再入阶段（该动力学在推演层 `TargetInferenceEngine.cpp:34-53` 建模并用于落点/发射点推演，雷达滤波不消费）。 |
| 3.2.2.2.2 多目标跟踪能力 | **完整** | `src/remote_identification_radar/tracking/RirTrackAssociator.cpp:66-217` 构造马氏距离代价、执行门控和全局一对一指派；`src/remote_identification_radar/tracking/RirLapjvSolver.cpp:13-16` 调用 LAPJV；`RirTrackLifecycle.cpp:48-252` 独立维护多航迹生命周期、快照和关联种子。 |

### 4.3 S 波段捕获、跟踪和识别能力

#### 3.2.2.3 模拟 S 波段（2-4 GHz）提供全天候捕获、跟踪和识别数据的能力

**状态：部分。** 默认发射频率是 3 GHz，检测、跟踪和识别链闭环；但配置未强制 2-4 GHz，"全天候"只通过外部环境损耗和杂波输入体现。

- 默认 S 波段配置：`include/1q/remote_identification_radar/config/RirHardwareConfig.h:126-138`，`RirTransmitterConfig` 默认 `frequency_hz=3e9`、频率计划 `{3.0e9}`。
- 周期闭环：`RirController.cpp:406-581`，探测 `:423-478`、跟踪 `:480-488`、识别 `:490-549`、输出 `:557-580`。
- 环境接线：`RirController.cpp:198-211`；全仓库未发现云雨衰、降水率或任务可用率模型。

| 需求 | 状态 | 源码对应关系与实现说明 |
| --- | --- | --- |
| 3.2.2.3.1 持续跟踪目标及测量其相应参数 | **部分** | `src/remote_identification_radar/tracking/RirTrackLifecycle.cpp:318-398` 持续执行航迹状态晋级、命中滤波和失配外推；`src/remote_identification_radar/tracking/RirTrackTypes.h:80-110` 的内部 `RirTrackState` 保存位置、速度、加速度、RCS、高斯状态及协方差。公开 `RirOutputFrame` 仅含识别结论和特征量测（`include/1q/remote_identification_radar/session/RirOutputTypes.h:66-75`）；结果层 `RirTrackAttributionRecord` 仅公开位置和速度模（`:77-92`，填充见 `RirController.cpp:569-579`），未公开速度向量、加速度、状态协方差和航迹时刻。 |
| 3.2.2.3.1.1 航迹关联 | **完整** | `RirTrackAssociator.cpp:43-55` 计算马氏距离，`:66-217` 构造全局代价矩阵、门控、LAPJV 指派、新航迹键和未命中集合；`RirTrackLifecycle.cpp:238-252` 生成下一周期关联种子。 |
| 3.2.2.3.1.2 跟踪滤波 | **完整** | `RirTrackFilter.cpp:29-72` 实现三维恒速 Kalman 滤波；`RirImmFilter.cpp:30-113` 实现多模型混合、预测、更新和组合；`RirTrackLifecycle.cpp:360-435` 根据航迹状态选择 KF/IMM 命中或失配路径。 |
| 3.2.2.3.2 根据目标 RCS 实时计算目标探测结果 | **完整** | `RirController.cpp:298-333` 每周期取目标当前 RCS 并填入普通检测和 detection-cell 目标；`RirSignalDetector.cpp:37-46` 和 `RirDetectionCellResolver.cpp:221-235` 将 RCS 用于回波功率，最终在 `RirController.cpp:358-364` 形成探测结果。 |
| 3.2.2.3.3 根据目标特征进行目标识别 | **完整** | `src/remote_identification_radar/recognition/RecognitionObservationBuilder.cpp:51-72` 构建四维特征观测；`RecognitionFeatureDatabase.cpp:257-696` 加载模板库；`RecognitionMatcher.cpp:139-264` 动态加权匹配；`RecognitionTracker.cpp:216-371` 多周期积累并形成类别/型号结论；`RirController.cpp:490-549` 接线。 |
| 3.2.2.3.3.1 运动特征处理 | **完整** | `src/remote_identification_radar/recognition/MotionFeatureExtractor.cpp:20-57` 从已确认航迹提取速度、高度、加速度、横向加速度、直线飞行标志和转弯半径，并按位置协方差生成质量。 |
| 3.2.2.3.3.2 RCS 统计特征处理 | **完整** | `src/remote_identification_radar/recognition/RcsFeatureExtractor.cpp:28-60` 插值/选取视角样本，`:64-119` 计算平均 RCS、标准差、方位/俯仰变化、峰谷差、视角覆盖和质量门。 |
| 3.2.2.3.3.3 极化特征解算 | **完整** | `src/remote_identification_radar/recognition/PolarizationFeatureExtractor.cpp:27-47` 选取双通道视角样本，`:51-89` 计算通道能量比、相对能量差、归一化能量和 SNR 质量。 |
| 3.2.2.3.3.4 宽带一维像特征解算 | **完整，按散射中心级效能模型** | `src/remote_identification_radar/recognition/RangeProfileFeatureExtractor.cpp:32-109` 根据 `c/(2B)` 求距离分辨率，执行噪声门和相邻峰合并，输出目标长度、峰数、前三峰能量集中率、分辨率和质量。没有原始宽带回波 IFFT 或完整 HRRP 图像生成。 |

### 4.4 锁定、应对和资源分配能力

#### 3.2.2.4 模拟锁定和应对远程目标能力

**状态：部分。** 已实现可限期或无限期的指定目标波束指向、识别完成/超时回扫和扫描波位选择；但指定期间仍处理全部场景候选目标，未形成排他锁定，也没有火控、拦截、电子对抗或处置命令链。

| 需求 | 状态 | 源码对应关系与实现说明 |
| --- | --- | --- |
| 3.2.2.4.1 锁定和应对远程目标 | **部分** | `src/remote_identification_radar/session/RirSession.cpp:34-105` 实现指定任务的 pending/acquired/expired 状态机，`:248-302` 在 pending 阶段将波束中心指向在场指定目标并报告完成、缺席回扫或超时；持续周期数为 0 时任务可无限期。`src/remote_identification_radar/runtime/RirController.cpp:428-478` 仍处理全部场景候选，因而这是指定指向/识别任务而非排他目标锁定；`:583-599` 只判断识别是否完成，全仓库未发现"应对"执行链。 |
| 3.2.2.4.2 根据一定的规则分配雷达资源 | **部分** | `RirSession.cpp:248-274` 选择指定目标指向或扫描波位；`RirController.cpp:439-461` 对全部候选按未识别优先、斜距近优先排序，但 `:463-478` 不设容量上限并逐个尝试全部候选。`:543-549` 只是按候选数和检测通过数事后计算驻留计数/时间，未执行预算准入；发射机上限字段 `maximum_peak_power_w`/`maximum_duty_cycle`/`maximum_pulse_energy_j`（`RirHardwareConfig.h:134-136`）在生产源码中无消费点；没有总时间、功率、频率、波形、通道竞争或抢占约束。 |
| 3.2.2.4.2.1 波束扫描 | **完整，按当前扫描策略** | `RirSession.cpp:142-163` 根据方位/俯仰限位、有效波束宽度、步长比例、起始位置和扫描顺序生成二维波位；`:264-274` 每周期选取波位；`RirController.cpp:284-296` 用波位计算离轴方向图增益。 |
| 3.2.2.4.2.2 调度策略 | **部分** | `RirSession.cpp:63-105`、`:248-302` 实现指定任务状态推进及指定指向/回扫；`RirController.cpp:439-461` 仅对全部候选排序，`:463-478` 随后仍遍历全部候选，因此未识别优先和近距优先没有形成受容量约束的资源选择。`:543-549` 为事后统计，不是预算检查或独立资源调度器。 |

## 5. 关键实现入口

### 5.1 红外载荷

| 层次 | 入口 | 行号 |
| --- | --- | --- |
| Session | `src/sbirs_sensor/session/SbirsSession.cpp`，`Step` / `StepWithResult` | 29-42 |
| Controller | `src/sbirs_sensor/runtime/SbirsController.cpp`，`RunOnce` | 19-67 |
| Pipeline | `src/sbirs_sensor/pipeline/SbirsPipeline.cpp`，`RunCycle` | 523-1319 |
| 目标滤波 | `src/sbirs_sensor/pipeline/SbirsTrackingCoordinator.cpp` | 14-285 |
| 生命周期事件 | `src/sbirs_sensor/session/SbirsDetectionLifecycleRecorder.cpp` | 56-238 |

### 5.2 远程识别雷达

| 层次 | 入口 | 行号 |
| --- | --- | --- |
| Session | `src/remote_identification_radar/session/RirSession.cpp`，`StepWithResult` | 195-303 |
| Controller | `src/remote_identification_radar/runtime/RirController.cpp`，`RunCycle` | 406-581 |
| 探测器 | `src/remote_identification_radar/dwell/RirSignalDetector.cpp` | 15-113 |
| 关联 | `src/remote_identification_radar/tracking/RirTrackAssociator.cpp` | 43-255 |
| 航迹生命周期 | `src/remote_identification_radar/tracking/RirTrackLifecycle.cpp` | 48-435 |
| 识别积累 | `src/remote_identification_radar/recognition/RecognitionTracker.cpp` | 216-371 |

### 5.3 估计/推演/决策层

| 层次 | 入口 | 行号 |
| --- | --- | --- |
| 融合引擎 | `src/fusion/FusionEngine.cpp`，`FusionEngine::Impl::Update` | 263-307 |
| 源适配 | `include/1q/fusion/SensorAdapters.h`（SBIRS/RIR → `DetectionRecord`） | 38-103 |
| UKF 原语 | `src/common/estimation/UnscentedPredictor.h` / `UnscentedUpdater.h` / `UnscentedTransform.h` | 41 / 44 / 全文件 |
| 逐航迹无迹滤波 | `src/fusion/FusionEngine.cpp`，`InitFilter` / `PredictTrack` / `ApplyFilterSample` / `AdvanceFilters` | 499-651 |
| 轨迹推演 | `src/target_inference/TargetInferenceEngine.cpp`，`Infer` | 165-400 |
| 威胁评估 | `src/threat_assessment/ThreatEvaluator.cpp`，`Evaluate` | 87-144 |

## 6. 需求文本异常与版本变化说明

以下异常不影响按章节语义建立对应关系，但建议在正式需求基线中修正：

- `3.21` 应为 `3.2.1`。
- 多处需求标识被截断，如 3.2.1.2、3.2.1.3、3.2.1.5.3.3、3.2.2.3。
- `TCZBFZ-XYDGD-TCZBFZ-XYDGD-...` 存在重复前缀。
- `TCZBFZ-YCSBLD+MNTC-03` 的 `+` 疑为 `-`。
- 3.2.2.4.2.1、3.2.2.4.2.2 使用了 `TCZBFZ-XYDGD-ZYDD-*`，与远程识别雷达模块前缀不一致。
- 旧 OCR 文本中的"卫星姿态误差""摄动定位误差"已按最新目录分别修正为"卫星自身定位误差""安装矩阵误差"；姿态误差与 FOV 误差仍作为等效角误差预算存在于误差模型中。
- 旧 OCR 文本中的"轨迹平滑与修正""关机点预测"已按最新目录分别修正为"目标轨迹预测""关机点判断"；轨迹平滑作为正文规定的预测处理步骤纳入 3.2.1.5.2 能力边界，不再作为独立目录项。
- 旧 OCR 的"规模目标识别"按同项目材料恢复为"集群多目标数量识别"；最新目录中 `3.2.1.5.1` 处于折叠状态，其不可见子项结合已有需求编号保留。
- "扫措/探则/跟综/融台/躁声"等旧 OCR 结果按最新图片修正为"扫描/探测/跟踪/融合/噪声"。
- 《功能需求》正文位于需求方工作区（`docs/xq/功能需求.doc`），未随本仓库提供；本文正文语义按第二版对照文档转述采纳，正式定稿前建议以需求方原文复核各"正文要求"表述。

## 7. 结论边界

- "未发现"为全仓库生产源码口径（`src/`、`include/`）；tests/ 与 examples/ 仅作佐证注记，不计为实现。
- 仓库分层契约（`docs/common/contract.md:146-193`）规定传感器层不得承载估计/推演逻辑；判定某需求时先核对其架构归属层（传感器/适配/估计/推演/决策）再定级，避免把"架构上不属于传感器层"误报为"仓库无此能力"。
- 红外模块中的多 NFOV 通道不是多传感器；WFOV 到 NFOV cue 不是跨传感器接力；IMM 多模型组合不是多源信息融合。
- 估计层 fusion 的同键跨源归并与样本级置信度累积是多源融合，但没有协方差交叉/航迹级融合，也不是接力协议。
- 估计层 UKF 默认开启（`enable_track_filtering` 默认 true，可关回无滤波）；红外模块自身的 EKF 当前周期校正和下一周期 CV predict 不是正文要求的动力学轨迹预测；短时 `p+v*t` 外推不是落点、发射点或关机点分析。
- 推演层落点/发射点预测基于无推力弹道模型，为本库设计选择，不建模推力/质量；关机点判断超出范围。红外模块自身的 EKF 当前周期校正和下一周期 CV predict 不是正文要求的动力学轨迹预测；短时 `p+v*t` 外推不是落点、发射点分析。
- 红外模块 Estimated 航迹首捕用真值初始化、滤波后验角度作为 raw output，均为已登记的开放议题（SBIRS-OQ-4、TARGET-OQ-3，`docs/common/open_questions.md`），非未公开的隐藏行为。
- 雷达模块的统计级 Pfa/Pd 判决不是带训练/保护单元的完整 CA-CFAR/OS-CFAR。
- 雷达模块的指定目标驻留是锁定/识别任务，不包含火控或对抗处置。
