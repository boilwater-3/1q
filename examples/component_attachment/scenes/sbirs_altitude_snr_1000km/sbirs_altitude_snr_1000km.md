# 场景预期表：sbirs_altitude_snr_1000km（天基通道高度专项）

## 场景元数据

| 项 | 值 |
| --- | --- |
| 场景文件 | `scenes/sbirs_altitude_snr_1000km/sbirs_altitude_snr_1000km.json` |
| 场景意图 | 被测通道：SBIRS（主）+ Fusion；被测行为：卫星高度对 FOV/信噪比的影响（链路 1/R² 标度 + wide/narrow 双门限边界）；验证深度：L2 |
| 构建模式 | release（运动学回退，与 ctest 一致；SBIRS 几何与平台动力学解耦，FD 开/关行为一致） |
| 日志模式 | delta + key（默认；本轮探针用 summary 复核） |
| 单变量 | `sbirs_satellite.altitude_m` 500→1000 km；**附带把 targets `v_north_mps` ±5 归零**（消除基线目标穿越质心的干扰变量，见结论 4） |
| 运行日期 | 2026-08-08 |

## 链路先验（手算，基线 500 km 实测 SNR 20.0/21.7 linear 标定）

SNR ∝ 1/R²（`ComputeReceivedPowerW`：点源辐射/孔径面积/R²，背景 radiance=0 → 探测器噪声主导）。
wide 门限 = 4.0 linear（6.02 dB）、narrow 门限 = 6.0 linear（7.78 dB）。

| 高度 | SNR_1001 手算 | 实测 | SNR_1002 手算 | 实测 | 预期行为 |
| --- | --- | --- | --- | --- | --- |
| 500 km（基线） | 20.0 | 20.0 | 21.7 | 21.7 | 发现 + 捕获跟踪 |
| 800 km（探针） | 7.81 | 7.8 | 8.48 | 8.5 | 发现 + 捕获跟踪（23 事件） |
| **1000 km（本场景）** | **5.0** | **∈ [4.0, 6.0)** | **5.4** | **∈ [4.0, 6.0)** | **发现但捕获永远失败（0 事件）** |
| 1500 km（探针） | 2.22 | 2.227 | 2.41 | 2.404 | SNR 排除（0 事件 + 诊断带量值） |

实测与手算误差 <0.3%：**链路 R² 标度验证成立**（探针为临时场景文件，不落库）。

## 预期事件表

| 通道 | 行为 | 预期周期窗 | 预期量级 | 预期计数 | 实测 | 判定 |
| --- | --- | --- | --- | --- | --- | --- |
| SBIRS | 首发现/丢失/跟踪锁定 | 全程 | — | **0**（初始预期"每 36 周期首发现循环"落空，见结论 2） | 0/0/0 | 通过（预期修正） |
| SBIRS | WFOV 发现（视图"已检测"） | 扫描覆盖窗口（1001: 29,65,…; 1002: 11,47,…） | SNR ∈ [4.0, 6.0) | **0 周期**（通道空闲 → 直接捕获判定，无 slewing 记录） | 0 周期（视图全程"不在输出"） | 通过 |
| SBIRS | 排除诊断 | 扫描离开窗口时 | `target_out_of_wfov` | 每窗口 2-3 条 | 出现（每周期 1 条，与扫描相位同步） | 通过 |
| SBIRS | SNR 排除诊断 | 全程 | `target_snr_below_threshold` | 0（SNR ≥ wide） | 0 | 通过 |
| AR/ESR/EOS | 探测事件 | 与基线一致 | — | ≥ 基线部分量 | 正常（events=301） | 通过 |
| SAR | 产品事件 | cycle 19 起 | L1 成像 | 1（key） | 1 | 通过 |
| Fusion | 融合目标 | cycle 1 起 | AR/EOS 主导（SBIRS 权重 0.6 无输入） | 峰值 ≥ 1 | 正常 | 通过 |
| Decision | command_issued | 置信度 ≥ 3.0 | 一次 | 1 | cycle 2 | 通过 |

## 冒烟下限

min_key_events=1 / **min_sbirs_events=0** / min_sar_products=1 / min_fused_targets=1
（SBIRS 零事件是**本场景预期结果**而非链路断裂——按零产出场景规则显式置 0，exit=0 通过）

## 结论

**判定：通过（含 1 项预期修正）**。无库问题、无场景问题。四项实测知识：

1. **链路 1/R² 标度精确成立**（<0.3%）：1000 km 使 SNR 从 13 dB 级落到 [6.02, 7.78) dB
   区间——WFOV 门（4.0）过、NFOV 捕获门（6.0）不过。高度是 SBIRS 探测边界的干净调节变量；
2. **"发现-不捕获"区间行为**（初始预期修正）：1000 km 下两目标每扫描窗口（36 周期）被
   WFOV 发现，但 **SearchAndStare 下 ATP 一步到位（LOS 角距 ~10° < 30°/s 限速）→ 无
   slewing WFOV 记录 → 直接捕获判定 → 失败 → detected=false 记录**。视图"不在输出"恒定、
   事件 0 条。**不同于基线 1001 的"首发现+丢失循环"**——那是通道被占（kSchedulerSkipped）
   的形态，本场景通道空闲，形态是捕获失败（见结论 3）；
3. **pipeline 摘要计数语义（2026-08-08 已修复）**：旧格式 `detections=1/2` 把
   detected=false 的捕获失败记录也算进"检测数"，1000 km 场景 cycle 29-31 摘要显示
   detections=1/2 但实际 0 探测。现摘要改为 `detected=0/2 records=1`——detected 只
   统计成功探测、records 为全部记录数，两数分离；
4. **基线预期表 SBIRS 行描述修正**：基线 targets 带 `v_north_mps=±5` → 两目标以 10 m/s
   相对接近、**cycle ~200 穿越质心** → 相对卫星的 az 从 120°/-60° 翻转到 -60°/120°（星下点
   附近 az 对 1 km 级偏移敏感）→ 1002 穿越瞬间 NFOV 几何门失败 → coasting（cycle 201）
   → 连续失败 2 周期丢锁（202）→ 209 重捕获。基线"星下点凝视全覆盖"的描述不准确，
   实际由扫描相位门控（每 36 周期）驱动，且 1001 全程只是调度跳过（从未获得 NFOV 通道）。

**原型库回写**（`scenario_archetypes.md` 原型 6）：高度是 SNR 探测边界的单变量调节旋钮
（1/R² 标度，1000 km 落在"发现-不捕获"区间）；基线星下点行为的真实驱动是 v_north 漂移
穿越质心 + 扫描相位门控 + 单通道竞争；triage 注意 detections 摘要计数含失败记录。
