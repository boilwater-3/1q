# JSBSim 飞行器高层机动原语核心算法设计

JSBSim 原生主要提供基础的飞行控制系统（FCS）组件（如 PID 控制器、开关、加法器、航路点组件等）以及基于时间触发的脚本事件系统。针对高层机动需求，需在业务逻辑层（1Q 端）进行相应的制导与控制律开发。本文档分章节详细定义了五种主要机动模式的核心算法。

## 1. 方向机动 (Heading Maneuver)

**机动描述**：控制飞行器朝向指定的航向角飞行。
**JSBSim 支撑情况**：✅ 内置（航向保持）。
**复杂度**：极低

### 核心算法实现
直接复用 JSBSim 内置的自动驾驶仪（Autopilot）功能。通过设置航向设定点，JSBSim 内部的航向保持 PID 闭环会自动生成滚转角指令并驱动副翼，完成方向机动。

**主要步骤**：
1. **指令下发**：1Q 端向 JSBSim 发送目标航向角设定值。
   - 目标属性：`ap/heading_setpoint`
2. **模式激活**：开启 JSBSim 的航向保持模式。
   - 目标属性：`ap/heading_hold` 设置为 1。
3. **状态监控**：读取当前真实航向（如 `attitude/heading-true-rad` 或相关属性），计算误差以判定机动是否完成（进入稳态误差范围）。

---

## 2. 固定点机动 (Point-to-Point Maneuver)

**机动描述**：控制飞行器飞向空间中指定的单一固定坐标点（经度、纬度、高度）。
**JSBSim 支撑情况**：⚠️ 部分支持（`FGWaypoint` 提供方位/距离计算，但制导律需自建）。
**复杂度**：~100 行代码

### 核心算法实现
基于当前位置与目标点位置，计算目标方位角，并将其转化为“方向机动”的控制指令。同时需结合高度和速度/油门控制。

**主要步骤**：
1. **方位角与距离计算**：
   - 使用大圆航线算法（Haversine 公式）或直接利用 JSBSim 的 `FGWaypoint` 组件，计算当前位置（$\phi_{curr}, \lambda_{curr}$）到目标点（$\phi_{target}, \lambda_{target}$）的期望方位角（Bearing）和剩余距离（Distance）。
2. **航向制导 (Lateral Control)**：
   - 将计算出的期望方位角作为目标航向，赋值给 `ap/heading_setpoint`。
   - 随着位置的不断更新，实时重算方位角并更新航向设定点，消除侧风引起的航迹偏差。
3. **纵向与推力控制 (Longitudinal & Throttle)**：
   - **高度控制**：计算高度差，通过爬升率/下降率映射，更新高度保持设定点（`ap/altitude_setpoint`）。
   - **速度控制**：通过 PID 控制器计算速度误差，动态调节油门指令（`fcs/throttle-cmd-norm`）。
4. **到达判定**：当剩余距离 $d < d_{threshold}$（如 50 米）时，判定为到达该固定点。

---

## 3. 航路点机动 (Waypoint Sequence Maneuver)

**机动描述**：控制飞行器按照预设的航路点序列依次飞行。
**JSBSim 支撑情况**：⚠️ 部分支持（同固定点机动，需外挂航路点序列管理器）。
**复杂度**：~200 行代码

### 核心算法实现
基于“固定点机动”算法，增加状态机用于航路点列表的遍历、管理与平滑切换。

**主要步骤**：
1. **序列管理初始化**：加载由一组三维坐标 $[W_1, W_2, ..., W_n]$ 组成的航路点列表，并初始化活动航路点索引 $i = 1$。
2. **单段制导**：将当前活动航路点 $W_i$ 作为“固定点机动”的目标，执行上述方位角计算与航向、高度闭环控制。
3. **切换判定 (Waypoint Transition)**：
   - 计算飞行器到 $W_i$ 的水平投影距离 $d$。
   - 当 $d < r_{accept}$（接受半径，考虑转弯半径设置，避免过冲）时，触发切换逻辑。
4. **序列推进**：
   - 索引更新：$i = i + 1$。
   - 若 $i > n$，则航路点任务执行完毕，转入盘旋待命或返航模式。
   - 若未结束，将新航路点 $W_{i+1}$ 设为当前目标，立即平滑更新 `ap/heading_setpoint`。

---

## 4. 蛇形机动 (Weaving / Snake Maneuver)

**机动描述**：飞行器在保持总体前进方向的同时，以类似正弦波的轨迹进行周期性的左右侧向机动。常用于规避防空火力或雷达探测。
**JSBSim 支撑情况**：❌ 不支持（需 1Q 端生成动态航向偏置）。
**复杂度**：~50 行代码

### 核心算法实现
在基础航向角上叠加一个随时间或位移变化的低频正弦偏置项，驱动飞行器进行横向摆动。

**主要步骤**：
1. **参数定义**：
   - 基础航向 $\Psi_{base}$（总体前进方向）
   - 摆动幅度 $A_{weave}$（最大偏航角偏移，例如 30 度）
   - 摆动周期 $T_{weave}$（完成一次左右摆动的时间，或角频率 $\omega = 2\pi/T_{weave}$）
2. **实时航向偏置计算 (1Q 端高频运行)**：
   - 获取当前系统时间或飞行时间 $t$。
   - 计算期望航向：$\Psi_{target}(t) = \Psi_{base} + A_{weave} \cdot \sin(\omega t)$。
3. **控制律下发**：
   - 将实时计算的 $\Psi_{target}(t)$ 连续下发至 `ap/heading_setpoint`。
   - JSBSim 的底层航向 PID 将努力追踪这个不断变化的正弦设定点，从而产生连续的左右滚转与转向。

---

## 5. 滚筒机动 (Barrel Roll Maneuver)

**机动描述**：飞行器沿纵轴进行 360 度滚转，同时可能伴随一定的俯仰变化（螺旋前进）。一种典型的近战规避或战术动作。
**JSBSim 支撑情况**：❌ 不支持（需 1Q 端直接接管舵面或姿态控制，生成时序化指令）。
**复杂度**：~100 行代码

### 核心算法实现
由于自动驾驶仪（Autopilot）通常不适用于此类极端姿态（包线边缘或大迎角/大滚转角机动），需要暂时接管/禁用 AP，直接通过开环时序控制或特定的大角度姿态 PID 输出底层的操纵面指令。

**主要步骤**：
1. **前置条件与模式切换**：
   - 检查动能与高度是否满足机动安全条件。
   - 禁用当前的航向保持/姿态保持 AP。
2. **时序化舵面控制 (1Q 端或 JSBSim 脚本时序)**：
   - **阶段一（拉起阶段）**：给定适度向上的升降舵指令（`fcs/elevator-cmd-norm` 为负，具体视坐标系而定），增加俯仰角，积累垂直势能并建立迎角。
   - **阶段二（滚转启动）**：同时施加持续的最大或次大副翼指令（`fcs/aileron-cmd-norm`），开始沿纵轴滚转。可配合方向舵（`fcs/rudder-cmd-norm`）进行协调转弯以抵消不利偏航。
   - **阶段三（过载维持与监测）**：监控当前滚转角（`attitude/roll-rad`）。当滚转角跨越 180 度（倒飞状态）并继续增加时，保持副翼输入，并根据需要微调升降舵以防掉高度。
   - **阶段四（改出）**：当滚转角即将接近 360 度（0 度）前，基于当前的滚转角速率（`velocities/p-rad_sec`）提前反向回中副翼指令，制止滚转趋势，恢复水平飞行。
3. **恢复控制**：重新激活常规的自动驾驶仪（如航向/高度保持），恢复平飞。
