namespace oneq.replay.airborne_radar.fb;

table Vector3f {
  x:float;
  y:float;
  z:float;
}

table EulerAnglesDeg {
  yaw_deg:float;
  pitch_deg:float;
  roll_deg:float;
}

table PoseState {
  position_m:Vector3f;
  velocity_mps:Vector3f;
  attitude_deg:EulerAnglesDeg;
}

table TargetFeature {
  external_target_id:ulong;
  velocity_mps:Vector3f;
  /// @deprecated Scene replay no longer carries derived speed; encoder writes from velocity and decoder uses it only as legacy fallback when velocity_mps is zero.
  current_track_speed:float;
  current_track_rcs:float;
  range_m:float;
  has_cartesian_position:bool;
  position_m:Vector3f;
  target_swerling_type:int;
}

table RadarCycleInput {
  dt_sec:float = 1.0;
  platform_pose:PoseState;
  target_features:[TargetFeature];
}

table TrackOutputFrame {
  cycle_index:uint;
  published_track_count:ulong;
  batch_id:ulong;
  confirmed_track_count:ulong;
  contains_lost_tracks:bool;
  tracks:[TrackStateSnapshot];
}

table DecisionTrackStateSnapshot {
  association_key:ulong;
  external_target_id:ulong;
  status:int;
  position_x:float;
  position_y:float;
  position_z:float;
  velocity_x:float;
  velocity_y:float;
  velocity_z:float;
  speed:float;
  acceleration_x:float;
  acceleration_y:float;
  acceleration_z:float;
  acceleration:float;
  rcs:float;
  jamming_detected:bool;
  hit_count:uint;
  miss_count:uint;
}

table TrackStateSnapshot {
  state:DecisionTrackStateSnapshot;
}

table RadarCycleResult {
  track_output_frame:TrackOutputFrame;
  validation_issue_count:uint;
  executed_this_cycle:bool;
}

table FailureMarker {
  error_code:string;
  message:string;
  location:string;
  has_cycle_index:bool;
  cycle_index:uint;
  has_sim_time_sec:bool;
  sim_time_sec:double;
  diagnostics:string;
  has_last_event_sequence:bool;
  last_event_sequence:ulong;
}

root_type RadarCycleInput;
file_identifier "ARCI";
